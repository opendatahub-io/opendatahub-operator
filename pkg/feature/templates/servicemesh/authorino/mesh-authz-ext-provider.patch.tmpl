apiVersion: maistra.io/v2
kind: ServiceMeshControlPlane
metadata:
  name: {{ .ControlPlane.Name }}
  namespace: {{ .ControlPlane.Namespace }}
spec:
  techPreview:
    meshConfig:
      extensionProviders:
      - name: {{ .AppNamespace }}-odh-auth-provider
        envoyExtAuthzGrpc:
          service: {{ .AuthorinoConfigs.Name }}-authorino-authorization.{{ .AppNamespace }}.svc.cluster.local
          port: 50051
