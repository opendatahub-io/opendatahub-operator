apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .AuthorinoConfigs.Name }}
  namespace: {{ .AppNamespace }}
spec:
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "true"
