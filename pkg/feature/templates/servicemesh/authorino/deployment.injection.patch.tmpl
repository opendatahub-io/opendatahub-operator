apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .AuthorinoConfigs.Name }}
  namespace: {{ .AuthorinoConfigs.Namespace }}
spec:
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "true"
