apiVersion: maistra.io/v2
kind: ServiceMeshControlPlane
metadata:
  name: {{ .ControlPlane.Name }}
  namespace: {{ .ControlPlane.Namespace }}
spec:
  techPreview:
    meshConfig:
      extensionProviders:
      - name: {{ .Auth.ExtensionName }}
        envoyExtAuthzGrpc:
          service: {{ .Auth.ProviderName }}-authorino-authorization.{{ .Auth.Namespace }}.svc.cluster.local
          port: 50051
