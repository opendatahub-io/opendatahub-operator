apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ .Name }}
  namespace: {{ .Namespace }}
{{- if .Channel }}
spec:
  channel: {{ .Channel }}
{{- end }}
{{- if .InstalledCSV }}
status:
  installedCSV: {{ .InstalledCSV }}
{{- end }}
