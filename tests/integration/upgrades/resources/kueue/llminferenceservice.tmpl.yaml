apiVersion: serving.kserve.io/v1alpha2
kind: LLMInferenceService
metadata:
  name: {{ .Name }}
  namespace: {{ .Namespace }}
{{- if .QueueName }}
  labels:
    kueue.x-k8s.io/queue-name: {{ .QueueName }}
{{- end }}
spec: {}
