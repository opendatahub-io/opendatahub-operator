# This patch adds the service-ca annotation to the metrics Service
# for OpenShift certificate provisioning.
apiVersion: v1
kind: Service
metadata:
  name: controller-manager-metrics-service
  namespace: system
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: opendatahub-operator-metrics-tls
