apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/created-by: opendatahub-operator
    app.kubernetes.io/part-of: opendatahub-operator
    app.kubernetes.io/managed-by: kustomize
  name: controller-manager-service-cert
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: opendatahub-operator-controller-manager-service-cert
spec:
  ports:
    - port: 443
      protocol: TCP
      targetPort: 9443
  selector:
    control-plane: controller-manager
