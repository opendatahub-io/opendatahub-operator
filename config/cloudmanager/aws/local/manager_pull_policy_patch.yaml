apiVersion: apps/v1
kind: Deployment
metadata:
  name: aws-cloud-manager-operator
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: manager
        imagePullPolicy: IfNotPresent
