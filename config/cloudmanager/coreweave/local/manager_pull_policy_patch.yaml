apiVersion: apps/v1
kind: Deployment
metadata:
  name: coreweave-cloud-manager-operator
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: manager
        imagePullPolicy: IfNotPresent
