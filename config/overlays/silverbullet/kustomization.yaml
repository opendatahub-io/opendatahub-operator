apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../manager
- ../../rbac
- ../../crd

namespace: opendatahub-operator-system
namePrefix: opendatahub-operator-

images:
- name: controller
  newName: REPLACE_IMAGE

patches:
- path: silverbullet-manager.yaml
