---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: dscmonitorings.services.opendatahub.io
spec:
  group: services.opendatahub.io
  names:
    kind: DSCMonitoring
    listKind: DSCMonitoringList
    plural: dscmonitorings
    singular: dscmonitoring
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: DSCMonitoring is the Schema for the dscmonitorings API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DSCMonitoringSpec defines the desired state of DSCMonitoring
            properties:
              foo:
                description: Foo is an example field of DSCMonitoring. Edit dscmonitoring_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: DSCMonitoringStatus defines the observed state of DSCMonitoring
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
