apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: opendatahub-operator
  name: controller-manager-metrics-service
  namespace: system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8080
  selector:
    control-plane: opendatahub-operator
