# Default manager image for the workbenches module operator Deployment.
# Community default; product builds override via RELATED_IMAGE_* (see values.yaml
# controllerImage.relatedImageEnv and opendatahub-operator ModuleConfig.ControllerImage).
workbenches-operator-image=quay.io/opendatahub/odh-workbenches-operator:odh-stable
