apiVersion: v1
kind: ConfigMap
metadata:
  name: odh-workbenches-config
  namespace: {{ include "workbenches-operator.applicationsNamespace" . | quote }}
  labels:
    platform.opendatahub.io/managed: "true"
data:
  distribution.name: Standalone
  distribution.version: {{ .Chart.AppVersion | default "0.0.0" | quote }}
