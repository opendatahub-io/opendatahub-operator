apiVersion: v1
kind: Service
metadata:
  labels:
    app: authorino
    authorino-component: authorino-webhooks
  name: authorino-webhooks
  namespace: {{ .Values.operatorNamespace }}
spec:
  ports:
    - port: 443
      protocol: TCP
      targetPort: 9443
  selector:
    app: authorino
    authorino-component: authorino-webhooks
