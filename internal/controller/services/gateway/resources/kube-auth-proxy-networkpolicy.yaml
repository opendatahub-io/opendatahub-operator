---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{.KubeAuthProxyServiceName}}
  namespace: {{.GatewayNamespace}}
  labels:
    app: {{.KubeAuthProxyServiceName}}
    {{.ComponentLabelKey}}: {{.ComponentLabelValue}}
spec:
  podSelector:
    matchLabels:
      app: {{.KubeAuthProxyServiceName}}
  policyTypes:
    - Ingress
    - Egress
  egress:
    # Allow DNS resolution (cluster DNS and external OIDC hostnames)
    - ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    # Allow HTTPS to Kubernetes API server (443, 6443) and external OIDC providers (443)
    - ports:
        - protocol: TCP
          port: 443
        - protocol: TCP
          port: 6443
  ingress:
    # Allow traffic from gateway/envoy pods for authentication
    - from:
      - podSelector:
          matchLabels: # match whats applied on the pods by the kube-auth-proxy
            {{.GatewayNameLabelKey}}: {{.GatewayName}}
        namespaceSelector:
          matchLabels:
            kubernetes.io/metadata.name: {{.GatewayNamespace}}
      ports:
        - protocol: TCP
          port: {{.GatewayHTTPSPort}}

    # Allow metrics collection from OpenShift monitoring
    - from:
      - namespaceSelector:
          matchLabels:
            kubernetes.io/metadata.name: openshift-monitoring
      ports:
        - protocol: TCP
          port: {{.AuthProxyMetricsPort}}

    # Allow metrics collection from user workload monitoring
    - from:
      - namespaceSelector:
          matchLabels:
            kubernetes.io/metadata.name: openshift-user-workload-monitoring
      ports:
        - protocol: TCP
          port: {{.AuthProxyMetricsPort}}
