apiVersion: perses.dev/v1alpha1
kind: Perses
metadata:
  name: data-science-perses
  namespace: {{.Namespace}}
spec:
  containerPort: 8080
  config:
    database:
      file:
        extension: "yaml"
        folder: "/var/lib/perses"
