---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: namespace-restricted-metrics
  namespace: {{.Namespace}}
  labels:
    app: namespace-restricted-metrics
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: namespace-restricted-metrics
rules:
  - apiGroups: ["authentication.k8s.io"]
    resources: ["tokenreviews"]
    verbs: ["create"]
  - apiGroups: ["authorization.k8s.io"]
    resources: ["subjectaccessreviews"]
    verbs: ["create"]
  - apiGroups: [""]
    resources: ["pods/metrics"]
    verbs: ["get"]
  - apiGroups: [""]
    resources: ["namespaces"]
    verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: namespace-restricted-metrics
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: namespace-restricted-metrics
subjects:
  - kind: ServiceAccount
    name: namespace-restricted-metrics
    namespace: {{.Namespace}}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: namespace-restricted-metrics-config
  namespace: {{.Namespace}}
  labels:
    app: namespace-restricted-metrics
data:
  kube-rbac-proxy.yaml: |
    authorization:
      rewrites:
        byQueryParameter:
          name: "namespace"
      resourceAttributes:
        apiVersion: v1
        resource: pods
        subresource: metrics
        namespace: "{{ "{{ .Value }}" }}"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: namespace-restricted-metrics
  namespace: {{.Namespace}}
  labels:
    app: namespace-restricted-metrics
spec:
  replicas: 1
  selector:
    matchLabels:
      app: namespace-restricted-metrics
  template:
    metadata:
      labels:
        app: namespace-restricted-metrics
    spec:
      serviceAccountName: namespace-restricted-metrics
      containers:
        # kube-rbac-proxy for authentication and authorization
        - name: kube-rbac-proxy
          image: gcr.io/kubebuilder/kube-rbac-proxy:v0.15.0
          args:
            - --secure-listen-address=0.0.0.0:8443
            - --upstream=http://127.0.0.1:9091/
            - --config-file=/etc/kube-rbac-proxy/kube-rbac-proxy.yaml
            - --tls-cert-file=/etc/tls/private/tls.crt
            - --tls-private-key-file=/etc/tls/private/tls.key
            - --logtostderr=true
            - --v=0
          ports:
            - containerPort: 8443
              name: https
              protocol: TCP
          resources:
            limits:
              cpu: 20m
              memory: 40Mi
            requests:
              cpu: 10m
              memory: 20Mi
          terminationMessagePolicy: FallbackToLogsOnError
          volumeMounts:
            - name: config
              mountPath: /etc/kube-rbac-proxy
              readOnly: true
            - name: tls
              mountPath: /etc/tls/private
              readOnly: true
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
        # prom-label-proxy for namespace filtering
        - name: prom-label-proxy
          image: quay.io/prometheuscommunity/prom-label-proxy:v0.8.0
          args:
            - --insecure-listen-address=127.0.0.1:9091
            - --upstream=http://data-science-monitoringstack-prometheus.{{.Namespace}}.svc:9090
            - --label=namespace
            - --enable-label-apis
          ports:
            - containerPort: 9091
              name: http
              protocol: TCP
          resources:
            limits:
              cpu: 20m
              memory: 40Mi
            requests:
              cpu: 10m
              memory: 20Mi
          terminationMessagePolicy: FallbackToLogsOnError
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
      volumes:
        - name: config
          configMap:
            name: namespace-restricted-metrics-config
        - name: tls
          secret:
            secretName: namespace-restricted-metrics-tls
      nodeSelector:
        kubernetes.io/os: linux
      priorityClassName: system-cluster-critical
      securityContext:
        runAsNonRoot: true
      tolerations:
        - effect: NoSchedule
          key: node-role.kubernetes.io/master
          operator: Exists
---
apiVersion: v1
kind: Service
metadata:
  name: namespace-restricted-metrics
  namespace: {{.Namespace}}
  labels:
    app: namespace-restricted-metrics
  annotations:
    service.alpha.openshift.io/serving-cert-secret-name: namespace-restricted-metrics-tls
spec:
  type: ClusterIP
  ports:
    - name: https
      port: 8443
      protocol: TCP
      targetPort: https
  selector:
    app: namespace-restricted-metrics
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: namespace-restricted-metrics
  namespace: {{.Namespace}}
  labels:
    app: namespace-restricted-metrics
spec:
  to:
    kind: Service
    name: namespace-restricted-metrics
    weight: 100
  port:
    targetPort: https
  tls:
    termination: reencrypt
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
