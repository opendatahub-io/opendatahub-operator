---
id: DSC-V3-001-02
parent: DSC-V3-001
jira: RHOAIENG-94812
title: Atomically cut over to v3, add v2 conversion, and remove v1
status: completed
start_date: 2026-09-18T10:00:00Z
end_date: 2026-09-18T11:08:00Z
assignee: Codex
created_date: 2026-09-18
last_updated: 2026-09-18T11:08:00Z
depends_on:
  - DSC-V3-001-01
blocked_by: []
---

# DSC-V3-001-02: Atomically cut over to v3 and remove v1

## Lifecycle and scope

Read the authoritative local documents, `001.md`, and the completed
`001-01.md` Outcomes. Update this file's metadata and Outcomes on start. Follow
DEC-002 through DEC-007 and DEC-014. V2 and v3 are schema-identical in this
subtask; conversion therefore preserves the complete object without
transforming fields.

## Indicative starting files

- Hub/spoke and deletion: `api/datasciencecluster/v1/*`,
  `api/datasciencecluster/v2/datasciencecluster_conversion.go`, and the new v3
  hub/type files. Preserve useful cases from
  `api/datasciencecluster/v1/datasciencecluster_conversion_test.go` before
  deleting the package.
- Admissions and endpoint: `internal/webhook/webhook.go`,
  `internal/webhook/datasciencecluster/v1/*`,
  `internal/webhook/datasciencecluster/v2/*`, the new v3 admission package, and
  `internal/webhook/envtestutil/envtestutil.go`.
- Version awareness and comparison: `pkg/cluster/gvk/gvk.go`,
  `pkg/dsc/compare/v2only.go`, `pkg/dsc/compare/v2only_test.go`, and
  `pkg/utils/test/scheme/scheme.go`.
- Generated conversion configuration:
  `config/crd/patches/{webhook,cainjection}_in_datasciencecluster_datascienceclusters.yaml`
  and the same two files under `config/rhoai/crd/patches`.
- Coordinate edits to `PROJECT` and `cmd/main.go` with 001-01; they are central
  registration files and must have only one active owner at a time.

## Implementation

1. In one compiling change, move `conversion.Hub` and
   `+kubebuilder:storageversion` from v2 to v3, make v2 the
   `conversion.Convertible` spoke, and implement explicit typed
   `ConvertTo`/`ConvertFrom` against the v3 hub. Do not leave a commit where v3
   is the hub but v2 conversion is absent.
2. Copy metadata, spec, and status semantically unchanged. Do not use JSON
   serialization, `unsafe`, defaulting, validation, normalization, logging-based
   recovery, cluster access, or proposed field mappings.
3. Register controller-runtime `/convert` using the v2 DSC type before removing
   the v1 registration later in this same atomic change.
4. Add distinct `DataScienceClusterV2` and `DataScienceClusterV3` GVK constants
   and bind each admission path to its own GVK.
5. Add v3 defaulting and validation by porting identical v2 rules. Preserve v2
   admissions independently.
6. Generate conversion configuration with
   `conversionReviewVersions: [v1]`; this is the Kubernetes protocol version,
   not a DSC served-version list.
7. Replace/adapt `pkg/dsc/compare/v2only.go` with a normalized v2/v3 OpenAPI
   guard covering paths, types, requiredness, nullability, defaults, enums, CEL,
   and list/map semantics. Its initial expected-difference set is empty.
   Define the shared machine-readable registry now: every future inventory
   entry has a stable `case_id`, v2/v3 paths, changed semantics, and executable
   forward, backward, v2-round-trip, and v3-round-trip cases. The guard fails
   when any category is missing or was not executed by the shared runner.
8. In that same compiling change, remove the v1 API and converter, scheme and
   PROJECT entries, admissions, webhook registration, GVK/constants, and
   compiled v1 consumers. The current v1 converter asserts that v2 is the hub,
   so it must not remain after the v3 hub switch. Do not create a temporary
   v1 <-> v3 converter.
9. Port useful v1-located behavior tests to v2/v3 compatibility tests. Inventory
   `conversion.opendatahub.io/aigateway-state`; delete it only if no remaining
   consumer needs it.
10. Regenerate and assert the CRD moves storage from v2 to v3, removes v1, and
    activates v2 conversion atomically.

## Tests

- Direct v2 -> v3 and v3 -> v2 equality with all metadata, components,
  deprecated fields, status, conditions, releases, and related objects filled.
- Both whole-object round trips, repeated-conversion idempotence, and empty,
  absent, `Managed`, and `Removed` values.
- Negative one-hop cases so inverse but mutually wrong mappings cannot pass by
  cancellation.
- Real `/convert` ConversionReview requests in both directions with one and
  multiple objects, including status and metadata preservation.
- Independent v2/v3 defaulting, validation, GVK, singleton, warnings, and
  update tests.
- OpenAPI guard negative tests proving each supported semantic category is
  detected.
- Registry negative tests proving an inventory entry without all four executed
  conversion categories fails.

## Definition of done

The atomic hub/storage/v1-removal cutover compiles without a transitional
v1 <-> v3 converter; the real conversion endpoint losslessly handles v2/v3
objects; version-specific admissions remain correct; `[v1]` is the only
ConversionReview protocol entry; and the OpenAPI guard prevents silent future
differences. Run all focused tests and mandatory Make gates; record exact
results.

## Outcomes

### Execution

- Assignee: Codex
- Start branch/commit: RHOAIENG-94812-DSC-v3 at cf9ca9270
- End commit/PR: `feat(dsc): cut over hub conversion and retire v1` (this subtask's commit)
- Pre-existing changes preserved: None (clean at start; subsequent subtask work is shared)

### Implementation

- Decisions used/added: DEC-001 through DEC-008, DEC-014, DEC-015, DEC-019, DEC-021; no new decision.
- Converter and registration changes: v3 `Hub`, v2 explicit typed `ConvertTo`/`ConvertFrom` across metadata, all spec components, and all status components; `/convert` is registered by v2's controller-runtime webhook builder.
- V1 code and registration removed: `api/datasciencecluster/v1`, its admissions/converter/tests, `PROJECT`, main/test schemes, and compiled v1 consumers. Useful behavior was ported to v2/v3 tests.
- Compatibility annotation disposition: `conversion.opendatahub.io/aigateway-state` had no remaining consumer and was removed from `pkg/metadata/annotations`.
- GVK/admission changes: distinct v2/v3 GVK constants and independent v2/v3 defaulting/validation; the v2 deprecation marker is added here and verified in the generated CRD under 001-04.
- OpenAPI guard location: `pkg/dsc/compare/openapi_test.go` with immutable v2 digests, full normalized schema comparison, semantic-category negative tests, and `testdata/conversion-differences.json` executable four-category fixture registry (empty for identity schema).

### Verification

- Direct and round-trip tests: `go test ./api/datasciencecluster/v2 ./api/datasciencecluster/v3` passed; fully populated one-hop, both round trips, empty objects, and deep-copy isolation covered.
- `/convert` integration tests: `TestConversionReviewBothDirections` passed against the registered HTTPS endpoint for v2→v3 and v3→v2 with one and three objects; `TestV2ClientAgainstV3Storage` passed with the envtest CRD configured to call that endpoint.
- Admission/GVK tests: v2 and v3 Gomega defaulting/validation suites pass; focused v2 envtest create/admission passes.
- OpenAPI guard tests: `go test ./pkg/dsc/compare` passed for ODH/RHOAI equality, semantic mutation detection, and missing executable conversion categories.
- Generation/fmt/lint/unit/build: generation, both platform manifest variants, format, lint (0 issues), `make unit-test` (81 Ginkgo suites plus other module suites), and build pass after restoring the pinned chart fixture.
- `git diff --check`: passed.

### Deviations and follow-up

- Initial registry difference set is intentionally empty under DEC-002; future semantic differences must add fixtures and executed forward/backward/round-trip cases.
