---
id: DSC-V3-006
jira: RHOAIENG-94812
title: Implement the accepted v3 API and transforming v2-v3 conversion
status: completed
start_date: null
end_date: 2026-09-23
assignee: null
created_date: 2026-09-18
last_updated: 2026-09-24
depends_on:
  - DSC-V3-001
  - DSC-V3-012
blocked_by: []
---

# DSC-V3-006: Implement the accepted v3 API and conversion contract

## Lifecycle and prerequisites

Read `../development.md`, `../decisions.md`, `../PLAN.md`, `../TASKS.md`, and
DSC-V3-005 and the completed DSC-V3-012 Outcomes. The implemented scope covers
the accepted Dashboard, AI Hub, Data, and MaaS contracts. The Training
Operator and Llama Stack Operator fields were carried over unchanged from v2
as an interim baseline under DEC-032. DEC-033 and DEC-034 now accept removing
those v3 spec/status entries and limiting the retained v2 spec fields to
retirement-only use. That outcome-based validation, schema, and conversion
work was not part of this completed task and requires a separate follow-up.
Data Registry runtime consumer projection remains deferred and does not block
this task.
Use the Modern Go guidance required by `development.md` before every Go edit.
Record new durable decisions before relying on them.

## Objective

Replace the initially identical v3 public API with the fully accepted DSC v3
shape and implement deterministic v2 <-> v3 conversion with the accepted
preservation and reserved-metadata lifecycle, plus admissions, generated
schemas, and conversion coverage. Component module projections are owned by
DSC-V3-007 through DSC-V3-009. KServe/MaaS is already owned by DSC-V3-012 and
is integration input, not work to repeat here.

## Indicative starting files and shared-type audit

- Implement v3-only public types under `api/datasciencecluster/v3` and extend
  the v2 spoke conversion and its tests under `api/datasciencecluster/v2`.
- Update versioned admissions under `internal/webhook/datasciencecluster/v2`
  and the v3 package, conversion envtests through
  `internal/webhook/envtestutil/envtestutil.go`, and the OpenAPI inventory/guard
  established under `pkg/dsc/compare` by DSC-V3-001.
- Audit—but do not mutate merely to obtain v3 behavior—the shared definitions
  in `api/components/v1alpha1/dashboard_types.go`, `modelregistry_types.go`,
  `modelregistry_types.odh.go`, `modelregistry_types.rhoai.go`,
  `feastoperator_types.go`, `aigateway_types.go`,
  `modelsasservice_types.go`, `kserve_types.go`, `trainer_types.go`,
  `trainingoperator_types.go`, `llamastackoperator_types.go`, and
  `ogx_types.go`. If an accepted change truly applies to v2 or an internal
  module CR too, record that scope explicitly in `../decisions.md` first.
- Generated touchpoints include both platform samples, DSC CRD conversion
  patches and CSV bases named in 001-04, plus
  `api/datasciencecluster/v2/init_resource_annotation_test.go` and
  `cmd/manifest-tools/pkg/config/init_resource_sync_test.go` when the default
  init resource changes.
- Final conversion E2E is owned by DSC-V3-011. Record the necessary scenarios
  and likely starting points (`tests/e2e/helper_test.go`,
  `test_context_test.go`, `controller_test.go`, `components_test.go`, and
  `v2tov3upgrade_test.go`) without editing them in this task solely for E2E.

## Required implementation

### Version-owned public types

- Implement the accepted Dashboard, AI Hub, and Data/Feature Store/Data
  Registry changes in `api/datasciencecluster/v3`. Training Operator and
  Llama Stack fields were retained as the initial interim v2-compatible
  baseline under DEC-032. Their removal is now accepted under DEC-033/DEC-034
  but is outside this completed task; implement it only through the separate
  follow-up contract covering v2 retirement validation and conversion.
- Preserve DEC-022 forward precedence and DEC-024/Task 012's KServe/MaaS
  schema, literal-marker normalization, and admission retirement behavior;
  change them only through a recorded follow-up decision. Reuse existing
  `componentApi` AI Gateway/canonical value types without canonical pointers
  or duplicated wrappers; retain unchanged OpenAPI and v2 CEL.
- Keep every unchanged v2 JSON name and semantic unchanged.
- Make each diverging nested public stanza v3-owned. Do not mutate shared
  `api/components/v1alpha1` or internal module types to make both versions
  change together.
- Preserve DEC-011 internal resource names and exclude DCH under DEC-012.
- Keep v2 served/deprecated with version-specific defaulting and validation.

### Transforming conversion

- Extend the v2 spoke's explicit typed `ConvertTo` and `ConvertFrom` methods
  only for differences listed by DSC-V3-005.
- Copy unrelated metadata and every unchanged spec/status field exactly.
  Reserved metadata follows DEC-024. Do not use whole-object JSON serialization,
  `unsafe`, cluster calls, defaulting, or validation inside conversion.
  DEC-024 requires no JSON payload or snapshot.
- Implement populated, absent, empty, deprecated, and conflicting cases exactly
  as accepted. Empty management state remains a wire-level empty value while
  its effective runtime value is `Removed`, except DEC-024's explicit legacy
  empty -> `Removed` reverse result.
- Preserve every v3-only value through a v2 read-modify-write using the accepted
  explicit field or annotation lifecycle. Remove stale compatibility data at
  the accepted point.
- Preserve Task 012's marker reset solely from current v2 C/L, creating it only
  for C=Removed/L=Managed, native v3
  CREATE stripping, and UPDATE `OldObject` authority. Unrelated edits preserve
  the marker; changes to canonical or either parent management state delete it
  permanently even after a revert. Reject unknown marker values. Reverse keeps
  C/A migrated and returns legacy Managed iff marked, else Removed including
  original empty; original C/A/absence are not restored. Do not add compatibility
  for an installed intermediate-v3 release; no such release is supported.

### Admissions and generated artifacts

- Update v3 defaults, validation, warnings, immutability, and update rules for
  the accepted fields; retain compatible v2 admission behavior.
- Update ODH and RHOAI CRDs, conversion patches, samples, API docs, deepcopy
  code, bundles/CSVs, and schema inputs affected by public API changes.
- Maintain v3 as sole storage, v2 as served/deprecated, v1 absent, `/convert`
  registered through v2, and ConversionReview protocol exactly `[v1]`.
- Keep the independent normalized v2 OpenAPI baseline immutable for v3-only
  changes. Update it only when an accepted decision explicitly changes the v2
  wire contract; list those v2 paths separately and add focused v2
  admission/conversion tests. Update the explicit difference inventory without
  allowing v3 generation to overwrite the v2 expectation.

## Tests

For every inventory entry add, in the same change:

- one stable `case_id` in the shared machine-readable registry, with exact
  v2/v3 paths and changed OpenAPI semantics;
- direct v2 -> v3 and v3 -> v2 cases;
- v2 -> v3 -> v2 and v3 -> v2 -> v3 cases;
- populated, absent, empty, and conflict cases;
- status and condition conversion where applicable;
- a v2 read-modify-write preservation case for each v3-only value; and
- version-specific defaulting/validation/warning/update tests.

The shared test runner must record execution of forward, backward,
v2-round-trip, and v3-round-trip categories for every `case_id`; the guard must
fail if any link or executed category is missing.

Exercise real `/convert` ConversionReview requests with single and multiple
objects in both directions. The OpenAPI guard must detect path, type,
requiredness, nullability, default, enum, CEL, and list/map semantic changes and
fail for an unlisted difference.

### Final E2E handoff

- Record the scenarios Task 011 must implement for every new v3-only
  representation, including v2 read-modify-write preservation, transformed
  spec/status/conditions, absent/empty/conflicting values, and any accepted
  fidelity mechanism.
- Identify ODH/RHOAI build-specific cases and any fixture prerequisites.
- E2E implementation and execution are not completion gates for this task;
  complete unit and integration coverage is sufficient.

## Required gates and definition of done

Run focused API/webhook/envtest suites and:

```bash
make generate manifests api-docs
make fmt
make lint
make unit-test
make build
git diff --check
```

Complete for this scope when accepted public fields are generated, the accepted
differences have bidirectional direct/round-trip coverage, v2 clients preserve
the in-scope v3 data, ODH/RHOAI artifacts agree, and no component-handler
behavior was guessed. Data Registry runtime projection is an explicit
follow-up, not a completion gate. Task 014 verified the interim Training
Operator and Llama Stack baseline. Tasks 015 and 016 recorded final v3 removal
outcomes; their separate validation/conversion follow-up remains outside this
task.

## Outcomes

### Execution

- Assignee: Codex
- Start branch/commit: Existing implementation in the working branch
- End commit/PR: Existing implementation; no code changes in this status update
- Pre-existing changes preserved: Yes; unrelated working-tree edits untouched

### Decisions and implementation

- Decisions used: DEC-022, DEC-024, DEC-025, DEC-027, DEC-028, DEC-029,
  DEC-031
- Decisions added/superseded: DEC-031 initially recorded retention of the
  Training Operator and Llama Stack Operator stanzas; DEC-032 superseded that
  conclusion with interim-only carry-over. Tasks 015/016 later recorded final
  removal under DEC-033/DEC-034. Data Registry runtime projection remains
  deferred by user direction on 2026-09-23.
- Post-completion decision update (2026-09-24): Tasks 015/016 recorded removal
  under DEC-033/DEC-034. These decisions do not reopen the completed Task 006;
  retirement validation, v3 field removal, and conversion coverage require a
  separate follow-up implementation task.
- V3 type/schema changes: Accepted Dashboard, AI Hub, and Data public shapes
  are present; v2 compatibility remains explicit. The completed implementation
  left Training Operator and Llama Stack as interim v2-shaped fields under
  DEC-032. DEC-033/DEC-034 now accept their final v3 removal; a separate
  follow-up must implement v2 retirement validation and conversion.
- Conversion mappings implemented: Dashboard, Model Registry/AI Hub namespace and status, Feature Store/Data Registry, and accepted MaaS mappings are present in the v2 spoke.
- Compatibility fields/annotations: Explicit v2 Data Registry field and the DEC-024 MaaS marker; no new annotation for Data.
- OpenAPI inventory location: `../PLAN.md` conversion matrix and this task's component evidence.

### Verification

- Generation/manifests/API docs: Not rerun in this status update.
- Format/lint/unit/build: `make unit-test` did not pass as a whole: unrelated CoreWeave tests require missing `opt/charts` manifests. Initial sandbox execution also blocked envtest localhost binding; an elevated retry passed that point. Focused API tests passed with `env -u GOROOT GOCACHE=/tmp/opendatahub-dscv3-gocache make unit-test-operator TEST_SRC=./api/datasciencecluster/...`.
- Focused conversion tests: API DSC v2/v3 Ginkgo suites passed; complete repository unit target remains limited by missing CoreWeave chart fixtures.
- Admission/envtest: Not separately rerun.
- Real `/convert` tests: Not rerun in this status update.
- E2E scenarios and fixtures handed to Task 011:
- ODH/RHOAI artifact checks: Not rerun in this status update.
- `git diff --check`: Passed.

### Deviations and follow-up

- Deferred by current scope: Data Registry runtime consumer projection. Task
  014 verifies the interim Training Operator/Llama Stack API and conversion
  shape. DEC-033/DEC-034 now authorize their v3 removal and v2 retirement-only
  validation through separate follow-up implementation work.
