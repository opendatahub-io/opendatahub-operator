---
id: DSC-V3-020
jira: null
title: Repair v3 Dashboard E2E state updates
status: in_progress
start_date: 2026-09-25
end_date: null
assignee: Codex
created_date: 2026-09-25
last_updated: 2026-09-25
depends_on:
  - DSC-V3-017
blocked_by: []
---

# DSC-V3-020: repair v3 Dashboard E2E state updates

## Objective

Make existing v3 Dashboard, gateway-redirect, and upgrade E2E paths update
`spec.components.dashboard.standard.managementState`, rather than the v2-only
`dashboard.managementState` path.

## Work and acceptance

- Fix the narrow shared spec-update helper or its callers without changing
  Dashboard status/condition lookup.
- Retain the old path only in tests explicitly constructing a v2 DSC.
- Add a focused helper or schema-backed test that catches a regression.
- Preserve existing E2E selection, fixture ownership, and cleanup behavior.

## Verification

Run the focused test, E2E compilation, mandatory Make gates, and
`git diff --check`. Cluster E2E evidence requires an installed updated image.

## Outcomes

- Started on `RHOAIENG-94812-DSC-v3` at `024bbe758`; the user's existing
  `docs/api-overview.md` change is preserved. Task 018 work is also in
  progress in separate files and will not be included in this task's commit.
- Updated v3 Dashboard spec writes in the shared helper and direct Dashboard,
  gateway redirect, and upgrade E2E paths. Status and condition lookup still
  uses the established Dashboard names.
- Added a cluster-independent helper/schema test. The focused
  `TestDashboardV3ManagementStateSpecPath` run passed; `make fmt` passed and
  `git diff --check` passed. A targeted search found no remaining old
  Dashboard spec path in `tests/e2e`.
- Full gates and installed-image E2E are pending. This task remains
  `in_progress`; no cluster-qualification result is claimed.
