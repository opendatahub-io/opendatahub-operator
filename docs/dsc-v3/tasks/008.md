---
id: DSC-V3-008
jira: RHOAIENG-95344
title: Implement the AI Hub v3 module projection and status
status: completed
start_date: 2026-09-22
end_date: 2026-09-23
assignee: Codex
created_date: 2026-09-18
last_updated: 2026-09-23
depends_on:
  - DSC-V3-006
blocked_by: []
---

# DSC-V3-008: Implement the AI Hub projection

## Lifecycle and scope

Read `../development.md`, `../decisions.md`, `../PLAN.md`, `../TASKS.md`,
`004.md`, `005.md`, and the `006.md` Outcomes. Update metadata and Outcomes
before editing. Use the accepted public `aiHub` contract from DEC-025 and the
namespace rename in DEC-028, together with Modern Go guidance. Internal rename
work is explicitly excluded by DEC-011.

## Existing behavior to preserve

`internal/controller/modules/modelregistry/handler.go` manages internal GVK
`AIHub`, singleton `default-aihub`, and module/manifest name `modelregistry`.
It projects public v3 `applicationNamespace` to internal `instancesNamespace`,
falls back to the applications namespace when required, and mirrors readiness,
release, namespace, and legacy status. Build-specific v2 defaulting uses
`odh-model-registries` or `rhoai-model-registries`.

## Indicative starting files

- Runtime: `internal/controller/modules/modelregistry/handler.go` and
  `handler_test.go`; inspect `internal/controller/modules/kserve/handler.go`
  for the existing Model Registry/MaaS interaction without expanding scope.
- Build-specific public-contract audit inputs:
  `api/components/v1alpha1/modelregistry_types.go`,
  `modelregistry_types.odh.go`, and `modelregistry_types.rhoai.go`. Do not edit
  them for a v3-only rename or shape; Task 006 owns versioned API/conversion.
- Cluster coverage: `tests/e2e/modelregistry_test.go` and, where cross-module
  behavior is asserted, `tests/e2e/kserve_modelregistry_test.go`.

## Implementation

1. Read the accepted v3 AI Hub fields, including `applicationNamespace`, from
   the typed DSC v3 context and project
   them into the existing internal AIHub CR without renaming internal resources.
2. Apply only the accepted namespace default/fallback, immutability, update, and
   removal behavior; keep ODH and RHOAI variants distinct where specified.
3. Preserve module lifecycle, applications namespace, gateway domain,
   releases, related objects, and management-state annotations.
4. Map source readiness to the accepted public DSC v3 status and condition
   names. Retain legacy status only where DSC-V3-005 requires it.
5. If an implementation discovery requires a durable behavior not in the
   accepted matrix, stop that portion and add a proposed decision; do not infer.
6. The AI Hub public type and conversion changes are owned by this
   implementation because DEC-025/DEC-028 supply the contract. Do not change the
   independent v2 OpenAPI baseline; update generated v3 artifacts and the
   explicit conversion tests together with the handler.

## Tests

- Cover `Managed`, `Removed`, empty/absent state and empty, build-defaulted,
  applications-fallback, and custom namespaces for ODH and RHOAI.
- Assert internal CR name/GVK/module name remain unchanged and the projected
  `instancesNamespace`, lifecycle, status, conditions, releases, and related
  objects are correct.
- Cover namespace validation/immutability and v2 compatibility through direct
  `registriesNamespace` <-> `applicationNamespace` cases and both round-trip
  conversions.
- Add schema-compliance, envtest/status, idempotence, and removal cases. Record
  the ODH/RHOAI cluster scenarios and fixture prerequisites for Task 011, which
  owns E2E implementation and execution.

## Required gates and definition of done

Run focused Model Registry/AI Hub tests and all mandatory Make gates. Complete
when v2 Model Registry and v3 AI Hub inputs produce the accepted equivalent
internal behavior, public names do not leak into internal identity, ODH/RHOAI
behavior is proven, and all evidence is recorded.

## Outcomes

### Execution

- Assignee: Codex
- Start branch/commit: `RHOAIENG-94812-DSC-v3` at `e87811488`.
- End implementation commit: `ecc43e106` (`feat(dsc-v3): rename AI Hub registries namespace`).
- Pre-existing changes preserved: Yes; unrelated user edits in
  `config/manager/manager.yaml` remain untouched.

### Implementation

- Decisions used/added: DEC-011, DEC-025, DEC-028.
- Handler/module changes: The v3 typed handler consumes `components.aiHub`;
  the internal AI Hub GVK, `default-aihub` CR, `modelregistry` module/manifest
  name, and projected internal fields remain unchanged. Public
  `applicationNamespace` projects to internal `instancesNamespace`.
- Namespace/default/update behavior: V2 `registriesNamespace` maps to v3
  `applicationNamespace`; Managed empty namespace uses the existing build
  default; Removed/empty state does not default; custom namespace and
  immutability are preserved.
- Status/condition behavior: v3 writes `status.components.aiHub` and `AIHubReady`; v2 conversion restores `modelregistry` and `ModelRegistryReady`.
- Conversion/OpenAPI changes: V2 `components.modelregistry` maps explicitly
  to v3 `components.aiHub`; `registriesNamespace` maps to
  `applicationNamespace` in spec and status, and reverse conversion restores
  the v2 names. `ModelRegistryReady` maps to `AIHubReady` and back. Separate
  v2 Model Registry and v3 AI Hub types preserve each version's public shape;
  generated v3 schema/API documentation and samples use the renamed field.

### Verification

- Focused unit tests: AI Hub, Model Registry, Dashboard, KServe, API conversion,
  and v3 webhook packages passed; the focused RHOAI API/controller/conversion
  suite passed.
- Integration/envtest: Focused operator test runs passed for the affected API,
  handlers, and webhook conversion paths.
- Conversion/round-trip tests: Direct conversion and v2 -> v3 -> v2 and
  v3 -> v2 -> v3 round trips passed for namespace spec/status and condition
  mapping.
- ODH/RHOAI tests: ODH and RHOAI generated schemas/samples were updated;
  build-specific namespace defaults remain distinct and covered.
- Generation/fmt/lint: `make generate manifests api-docs`, `make fmt`, and
  `make lint` passed after the rename. Full `make unit-test` and `make build`
  were not run in this follow-up.
- Webhook E2E: `make e2e-test-webhooks` passed against the cluster, including
  AI Hub conversion scenarios.
- E2E scenarios and fixtures handed to Task 011: Existing Model Registry and KServe E2E paths updated to use v3 `aiHub` and `AIHubReady`.
- `git diff --check`: Passed.

### Deviations and follow-up

- DEC-028 changed the public v3 namespace field after the initial
  implementation; the rename, bidirectional conversion, generated artifacts,
  tests, and documentation are now complete. The broader DSC-V3-006 work
  remains separately tracked and blocked.
