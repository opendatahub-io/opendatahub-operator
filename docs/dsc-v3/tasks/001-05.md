---
id: DSC-V3-001-05
parent: DSC-V3-001
jira: RHOAIENG-94812
title: Integrate and verify the initial DSC v3 machinery milestone
status: completed
start_date: 2026-09-18T10:15:00Z
end_date: 2026-09-18T12:47:56Z
assignee: Codex
created_date: 2026-09-18
last_updated: 2026-09-18T14:35:24Z
depends_on:
  - DSC-V3-001-02
  - DSC-V3-001-03
  - DSC-V3-001-04
blocked_by: []
---

# DSC-V3-001-05: Integrate and verify the machinery milestone

## Lifecycle and purpose

Read the authoritative local documents, `001.md`, and all completed 001 subtask
Outcomes. Update metadata and Outcomes before integration. This is not a place
to postpone unit or conversion tests owned by earlier subtasks; it proves their
combined behavior and resolves integration-only defects.

## Indicative starting files

- Core E2E infrastructure: `tests/e2e/helper_test.go`,
  `test_context_test.go`, and `controller_test.go`.
- Version/GVK and component-count assumptions:
  `tests/e2e/components_test.go` and `resilience_test.go`.
- Fresh installation and legacy upgrade coverage: `tests/e2e/creation_test.go`
  and `v2tov3upgrade_test.go`. The latter must be rewritten or supplemented to
  exercise DSC API storage conversion; its name is not sufficient evidence.
- Recheck both platform samples, CRD patches, and CSV bases named in 001-04,
  plus the init-resource tests, after all generated changes compose.

## Integration checks

1. Review subtask diffs for conflicting scheme, GVK, webhook, generated patch,
   sample, fixture, and test changes.
2. Confirm v2 and v3 generated OpenAPI remain identical and the independent
   baseline/guard fail on an unlisted semantic difference.
3. Confirm production reconciliation uses typed v3 exclusively and the exact
   v2 compatibility allowlist is documented.
4. Confirm v1 has no production code or generated serving, v2 is
   served/deprecated, and v3 is sole storage.
5. Exercise the real `/convert` endpoint in both directions with single and
   multiple fully populated objects, plus a v2 read-update against v3 storage.
6. Verify ODH and RHOAI fresh installs, version-specific admissions, primary v3
   samples, status-subresource conversion, and metadata preservation.
7. Ensure unresolved component shapes are absent. Accepted G5 proceeds in
   independent DSC-V3-012 under DEC-022/DEC-024 after this milestone; remaining
   data-model changes wait for DSC-V3-006 and their accepted contracts.
8. Record the v1-free artifact handoff to future DSC-V3-010. Do not implement
   or run odh-cli qualification in this subtask.

## Required verification

Run focused integration/envtest suites and:

```bash
make generate manifests api-docs
make fmt
make lint
make unit-test
make build
git diff --check
```

Record the ODH/RHOAI fresh-install and v2-client scenarios for consolidated
E2E qualification in Task 011.

## Definition of done

All 001 subtasks compose without regression; generated output is reproducible;
identity conversion, admissions, storage, deprecation, v1 removal, and typed-v3
runtime are proven; no proposed stanza change is encoded; and the parent
`001.md` Outcomes can be completed from recorded evidence.

## Outcomes

### Execution

- Assignee: Codex
- Start branch/commit: RHOAIENG-94812-DSC-v3 at cf9ca9270
- End commit/PR: `75c2018d4` (`test(dsc): record v3 integration milestone`)
- Pre-existing changes preserved: None (clean at start; subsequent subtask work is shared)

### Integration result

- Decisions used/added: DEC-001 through DEC-010, DEC-013 through DEC-017, DEC-019, DEC-021, and DEC-023.
- Subtask commits: 001-01 `787298bbb`, 001-02 `64a3292fa`, 001-03 `f6c8827ec`, and 001-04 `dd14e8b35`; this record is committed separately as 001-05.
- Final production v2 allowlist: v2 API/converter, v2 admission and `/convert` registration, explicit GVK/scheme support, v2 envtest factory, and compatibility/upgrade tests only; no typed-v2 reconciler/handler.
- Final v1 search: no v1 DSC code, scheme, CRD serving, sample, or webhook; only negative assertion text in the manifest test.
- Generated artifact result: ODH and RHOAI CRD/webhook output checked; tracked API docs, samples, CSV annotation, and conversion patches updated.
- DSC-V3-010 handoff inputs: v1-free CRD, v2 compatibility endpoint, baseline/guard, E2E assertion of v1 absent from `storedVersions`; promotion still blocked on gate/CI evidence.

### Verification

- OpenAPI/conversion guard: `go test ./pkg/dsc/compare` passed for both generated platform schemas and missing semantic/executable-case negatives.
- `/convert` and v2-client integration: HTTPS endpoint tests passed in both directions with one/three objects; v2-client envtest passed with the CRD configured to use the webhook and v3 storage.
- Admissions/status tests: independent v2/v3 defaulting/validation unit suites passed; v2-client test covered metadata and status-subresource preservation.
- ODH/RHOAI fresh-install E2E: Tier3 case extended with v2 create/read/update and CRD version/storage checks; `go test -c -o /private/tmp/odh-dsc-e2e.test ./tests/e2e` passed. No cluster is configured locally, so neither platform job has executed.
- Generation/fmt/lint/unit/build: generation, ODH/RHOAI manifests, format, lint (0 issues), full unit (81 Ginkgo suites plus module suites), and build passed after fixture restoration.
- `git diff --check`: passed.

### Deviations and follow-up

- This integration milestone is complete. ODH/RHOAI cluster E2E evidence is deferred to Task 011 under the integration-first staging rule. `make fmt` also normalized four previously unformatted `cmd/test-retry` files; these formatting-only diffs are included in this integration commit.
- Follow-up test-selection fix (2026-09-18): added
  `./api/datasciencecluster/...` to both Makefile `TEST_SRC` and the unit CI
  workflow's override. This includes the v2 conversion and v3 registration/
  init-resource suites in normal validation. Verified with
  `GOCACHE=/private/tmp/odh-go-cache make unit-test-operator TEST_SRC='./api/datasciencecluster/...'`:
  both API suites passed. The selection fix is committed separately as
  `8fb777a0a` (`test(dsc): include versioned API suites in CI`). Task 012 moved
  the 81-case MaaS matrix to the webhook package, which is already in normal
  `TEST_SRC`, and records the subsequent full repository gate results.
