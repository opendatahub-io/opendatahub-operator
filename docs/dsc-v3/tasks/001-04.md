---
id: DSC-V3-001-04
parent: DSC-V3-001
jira: RHOAIENG-94812
title: Finalize the v2-deprecated v3-storage delivery artifacts
status: completed
start_date: 2026-09-18T10:13:00Z
end_date: 2026-09-18T12:47:56Z
assignee: Codex
created_date: 2026-09-18
last_updated: 2026-09-18T12:47:56Z
depends_on:
  - DSC-V3-001-02
  - DSC-V3-001-03
blocked_by: []
---

# DSC-V3-001-04: Finalize generated delivery artifacts

## Lifecycle and precondition

Read the authoritative local documents, `001.md`, and Outcomes from
`001-02.md` and `001-03.md`. Update metadata and Outcomes before work. This
subtask finalizes the already v1-free cutover from DSC-V3-001-02 but does not
implement or qualify odh-cli. DSC-V3-010 later proves the external gate rewrites
v1 storage through v2 and removes v1 from `status.storedVersions` before OLM
applies the finalized CRD.

## Indicative starting files

- Primary samples:
  `config/samples/datasciencecluster_v3_datasciencecluster.yaml` and
  `config/rhoai/samples/datasciencecluster_v3_datasciencecluster.yaml`.
- Platform CSV bases:
  `config/manifests/bases/opendatahub-operator.clusterserviceversion.yaml` and
  `config/rhoai/manifests/bases/rhods-operator.clusterserviceversion.yaml`.
- Conversion patches: the webhook and CA-injection DSC patches under both
  `config/crd/patches` and `config/rhoai/crd/patches`.
- Init-resource contract tests:
  `api/datasciencecluster/v2/init_resource_annotation_test.go` and
  `cmd/manifest-tools/pkg/config/init_resource_sync_test.go`; migrate their
  asserted API version and source-of-truth relationship rather than deleting
  the protection.
- Include all output changed by `make generate manifests api-docs`; there is no
  checked-in top-level `bundle/` directory in the current tree, so resolve
  generated bundle/CSV output from the Make targets rather than assuming that
  path.

## Implementation

1. Confirm `/convert` is registered through the v2 spoke, production code is
   on typed v3, and DSC-V3-001-02 left no compiled v1 consumer.
2. Remove residual generated v1 references from fixtures, samples, CRDs,
   bundles/CSVs, webhook content, and API docs. Preserve a historical
   unstructured fixture only for a named non-odh-cli repository test.
3. Mark v2 `served: true`, `storage: false`, `deprecated: true`, with DEC-019's
   exact deprecation warning. Keep v2 admissions and conversion supported.
4. Regenerate ODH/RHOAI CRDs, patches, webhook manifests, bundles/CSVs, API
   docs, deepcopy/object code, samples, and RBAC if affected.
5. Require generated CRDs to serve only v2/v3, store only v3, omit v1, expose
   `/convert` through v2, and use ConversionReview protocol `[v1]`.

## Tests

- Generated-manifest assertions for versions, storage, deprecation warning,
  conversion path, and protocol version for ODH and RHOAI.
- Repository-wide v1 search with an empty production result. Historical
  unstructured gate fixtures may remain only with an explicit justification.
- V2 client create/read/update against v3 storage and v2/v3 admission checks.
- Fresh-install assertions that v1 is not discoverable.
- Do not add odh-cli-specific fixtures or gate tests. DSC-V3-010 owns that
  future work and consumes the completed v1-free artifacts from this subtask.

## Definition of done

The code and generated delivery surface are v1-free, v2 is served/deprecated,
v3 is sole storage/runtime, behavioral coverage is preserved, and all focused
and mandatory gates pass. Task completion is independent of DSC-V3-010; release
promotion remains explicitly blocked on its DEC-009 evidence.

## Outcomes

### Execution

- Assignee: Codex
- Start branch/commit: RHOAIENG-94812-DSC-v3 at cf9ca9270
- End commit/PR: `dd14e8b35` (`chore(dsc): publish v3 storage and deprecated v2 artifacts`)
- Pre-existing changes preserved: None (clean at start; subsequent subtask work is shared)

### Removal and generation

- Decisions used/added: DEC-001, DEC-005, DEC-006, DEC-008, DEC-009, DEC-013, DEC-015, DEC-016, DEC-019, DEC-021, and DEC-023.
- Residual generated v1 references removed: ODH/RHOAI DSC CRD versions and webhook manifests generated with v2/v3 only; primary samples and RHOAI CSV initialization-resource moved to v3. The v1-free deliverable remains ineligible for upgrade promotion until Task 010.
- Retained historical fixtures and justification: none in compiled code or generated serving; Task 010 owns any future gate fixture.
- Generated artifacts: ODH/RHOAI CRD and webhook output via `make manifests` for both platform types (ignored build output), v3 deepcopy, tracked `docs/api-overview.md`, v3 primary samples/kustomizations, RHOAI CSV annotation, and DSC conversion patches with protocol `[v1]`.

### Verification

- V1 search: `rg` over `api cmd internal pkg tests config` found no v1 DSC production or generated serving reference; only a negative assertion string in the new manifest test remains.
- Manifest/deprecation assertions: `TestGeneratedDSCServingAndConversion` passed for both ODH/RHOAI: v2 served/deprecated with DEC-019's exact warning, v3 served/sole storage, v1 absent, `/convert`, protocol `[v1]`, and v2/v3 admissions.
- V2 compatibility tests: real HTTPS `/convert` integration and v2-client create/read/update/status envtest passed.
- Fresh-install tests: generated CRD assertions passed; Tier3 cluster E2E case implemented but not run locally.
- Generation/fmt/lint/unit/build: generation, both platform manifest variants, format, lint (0 issues), full unit (81 Ginkgo suites plus module suites), and build passed.
- `git diff --check`: passed.

### Release handoff and follow-up

- Inputs handed to DSC-V3-010: v1-free v2-served/v3-storage CRD, v2 compatibility endpoint and tests, immutable OpenAPI baseline, and explicit requirement to migrate/verify `status.storedVersions` before OLM applies the CRD.
- Upgrade release remains blocked: yes
- No odh-cli gate implementation or qualification was attempted here.
