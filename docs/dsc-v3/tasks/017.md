---
id: DSC-V3-017
jira: null
title: Reconcile retired-operator decision and task records
status: completed
start_date: 2026-09-25
end_date: 2026-09-25
assignee: Codex
created_date: 2026-09-25
last_updated: 2026-09-25
depends_on: []
blocked_by: []
---

# DSC-V3-017: reconcile retired-operator records

## Objective

Make the current local records accurately describe the accepted final API:
`trainingoperator` and `llamastackoperator` exist only in v2; v3 has neither
spec nor status field. Preserve DEC-032 and Task 014 as interim history, while
DEC-033/DEC-034 and the completed implementation describe the final state.

## Work

- Correct Tasks 015/016 without closing their separate Jira-follow-up work.
- Align stale current-state prose in PLAN, TASKS, Task 005/006/011, and the
  development guide. State that v2-to-v3 drops both retired fields and
  v3-to-v2 reports `Removed` for both spec and status.
- Keep the v2 `optionalOldSelf` CEL retirement guard distinct from the
  conversion contract: an existing `Managed` value may stay unchanged or move
  to `Removed`/empty, but cannot be re-enabled after removal. Empty is
  effectively `Removed`.
- Do not change the API, conversion code, generated artifacts, or Jira.

## Verification

Search the current-state docs for stale claims, review the diff, and run
`git diff --check`. Record any intentionally retained historical references.

## Outcomes

- Started on `RHOAIENG-94812-DSC-v3` at `3d654b931`; the only pre-existing
  worktree change was the user's `docs/api-overview.md`, which was preserved.
- Corrected Tasks 015/016, the current-state matrix and tracker, the accepted
  decision implementation notes, Task 011 ownership/dependencies, and the
  development guide. Task 014/DEC-032 and Task 006 Outcomes remain historical.
- No API, generated artifact, or Jira was changed. Tasks 015/016 remain open
  for their separate Jira follow-up. No new durable behavior decision was made.
- Verification: targeted stale-current-state search found no remaining claim
  that the current v3 fields exist or that conversion mapping is undefined;
  `git diff --check -- docs/dsc-v3` passed. No code tests were run for this
  documentation-only task.
