apiVersion: v1
kind: Service
metadata:
  name: metrics-service
  namespace: system
  labels:
    app.kubernetes.io/name: opendatahub-ogx-operator
spec:
  selector:
    app.kubernetes.io/name: opendatahub-ogx-operator
  ports:
    - name: https
      port: 8443
      protocol: TCP
      targetPort: 8443
