---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: ogxservers.ogx.io
spec:
  group: ogx.io
  names:
    kind: OGXServer
    listKind: OGXServerList
    plural: ogxservers
    singular: ogxserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.resolvedDistribution.image
      name: Distribution
      priority: 1
      type: string
    - jsonPath: .status.configGeneration.configMapName
      name: Config
      priority: 1
      type: string
    - jsonPath: .status.configGeneration.providerCount
      name: Providers
      type: integer
    - jsonPath: .status.version.operatorVersion
      name: Operator Version
      priority: 1
      type: string
    - jsonPath: .status.version.serverVersion
      name: Server Version
      priority: 1
      type: string
    - jsonPath: .status.availableReplicas
      name: Available
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: OGXServer is the Schema for the ogxservers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OGXServerSpec defines the desired state of OGXServer.
            properties:
              baseConfig:
                description: |-
                  BaseConfig references a ConfigMap key containing the base config.yaml used
                  as the starting point for declarative config generation.
                  When set, this takes precedence over OCI label resolution.
                  Mutually exclusive with overrideConfig.
                  The ConfigMap must be in the same namespace as the OGXServer
                  and must have the label ogx.io/watch: "true".
                properties:
                  key:
                    description: Key is the key within the ConfigMap.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                    type: string
                  name:
                    description: Name is the name of the ConfigMap.
                    minLength: 1
                    type: string
                required:
                - key
                - name
                type: object
              disabledAPIs:
                description: |-
                  DisabledAPIs lists API names to remove from the generated config.
                  Mutually exclusive with overrideConfig.
                items:
                  enum:
                  - batches
                  - file_processors
                  - inference
                  - responses
                  - tool_runtime
                  - vector_io
                  - files
                  type: string
                maxItems: 7
                minItems: 1
                type: array
              distribution:
                description: Distribution identifies the OGX distribution to deploy.
                properties:
                  image:
                    description: Image is a direct container image reference to use.
                    type: string
                  name:
                    description: |-
                      Name is the distribution name that maps to a supported distribution (e.g., "starter", "remote-vllm").
                      Resolved to a container image via distributions.json and image-overrides.
                    type: string
                type: object
                x-kubernetes-validations:
                - message: only one of name or image can be specified
                  rule: '!(has(self.name) && has(self.image))'
                - message: one of name or image must be specified
                  rule: has(self.name) || has(self.image)
              monitoring:
                description: Monitoring configures Prometheus monitoring and observability.
                properties:
                  enabled:
                    default: true
                    description: |-
                      Enabled controls whether the operator creates monitoring resources
                      (ServiceMonitor, PrometheusRule) for this server.
                      Defaults to true. Set to false to disable monitoring without removing the config.
                    type: boolean
                  metricsPort:
                    description: |-
                      MetricsPort is the port serving the /metrics endpoint.
                      When omitted, metrics are served on the main API port.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                type: object
              network:
                description: Network defines network access controls.
                properties:
                  externalAccess:
                    description: ExternalAccess controls external service exposure.
                    properties:
                      enabled:
                        default: false
                        description: Enabled controls whether external access is created.
                        type: boolean
                      hostname:
                        description: |-
                          Hostname sets a custom hostname for the external endpoint.
                          When omitted, an auto-generated hostname is used.
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: hostname must not be empty if specified
                      rule: '!has(self.hostname) || self.hostname.size() > 0'
                  policy:
                    description: |-
                      Policy configures the operator-managed NetworkPolicy.
                      When nil, the operator creates a default NetworkPolicy with safe ingress rules.
                    properties:
                      egress:
                        description: |-
                          Egress rules. When non-empty, a kube-dns egress rule is auto-injected
                          to prevent DNS breakage.
                        items:
                          description: |-
                            NetworkPolicyEgressRule describes a particular set of traffic that is allowed out of pods
                            matched by a NetworkPolicySpec's podSelector. The traffic must match both ports and to.
                            This type is beta-level in 1.8
                          properties:
                            ports:
                              description: |-
                                ports is a list of destination ports for outgoing traffic.
                                Each item in this list is combined using a logical OR. If this field is
                                empty or missing, this rule matches all ports (traffic not restricted by port).
                                If this field is present and contains at least one item, then this rule allows
                                traffic only if the traffic matches at least one port in the list.
                              items:
                                description: NetworkPolicyPort describes a port to
                                  allow traffic on
                                properties:
                                  endPort:
                                    description: |-
                                      endPort indicates that the range of ports from port to endPort if set, inclusive,
                                      should be allowed by the policy. This field cannot be defined if the port field
                                      is not defined or if the port field is defined as a named (string) port.
                                      The endPort must be equal or greater than port.
                                    format: int32
                                    type: integer
                                  port:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: |-
                                      port represents the port on the given protocol. This can either be a numerical or named
                                      port on a pod. If this field is not provided, this matches all port names and
                                      numbers.
                                      If present, only traffic on the specified protocol AND port will be matched.
                                    x-kubernetes-int-or-string: true
                                  protocol:
                                    description: |-
                                      protocol represents the protocol (TCP, UDP, or SCTP) which traffic must match.
                                      If not specified, this field defaults to TCP.
                                    type: string
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            to:
                              description: |-
                                to is a list of destinations for outgoing traffic of pods selected for this rule.
                                Items in this list are combined using a logical OR operation. If this field is
                                empty or missing, this rule matches all destinations (traffic not restricted by
                                destination). If this field is present and contains at least one item, this rule
                                allows traffic only if the traffic matches at least one item in the to list.
                              items:
                                description: |-
                                  NetworkPolicyPeer describes a peer to allow traffic to/from. Only certain combinations of
                                  fields are allowed
                                properties:
                                  ipBlock:
                                    description: |-
                                      ipBlock defines policy on a particular IPBlock. If this field is set then
                                      neither of the other fields can be.
                                    properties:
                                      cidr:
                                        description: |-
                                          cidr is a string representing the IPBlock
                                          Valid examples are "192.168.1.0/24" or "2001:db8::/64"
                                        type: string
                                      except:
                                        description: |-
                                          except is a slice of CIDRs that should not be included within an IPBlock
                                          Valid examples are "192.168.1.0/24" or "2001:db8::/64"
                                          Except values will be rejected if they are outside the cidr range
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - cidr
                                    type: object
                                  namespaceSelector:
                                    description: |-
                                      namespaceSelector selects namespaces using cluster-scoped labels. This field follows
                                      standard label selector semantics; if present but empty, it selects all namespaces.

                                      If podSelector is also set, then the NetworkPolicyPeer as a whole selects
                                      the pods matching podSelector in the namespaces selected by namespaceSelector.
                                      Otherwise it selects all pods in the namespaces selected by namespaceSelector.
                                    properties:
                                      matchExpressions:
                                        description: matchExpressions is a list of
                                          label selector requirements. The requirements
                                          are ANDed.
                                        items:
                                          description: |-
                                            A label selector requirement is a selector that contains values, a key, and an operator that
                                            relates the key and values.
                                          properties:
                                            key:
                                              description: key is the label key that
                                                the selector applies to.
                                              type: string
                                            operator:
                                              description: |-
                                                operator represents a key's relationship to a set of values.
                                                Valid operators are In, NotIn, Exists and DoesNotExist.
                                              type: string
                                            values:
                                              description: |-
                                                values is an array of string values. If the operator is In or NotIn,
                                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                the values array must be empty. This array is replaced during a strategic
                                                merge patch.
                                              items:
                                                type: string
                                              type: array
                                              x-kubernetes-list-type: atomic
                                          required:
                                          - key
                                          - operator
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      matchLabels:
                                        additionalProperties:
                                          type: string
                                        description: |-
                                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                                        type: object
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  podSelector:
                                    description: |-
                                      podSelector is a label selector which selects pods. This field follows standard label
                                      selector semantics; if present but empty, it selects all pods.

                                      If namespaceSelector is also set, then the NetworkPolicyPeer as a whole selects
                                      the pods matching podSelector in the Namespaces selected by NamespaceSelector.
                                      Otherwise it selects the pods matching podSelector in the policy's own namespace.
                                    properties:
                                      matchExpressions:
                                        description: matchExpressions is a list of
                                          label selector requirements. The requirements
                                          are ANDed.
                                        items:
                                          description: |-
                                            A label selector requirement is a selector that contains values, a key, and an operator that
                                            relates the key and values.
                                          properties:
                                            key:
                                              description: key is the label key that
                                                the selector applies to.
                                              type: string
                                            operator:
                                              description: |-
                                                operator represents a key's relationship to a set of values.
                                                Valid operators are In, NotIn, Exists and DoesNotExist.
                                              type: string
                                            values:
                                              description: |-
                                                values is an array of string values. If the operator is In or NotIn,
                                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                the values array must be empty. This array is replaced during a strategic
                                                merge patch.
                                              items:
                                                type: string
                                              type: array
                                              x-kubernetes-list-type: atomic
                                          required:
                                          - key
                                          - operator
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      matchLabels:
                                        additionalProperties:
                                          type: string
                                        description: |-
                                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                                        type: object
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                          type: object
                        type: array
                      enabled:
                        default: true
                        description: |-
                          Enabled controls whether the operator manages a NetworkPolicy for this server.
                          Defaults to true. Set to false to disable NetworkPolicy creation entirely.
                        type: boolean
                      ingress:
                        description: |-
                          Ingress defines additional ingress rules, merged with operator defaults
                          (allow from same-namespace and operator-namespace on the service port).
                        items:
                          description: |-
                            NetworkPolicyIngressRule describes a particular set of traffic that is allowed to the pods
                            matched by a NetworkPolicySpec's podSelector. The traffic must match both ports and from.
                          properties:
                            from:
                              description: |-
                                from is a list of sources which should be able to access the pods selected for this rule.
                                Items in this list are combined using a logical OR operation. If this field is
                                empty or missing, this rule matches all sources (traffic not restricted by
                                source). If this field is present and contains at least one item, this rule
                                allows traffic only if the traffic matches at least one item in the from list.
                              items:
                                description: |-
                                  NetworkPolicyPeer describes a peer to allow traffic to/from. Only certain combinations of
                                  fields are allowed
                                properties:
                                  ipBlock:
                                    description: |-
                                      ipBlock defines policy on a particular IPBlock. If this field is set then
                                      neither of the other fields can be.
                                    properties:
                                      cidr:
                                        description: |-
                                          cidr is a string representing the IPBlock
                                          Valid examples are "192.168.1.0/24" or "2001:db8::/64"
                                        type: string
                                      except:
                                        description: |-
                                          except is a slice of CIDRs that should not be included within an IPBlock
                                          Valid examples are "192.168.1.0/24" or "2001:db8::/64"
                                          Except values will be rejected if they are outside the cidr range
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - cidr
                                    type: object
                                  namespaceSelector:
                                    description: |-
                                      namespaceSelector selects namespaces using cluster-scoped labels. This field follows
                                      standard label selector semantics; if present but empty, it selects all namespaces.

                                      If podSelector is also set, then the NetworkPolicyPeer as a whole selects
                                      the pods matching podSelector in the namespaces selected by namespaceSelector.
                                      Otherwise it selects all pods in the namespaces selected by namespaceSelector.
                                    properties:
                                      matchExpressions:
                                        description: matchExpressions is a list of
                                          label selector requirements. The requirements
                                          are ANDed.
                                        items:
                                          description: |-
                                            A label selector requirement is a selector that contains values, a key, and an operator that
                                            relates the key and values.
                                          properties:
                                            key:
                                              description: key is the label key that
                                                the selector applies to.
                                              type: string
                                            operator:
                                              description: |-
                                                operator represents a key's relationship to a set of values.
                                                Valid operators are In, NotIn, Exists and DoesNotExist.
                                              type: string
                                            values:
                                              description: |-
                                                values is an array of string values. If the operator is In or NotIn,
                                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                the values array must be empty. This array is replaced during a strategic
                                                merge patch.
                                              items:
                                                type: string
                                              type: array
                                              x-kubernetes-list-type: atomic
                                          required:
                                          - key
                                          - operator
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      matchLabels:
                                        additionalProperties:
                                          type: string
                                        description: |-
                                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                                        type: object
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  podSelector:
                                    description: |-
                                      podSelector is a label selector which selects pods. This field follows standard label
                                      selector semantics; if present but empty, it selects all pods.

                                      If namespaceSelector is also set, then the NetworkPolicyPeer as a whole selects
                                      the pods matching podSelector in the Namespaces selected by NamespaceSelector.
                                      Otherwise it selects the pods matching podSelector in the policy's own namespace.
                                    properties:
                                      matchExpressions:
                                        description: matchExpressions is a list of
                                          label selector requirements. The requirements
                                          are ANDed.
                                        items:
                                          description: |-
                                            A label selector requirement is a selector that contains values, a key, and an operator that
                                            relates the key and values.
                                          properties:
                                            key:
                                              description: key is the label key that
                                                the selector applies to.
                                              type: string
                                            operator:
                                              description: |-
                                                operator represents a key's relationship to a set of values.
                                                Valid operators are In, NotIn, Exists and DoesNotExist.
                                              type: string
                                            values:
                                              description: |-
                                                values is an array of string values. If the operator is In or NotIn,
                                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                the values array must be empty. This array is replaced during a strategic
                                                merge patch.
                                              items:
                                                type: string
                                              type: array
                                              x-kubernetes-list-type: atomic
                                          required:
                                          - key
                                          - operator
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      matchLabels:
                                        additionalProperties:
                                          type: string
                                        description: |-
                                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                                        type: object
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            ports:
                              description: |-
                                ports is a list of ports which should be made accessible on the pods selected for
                                this rule. Each item in this list is combined using a logical OR. If this field is
                                empty or missing, this rule matches all ports (traffic not restricted by port).
                                If this field is present and contains at least one item, then this rule allows
                                traffic only if the traffic matches at least one port in the list.
                              items:
                                description: NetworkPolicyPort describes a port to
                                  allow traffic on
                                properties:
                                  endPort:
                                    description: |-
                                      endPort indicates that the range of ports from port to endPort if set, inclusive,
                                      should be allowed by the policy. This field cannot be defined if the port field
                                      is not defined or if the port field is defined as a named (string) port.
                                      The endPort must be equal or greater than port.
                                    format: int32
                                    type: integer
                                  port:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: |-
                                      port represents the port on the given protocol. This can either be a numerical or named
                                      port on a pod. If this field is not provided, this matches all port names and
                                      numbers.
                                      If present, only traffic on the specified protocol AND port will be matched.
                                    x-kubernetes-int-or-string: true
                                  protocol:
                                    description: |-
                                      protocol represents the protocol (TCP, UDP, or SCTP) which traffic must match.
                                      If not specified, this field defaults to TCP.
                                    type: string
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                          type: object
                        type: array
                      policyTypes:
                        description: |-
                          PolicyTypes specifies which policy directions are enforced.
                          Follows Kubernetes NetworkPolicy semantics: when omitted or empty,
                          Ingress is always included and Egress is included only if egress
                          rules are provided.
                        items:
                          description: |-
                            PolicyType string describes the NetworkPolicy type
                            This type is beta-level in 1.8
                          enum:
                          - Ingress
                          - Egress
                          type: string
                        type: array
                    type: object
                  port:
                    default: 8321
                    description: Port is the server listen port.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  tls:
                    description: |-
                      TLS configures optional TLS termination for the server.
                      When omitted, the server listens over plain HTTP.
                    properties:
                      secretName:
                        description: |-
                          SecretName references a Kubernetes TLS Secret containing a valid TLS certificate
                          for server TLS termination. The Secret must be in the same namespace as the
                          OGXServer and must have the label ogx.io/watch: "true" to be detected by the
                          operator's cache.
                        minLength: 1
                        type: string
                    required:
                    - secretName
                    type: object
                type: object
              overrideConfig:
                description: |-
                  OverrideConfig references a ConfigMap key containing a full config.yaml override.
                  Mutually exclusive with providers, resources, storage, disabledAPIs, and baseConfig.
                  The ConfigMap must be in the same namespace as the OGXServer
                  and must have the label ogx.io/watch: "true".
                properties:
                  key:
                    description: Key is the key within the ConfigMap.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                    type: string
                  name:
                    description: Name is the name of the ConfigMap.
                    minLength: 1
                    type: string
                required:
                - key
                - name
                type: object
              providers:
                description: |-
                  Providers configures providers by API type.
                  Mutually exclusive with overrideConfig.
                properties:
                  batches:
                    description: BatchesProvidersSpec configures batches providers.
                    properties:
                      inline:
                        description: BatchesInlineProviders groups inline batches
                          providers.
                        properties:
                          custom:
                            items:
                              description: CustomProvider defines the configuration
                                for a custom provider instance.
                              properties:
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                secretRefs:
                                  additionalProperties:
                                    description: |-
                                      SecretKeyRef references a specific key in a Kubernetes Secret.
                                      The Secret must be in the same namespace as the OGXServer and must have
                                      the label ogx.io/watch: "true" to be detected by the operator's cache.
                                    properties:
                                      key:
                                        description: Key is the key within the Secret.
                                        maxLength: 253
                                        minLength: 1
                                        pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                        type: string
                                      name:
                                        description: Name is the name of the Kubernetes
                                          Secret.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                  description: |-
                                    SecretRefs is a map of named secret references for provider-specific
                                    connection fields (e.g., host, password). Each key becomes the env var
                                    field suffix and maps to config.<key> with env var substitution.
                                    Each Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  minProperties: 1
                                  type: object
                                settings:
                                  description: |-
                                    Settings contains provider-specific configuration merged into the
                                    provider's config section in config.yaml. Passed through as-is
                                    without any secret resolution. Use secretRefs for secret values.
                                  x-kubernetes-preserve-unknown-fields: true
                                type:
                                  description: |-
                                    Type is the provider type, specified with a "remote::" or "inline::"
                                    prefix (e.g., "remote::llama-guard", "inline::my-provider").
                                  minLength: 1
                                  type: string
                              required:
                              - type
                              type: object
                              x-kubernetes-validations:
                              - message: type must have a 'remote::' or 'inline::'
                                  prefix (e.g., 'remote::llama-guard', 'inline::my-provider')
                                rule: self.type.startsWith('remote::') || self.type.startsWith('inline::')
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                          reference:
                            description: InlineReferenceProvider configures inline::reference
                              for batches.
                            properties:
                              maxConcurrentBatches:
                                description: |-
                                  MaxConcurrentBatches is the maximum number of concurrent batches
                                  to process simultaneously.
                                minimum: 1
                                type: integer
                              maxConcurrentRequestsPerBatch:
                                description: |-
                                  MaxConcurrentRequestsPerBatch is the maximum number of concurrent
                                  requests to process per batch.
                                minimum: 1
                                type: integer
                            type: object
                        type: object
                      remote:
                        description: BatchesRemoteProviders groups remote batches
                          providers.
                        properties:
                          custom:
                            items:
                              description: CustomProvider defines the configuration
                                for a custom provider instance.
                              properties:
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                secretRefs:
                                  additionalProperties:
                                    description: |-
                                      SecretKeyRef references a specific key in a Kubernetes Secret.
                                      The Secret must be in the same namespace as the OGXServer and must have
                                      the label ogx.io/watch: "true" to be detected by the operator's cache.
                                    properties:
                                      key:
                                        description: Key is the key within the Secret.
                                        maxLength: 253
                                        minLength: 1
                                        pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                        type: string
                                      name:
                                        description: Name is the name of the Kubernetes
                                          Secret.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                  description: |-
                                    SecretRefs is a map of named secret references for provider-specific
                                    connection fields (e.g., host, password). Each key becomes the env var
                                    field suffix and maps to config.<key> with env var substitution.
                                    Each Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  minProperties: 1
                                  type: object
                                settings:
                                  description: |-
                                    Settings contains provider-specific configuration merged into the
                                    provider's config section in config.yaml. Passed through as-is
                                    without any secret resolution. Use secretRefs for secret values.
                                  x-kubernetes-preserve-unknown-fields: true
                                type:
                                  description: |-
                                    Type is the provider type, specified with a "remote::" or "inline::"
                                    prefix (e.g., "remote::llama-guard", "inline::my-provider").
                                  minLength: 1
                                  type: string
                              required:
                              - type
                              type: object
                              x-kubernetes-validations:
                              - message: type must have a 'remote::' or 'inline::'
                                  prefix (e.g., 'remote::llama-guard', 'inline::my-provider')
                                rule: self.type.startsWith('remote::') || self.type.startsWith('inline::')
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                        type: object
                    type: object
                  fileProcessors:
                    description: FileProcessorsProvidersSpec configures file_processors
                      providers.
                    properties:
                      inline:
                        description: FileProcessorsInlineProviders groups inline file_processors
                          providers.
                        properties:
                          auto:
                            description: InlineAutoFileProcessorProvider configures
                              inline::auto for file_processors.
                            properties:
                              cleanText:
                                description: |-
                                  CleanText controls whether to clean extracted text
                                  (remove extra whitespace, normalize line breaks).
                                type: boolean
                              defaultChunkOverlapTokens:
                                description: |-
                                  DefaultChunkOverlapTokens is the default chunk overlap in tokens when
                                  chunking_strategy type is 'auto'.
                                maximum: 2048
                                minimum: 0
                                type: integer
                              defaultChunkSizeTokens:
                                description: |-
                                  DefaultChunkSizeTokens is the default chunk size in tokens when
                                  chunking_strategy type is 'auto'.
                                maximum: 4096
                                minimum: 100
                                type: integer
                              extractMetadata:
                                description: |-
                                  ExtractMetadata controls whether to extract PDF metadata
                                  (title, author, etc.).
                                type: boolean
                            type: object
                          custom:
                            items:
                              description: CustomProvider defines the configuration
                                for a custom provider instance.
                              properties:
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                secretRefs:
                                  additionalProperties:
                                    description: |-
                                      SecretKeyRef references a specific key in a Kubernetes Secret.
                                      The Secret must be in the same namespace as the OGXServer and must have
                                      the label ogx.io/watch: "true" to be detected by the operator's cache.
                                    properties:
                                      key:
                                        description: Key is the key within the Secret.
                                        maxLength: 253
                                        minLength: 1
                                        pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                        type: string
                                      name:
                                        description: Name is the name of the Kubernetes
                                          Secret.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                  description: |-
                                    SecretRefs is a map of named secret references for provider-specific
                                    connection fields (e.g., host, password). Each key becomes the env var
                                    field suffix and maps to config.<key> with env var substitution.
                                    Each Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  minProperties: 1
                                  type: object
                                settings:
                                  description: |-
                                    Settings contains provider-specific configuration merged into the
                                    provider's config section in config.yaml. Passed through as-is
                                    without any secret resolution. Use secretRefs for secret values.
                                  x-kubernetes-preserve-unknown-fields: true
                                type:
                                  description: |-
                                    Type is the provider type, specified with a "remote::" or "inline::"
                                    prefix (e.g., "remote::llama-guard", "inline::my-provider").
                                  minLength: 1
                                  type: string
                              required:
                              - type
                              type: object
                              x-kubernetes-validations:
                              - message: type must have a 'remote::' or 'inline::'
                                  prefix (e.g., 'remote::llama-guard', 'inline::my-provider')
                                rule: self.type.startsWith('remote::') || self.type.startsWith('inline::')
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                          docling:
                            description: InlineDoclingFileProcessorProvider configures
                              inline::docling for file_processors.
                            properties:
                              defaultChunkOverlapTokens:
                                description: |-
                                  DefaultChunkOverlapTokens is the default chunk overlap in tokens when
                                  chunking_strategy type is 'auto'.
                                maximum: 2048
                                minimum: 0
                                type: integer
                              defaultChunkSizeTokens:
                                description: |-
                                  DefaultChunkSizeTokens is the default chunk size in tokens when
                                  chunking_strategy type is 'auto'.
                                maximum: 4096
                                minimum: 100
                                type: integer
                              doOcr:
                                description: DoOCR controls whether to enable OCR
                                  for scanned documents.
                                type: boolean
                            type: object
                          markitdown:
                            description: InlineMarkItDownFileProcessorProvider configures
                              inline::markitdown for file_processors.
                            properties:
                              defaultChunkOverlapTokens:
                                description: |-
                                  DefaultChunkOverlapTokens is the default chunk overlap in tokens when
                                  chunking_strategy type is 'auto'.
                                maximum: 2048
                                minimum: 0
                                type: integer
                              defaultChunkSizeTokens:
                                description: |-
                                  DefaultChunkSizeTokens is the default chunk size in tokens when
                                  chunking_strategy type is 'auto'.
                                maximum: 4096
                                minimum: 100
                                type: integer
                            type: object
                          pypdf:
                            description: InlinePyPDFFileProcessorProvider configures
                              inline::pypdf for file_processors.
                            properties:
                              cleanText:
                                description: |-
                                  CleanText controls whether to clean extracted text
                                  (remove extra whitespace, normalize line breaks).
                                type: boolean
                              defaultChunkOverlapTokens:
                                description: |-
                                  DefaultChunkOverlapTokens is the default chunk overlap in tokens when
                                  chunking_strategy type is 'auto'.
                                maximum: 2048
                                minimum: 0
                                type: integer
                              defaultChunkSizeTokens:
                                description: |-
                                  DefaultChunkSizeTokens is the default chunk size in tokens when
                                  chunking_strategy type is 'auto'.
                                maximum: 4096
                                minimum: 100
                                type: integer
                              extractMetadata:
                                description: |-
                                  ExtractMetadata controls whether to extract PDF metadata
                                  (title, author, etc.).
                                type: boolean
                            type: object
                        type: object
                      remote:
                        description: FileProcessorsRemoteProviders groups remote file_processors
                          providers.
                        properties:
                          custom:
                            items:
                              description: CustomProvider defines the configuration
                                for a custom provider instance.
                              properties:
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                secretRefs:
                                  additionalProperties:
                                    description: |-
                                      SecretKeyRef references a specific key in a Kubernetes Secret.
                                      The Secret must be in the same namespace as the OGXServer and must have
                                      the label ogx.io/watch: "true" to be detected by the operator's cache.
                                    properties:
                                      key:
                                        description: Key is the key within the Secret.
                                        maxLength: 253
                                        minLength: 1
                                        pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                        type: string
                                      name:
                                        description: Name is the name of the Kubernetes
                                          Secret.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                  description: |-
                                    SecretRefs is a map of named secret references for provider-specific
                                    connection fields (e.g., host, password). Each key becomes the env var
                                    field suffix and maps to config.<key> with env var substitution.
                                    Each Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  minProperties: 1
                                  type: object
                                settings:
                                  description: |-
                                    Settings contains provider-specific configuration merged into the
                                    provider's config section in config.yaml. Passed through as-is
                                    without any secret resolution. Use secretRefs for secret values.
                                  x-kubernetes-preserve-unknown-fields: true
                                type:
                                  description: |-
                                    Type is the provider type, specified with a "remote::" or "inline::"
                                    prefix (e.g., "remote::llama-guard", "inline::my-provider").
                                  minLength: 1
                                  type: string
                              required:
                              - type
                              type: object
                              x-kubernetes-validations:
                              - message: type must have a 'remote::' or 'inline::'
                                  prefix (e.g., 'remote::llama-guard', 'inline::my-provider')
                                rule: self.type.startsWith('remote::') || self.type.startsWith('inline::')
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                          doclingServe:
                            description: DoclingServeProvider configures a remote::docling-serve
                              file_processors provider instance.
                            properties:
                              apiKey:
                                description: |-
                                  APIKey is the API key for authenticating with Docling Serve.
                                  The Secret must be in the same namespace as the OGXServer
                                  and must have the label ogx.io/watch: "true".
                                properties:
                                  key:
                                    description: Key is the key within the Secret.
                                    maxLength: 253
                                    minLength: 1
                                    pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                    type: string
                                  name:
                                    description: Name is the name of the Kubernetes
                                      Secret.
                                    minLength: 1
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              baseUrl:
                                description: BaseURL is the base URL of the Docling
                                  Serve instance.
                                minLength: 1
                                type: string
                              defaultChunkSizeTokens:
                                description: |-
                                  DefaultChunkSizeTokens is the default chunk size in tokens when
                                  chunking_strategy type is 'auto'.
                                maximum: 4096
                                minimum: 100
                                type: integer
                            required:
                            - baseUrl
                            type: object
                            x-kubernetes-validations:
                            - message: baseUrl must not be empty if specified
                              rule: '!has(self.baseUrl) || self.baseUrl.size() > 0'
                        type: object
                    type: object
                  files:
                    description: FilesProvidersSpec configures files providers.
                    properties:
                      inline:
                        description: FilesInlineProviders groups inline files providers.
                        properties:
                          custom:
                            items:
                              description: CustomProvider defines the configuration
                                for a custom provider instance.
                              properties:
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                secretRefs:
                                  additionalProperties:
                                    description: |-
                                      SecretKeyRef references a specific key in a Kubernetes Secret.
                                      The Secret must be in the same namespace as the OGXServer and must have
                                      the label ogx.io/watch: "true" to be detected by the operator's cache.
                                    properties:
                                      key:
                                        description: Key is the key within the Secret.
                                        maxLength: 253
                                        minLength: 1
                                        pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                        type: string
                                      name:
                                        description: Name is the name of the Kubernetes
                                          Secret.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                  description: |-
                                    SecretRefs is a map of named secret references for provider-specific
                                    connection fields (e.g., host, password). Each key becomes the env var
                                    field suffix and maps to config.<key> with env var substitution.
                                    Each Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  minProperties: 1
                                  type: object
                                settings:
                                  description: |-
                                    Settings contains provider-specific configuration merged into the
                                    provider's config section in config.yaml. Passed through as-is
                                    without any secret resolution. Use secretRefs for secret values.
                                  x-kubernetes-preserve-unknown-fields: true
                                type:
                                  description: |-
                                    Type is the provider type, specified with a "remote::" or "inline::"
                                    prefix (e.g., "remote::llama-guard", "inline::my-provider").
                                  minLength: 1
                                  type: string
                              required:
                              - type
                              type: object
                              x-kubernetes-validations:
                              - message: type must have a 'remote::' or 'inline::'
                                  prefix (e.g., 'remote::llama-guard', 'inline::my-provider')
                                rule: self.type.startsWith('remote::') || self.type.startsWith('inline::')
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                          localfs:
                            description: InlineLocalFSProvider configures inline::localfs.
                            properties:
                              ttlSecs:
                                description: TTLSecs is the time-to-live in seconds
                                  for uploaded files.
                                minimum: 1
                                type: integer
                            type: object
                        type: object
                      remote:
                        description: FilesRemoteProviders groups remote files providers.
                        properties:
                          custom:
                            items:
                              description: CustomProvider defines the configuration
                                for a custom provider instance.
                              properties:
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                secretRefs:
                                  additionalProperties:
                                    description: |-
                                      SecretKeyRef references a specific key in a Kubernetes Secret.
                                      The Secret must be in the same namespace as the OGXServer and must have
                                      the label ogx.io/watch: "true" to be detected by the operator's cache.
                                    properties:
                                      key:
                                        description: Key is the key within the Secret.
                                        maxLength: 253
                                        minLength: 1
                                        pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                        type: string
                                      name:
                                        description: Name is the name of the Kubernetes
                                          Secret.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                  description: |-
                                    SecretRefs is a map of named secret references for provider-specific
                                    connection fields (e.g., host, password). Each key becomes the env var
                                    field suffix and maps to config.<key> with env var substitution.
                                    Each Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  minProperties: 1
                                  type: object
                                settings:
                                  description: |-
                                    Settings contains provider-specific configuration merged into the
                                    provider's config section in config.yaml. Passed through as-is
                                    without any secret resolution. Use secretRefs for secret values.
                                  x-kubernetes-preserve-unknown-fields: true
                                type:
                                  description: |-
                                    Type is the provider type, specified with a "remote::" or "inline::"
                                    prefix (e.g., "remote::llama-guard", "inline::my-provider").
                                  minLength: 1
                                  type: string
                              required:
                              - type
                              type: object
                              x-kubernetes-validations:
                              - message: type must have a 'remote::' or 'inline::'
                                  prefix (e.g., 'remote::llama-guard', 'inline::my-provider')
                                rule: self.type.startsWith('remote::') || self.type.startsWith('inline::')
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                          s3:
                            description: S3Provider configures a remote::s3 files
                              provider instance.
                            properties:
                              autoCreateBucket:
                                description: |-
                                  AutoCreateBucket controls whether to automatically create the S3 bucket
                                  if it doesn't exist.
                                type: boolean
                              awsAccessKeyId:
                                description: |-
                                  AWSAccessKeyID is the AWS access key ID (optional if using IAM roles).
                                  The Secret must be in the same namespace as the OGXServer
                                  and must have the label ogx.io/watch: "true".
                                properties:
                                  key:
                                    description: Key is the key within the Secret.
                                    maxLength: 253
                                    minLength: 1
                                    pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                    type: string
                                  name:
                                    description: Name is the name of the Kubernetes
                                      Secret.
                                    minLength: 1
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              awsSecretAccessKey:
                                description: |-
                                  AWSSecretAccessKey is the AWS secret access key (optional if using IAM roles).
                                  The Secret must be in the same namespace as the OGXServer
                                  and must have the label ogx.io/watch: "true".
                                properties:
                                  key:
                                    description: Key is the key within the Secret.
                                    maxLength: 253
                                    minLength: 1
                                    pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                    type: string
                                  name:
                                    description: Name is the name of the Kubernetes
                                      Secret.
                                    minLength: 1
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              bucketName:
                                description: BucketName is the S3 bucket name to store
                                  files.
                                minLength: 1
                                type: string
                              endpointUrl:
                                description: EndpointURL is a custom S3 endpoint URL
                                  (for MinIO, LocalStack, etc.).
                                type: string
                              region:
                                description: Region is the AWS region where the bucket
                                  is located.
                                type: string
                            required:
                            - bucketName
                            type: object
                            x-kubernetes-validations:
                            - message: region must not be empty if specified
                              rule: '!has(self.region) || self.region.size() > 0'
                            - message: endpointUrl must not be empty if specified
                              rule: '!has(self.endpointUrl) || self.endpointUrl.size()
                                > 0'
                        type: object
                    type: object
                  inference:
                    description: InferenceProvidersSpec configures inference providers.
                    properties:
                      inline:
                        description: InferenceInlineProviders groups inline inference
                          providers.
                        properties:
                          custom:
                            items:
                              description: CustomProvider defines the configuration
                                for a custom provider instance.
                              properties:
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                secretRefs:
                                  additionalProperties:
                                    description: |-
                                      SecretKeyRef references a specific key in a Kubernetes Secret.
                                      The Secret must be in the same namespace as the OGXServer and must have
                                      the label ogx.io/watch: "true" to be detected by the operator's cache.
                                    properties:
                                      key:
                                        description: Key is the key within the Secret.
                                        maxLength: 253
                                        minLength: 1
                                        pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                        type: string
                                      name:
                                        description: Name is the name of the Kubernetes
                                          Secret.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                  description: |-
                                    SecretRefs is a map of named secret references for provider-specific
                                    connection fields (e.g., host, password). Each key becomes the env var
                                    field suffix and maps to config.<key> with env var substitution.
                                    Each Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  minProperties: 1
                                  type: object
                                settings:
                                  description: |-
                                    Settings contains provider-specific configuration merged into the
                                    provider's config section in config.yaml. Passed through as-is
                                    without any secret resolution. Use secretRefs for secret values.
                                  x-kubernetes-preserve-unknown-fields: true
                                type:
                                  description: |-
                                    Type is the provider type, specified with a "remote::" or "inline::"
                                    prefix (e.g., "remote::llama-guard", "inline::my-provider").
                                  minLength: 1
                                  type: string
                              required:
                              - type
                              type: object
                              x-kubernetes-validations:
                              - message: type must have a 'remote::' or 'inline::'
                                  prefix (e.g., 'remote::llama-guard', 'inline::my-provider')
                                rule: self.type.startsWith('remote::') || self.type.startsWith('inline::')
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                        type: object
                      remote:
                        description: InferenceRemoteProviders groups remote inference
                          providers.
                        properties:
                          azure:
                            items:
                              description: AzureProvider configures a remote::azure
                                inference provider instance.
                              properties:
                                allowedModels:
                                  description: |-
                                    AllowedModels restricts which models can be registered with this provider.
                                    When empty, all models are allowed.
                                  items:
                                    minLength: 1
                                    type: string
                                  minItems: 1
                                  type: array
                                apiKey:
                                  description: |-
                                    APIKey is the authentication credential for the Azure provider.
                                    The Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  properties:
                                    key:
                                      description: Key is the key within the Secret.
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                      type: string
                                    name:
                                      description: Name is the name of the Kubernetes
                                        Secret.
                                      minLength: 1
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                apiType:
                                  description: APIType is the Azure API type (e.g.,
                                    azure).
                                  type: string
                                apiVersion:
                                  description: APIVersion is the Azure API version
                                    (e.g., 2024-12-01-preview).
                                  type: string
                                endpoint:
                                  description: |-
                                    Endpoint is the Azure API base URL
                                    (e.g., https://your-resource-name.openai.azure.com/openai/v1).
                                  minLength: 1
                                  type: string
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                network:
                                  description: |-
                                    Network configures network settings (TLS, proxy, timeouts, headers)
                                    for the remote connection.
                                  properties:
                                    headers:
                                      additionalProperties:
                                        type: string
                                      description: Headers specifies additional HTTP
                                        headers to include in all requests.
                                      minProperties: 1
                                      type: object
                                    proxy:
                                      description: Proxy configures HTTP proxy settings.
                                      properties:
                                        cacert:
                                          description: CACert is the path to a CA
                                            certificate for verifying the proxy's
                                            certificate.
                                          type: string
                                        http:
                                          description: HTTP is the proxy URL for HTTP
                                            connections.
                                          type: string
                                        https:
                                          description: HTTPS is the proxy URL for
                                            HTTPS connections.
                                          type: string
                                        noProxy:
                                          description: NoProxy is a list of hosts
                                            that should bypass the proxy.
                                          items:
                                            minLength: 1
                                            type: string
                                          minItems: 1
                                          type: array
                                        url:
                                          description: URL is the proxy URL for all
                                            connections.
                                          type: string
                                      type: object
                                      x-kubernetes-validations:
                                      - message: url must not be empty if specified
                                        rule: '!has(self.url) || self.url.size() >
                                          0'
                                      - message: http must not be empty if specified
                                        rule: '!has(self.http) || self.http.size()
                                          > 0'
                                      - message: https must not be empty if specified
                                        rule: '!has(self.https) || self.https.size()
                                          > 0'
                                      - message: cacert must not be empty if specified
                                        rule: '!has(self.cacert) || self.cacert.size()
                                          > 0'
                                    timeout:
                                      description: Timeout configures connection and
                                        read timeout settings.
                                      properties:
                                        connect:
                                          description: Connect is the connection timeout
                                            in seconds.
                                          minimum: 1
                                          type: integer
                                        read:
                                          description: Read is the read timeout in
                                            seconds.
                                          minimum: 1
                                          type: integer
                                      type: object
                                    tls:
                                      description: TLS configures TLS/SSL settings.
                                      properties:
                                        ciphers:
                                          description: Ciphers is a list of allowed
                                            TLS cipher suites.
                                          items:
                                            minLength: 1
                                            type: string
                                          minItems: 1
                                          type: array
                                        minVersion:
                                          description: MinVersion sets the minimum
                                            TLS version.
                                          enum:
                                          - TLSv1.2
                                          - TLSv1.3
                                          type: string
                                        verify:
                                          description: |-
                                            Verify controls whether TLS certificate verification is enabled.
                                            Trust anchors and client identity are configured globally via spec.tls.
                                          type: boolean
                                      type: object
                                  type: object
                                refreshModels:
                                  description: |-
                                    RefreshModels controls whether the provider periodically refreshes
                                    its model list from the remote endpoint.
                                  type: boolean
                              required:
                              - apiKey
                              - endpoint
                              type: object
                              x-kubernetes-validations:
                              - message: apiVersion must not be empty if specified
                                rule: '!has(self.apiVersion) || self.apiVersion.size()
                                  > 0'
                              - message: apiType must not be empty if specified
                                rule: '!has(self.apiType) || self.apiType.size() >
                                  0'
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                          bedrock:
                            items:
                              description: BedrockProvider configures a remote::bedrock
                                inference provider instance.
                              properties:
                                allowedModels:
                                  description: |-
                                    AllowedModels restricts which models can be registered with this provider.
                                    When empty, all models are allowed.
                                  items:
                                    minLength: 1
                                    type: string
                                  minItems: 1
                                  type: array
                                apiKey:
                                  description: |-
                                    APIKey is the authentication credential for the Bedrock provider.
                                    The Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  properties:
                                    key:
                                      description: Key is the key within the Secret.
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                      type: string
                                    name:
                                      description: Name is the name of the Kubernetes
                                        Secret.
                                      minLength: 1
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                awsAccessKeyId:
                                  description: |-
                                    AWSAccessKeyID is the AWS access key to use.
                                    The Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  properties:
                                    key:
                                      description: Key is the key within the Secret.
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                      type: string
                                    name:
                                      description: Name is the name of the Kubernetes
                                        Secret.
                                      minLength: 1
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                awsRoleArn:
                                  description: AWSRoleArn is the AWS role ARN to assume.
                                  type: string
                                awsRoleSessionName:
                                  description: AWSRoleSessionName is the session name
                                    to use when assuming a role.
                                  type: string
                                awsSecretAccessKey:
                                  description: |-
                                    AWSSecretAccessKey is the AWS secret access key to use.
                                    The Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  properties:
                                    key:
                                      description: Key is the key within the Secret.
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                      type: string
                                    name:
                                      description: Name is the name of the Kubernetes
                                        Secret.
                                      minLength: 1
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                awsSessionToken:
                                  description: |-
                                    AWSSessionToken is the AWS session token to use.
                                    The Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  properties:
                                    key:
                                      description: Key is the key within the Secret.
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                      type: string
                                    name:
                                      description: Name is the name of the Kubernetes
                                        Secret.
                                      minLength: 1
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                awsWebIdentityTokenFile:
                                  description: AWSWebIdentityTokenFile is the path
                                    to the web identity token file.
                                  type: string
                                connectTimeout:
                                  description: ConnectTimeout is the connection timeout
                                    in seconds.
                                  minimum: 1
                                  type: integer
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                network:
                                  description: |-
                                    Network configures network settings (TLS, proxy, timeouts, headers)
                                    for the remote connection.
                                  properties:
                                    headers:
                                      additionalProperties:
                                        type: string
                                      description: Headers specifies additional HTTP
                                        headers to include in all requests.
                                      minProperties: 1
                                      type: object
                                    proxy:
                                      description: Proxy configures HTTP proxy settings.
                                      properties:
                                        cacert:
                                          description: CACert is the path to a CA
                                            certificate for verifying the proxy's
                                            certificate.
                                          type: string
                                        http:
                                          description: HTTP is the proxy URL for HTTP
                                            connections.
                                          type: string
                                        https:
                                          description: HTTPS is the proxy URL for
                                            HTTPS connections.
                                          type: string
                                        noProxy:
                                          description: NoProxy is a list of hosts
                                            that should bypass the proxy.
                                          items:
                                            minLength: 1
                                            type: string
                                          minItems: 1
                                          type: array
                                        url:
                                          description: URL is the proxy URL for all
                                            connections.
                                          type: string
                                      type: object
                                      x-kubernetes-validations:
                                      - message: url must not be empty if specified
                                        rule: '!has(self.url) || self.url.size() >
                                          0'
                                      - message: http must not be empty if specified
                                        rule: '!has(self.http) || self.http.size()
                                          > 0'
                                      - message: https must not be empty if specified
                                        rule: '!has(self.https) || self.https.size()
                                          > 0'
                                      - message: cacert must not be empty if specified
                                        rule: '!has(self.cacert) || self.cacert.size()
                                          > 0'
                                    timeout:
                                      description: Timeout configures connection and
                                        read timeout settings.
                                      properties:
                                        connect:
                                          description: Connect is the connection timeout
                                            in seconds.
                                          minimum: 1
                                          type: integer
                                        read:
                                          description: Read is the read timeout in
                                            seconds.
                                          minimum: 1
                                          type: integer
                                      type: object
                                    tls:
                                      description: TLS configures TLS/SSL settings.
                                      properties:
                                        ciphers:
                                          description: Ciphers is a list of allowed
                                            TLS cipher suites.
                                          items:
                                            minLength: 1
                                            type: string
                                          minItems: 1
                                          type: array
                                        minVersion:
                                          description: MinVersion sets the minimum
                                            TLS version.
                                          enum:
                                          - TLSv1.2
                                          - TLSv1.3
                                          type: string
                                        verify:
                                          description: |-
                                            Verify controls whether TLS certificate verification is enabled.
                                            Trust anchors and client identity are configured globally via spec.tls.
                                          type: boolean
                                      type: object
                                  type: object
                                profileName:
                                  description: ProfileName is the AWS profile name
                                    that contains credentials to use.
                                  type: string
                                readTimeout:
                                  description: ReadTimeout is the read timeout in
                                    seconds.
                                  minimum: 1
                                  type: integer
                                refreshModels:
                                  description: |-
                                    RefreshModels controls whether the provider periodically refreshes
                                    its model list from the remote endpoint.
                                  type: boolean
                                region:
                                  description: Region is the AWS region for the Bedrock
                                    Runtime endpoint.
                                  minLength: 1
                                  type: string
                                retryMode:
                                  description: RetryMode is the type of retries to
                                    perform (e.g., standard, adaptive).
                                  type: string
                                sessionTTL:
                                  description: SessionTTL is the time in seconds until
                                    a session expires.
                                  minimum: 1
                                  type: integer
                                totalMaxAttempts:
                                  description: |-
                                    TotalMaxAttempts is the maximum number of attempts for a single request,
                                    including the initial attempt.
                                  minimum: 1
                                  type: integer
                              required:
                              - region
                              type: object
                              x-kubernetes-validations:
                              - message: awsRoleArn must not be empty if specified
                                rule: '!has(self.awsRoleArn) || self.awsRoleArn.size()
                                  > 0'
                              - message: awsWebIdentityTokenFile must not be empty
                                  if specified
                                rule: '!has(self.awsWebIdentityTokenFile) || self.awsWebIdentityTokenFile.size()
                                  > 0'
                              - message: awsRoleSessionName must not be empty if specified
                                rule: '!has(self.awsRoleSessionName) || self.awsRoleSessionName.size()
                                  > 0'
                              - message: profileName must not be empty if specified
                                rule: '!has(self.profileName) || self.profileName.size()
                                  > 0'
                              - message: retryMode must not be empty if specified
                                rule: '!has(self.retryMode) || self.retryMode.size()
                                  > 0'
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                          custom:
                            items:
                              description: CustomProvider defines the configuration
                                for a custom provider instance.
                              properties:
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                secretRefs:
                                  additionalProperties:
                                    description: |-
                                      SecretKeyRef references a specific key in a Kubernetes Secret.
                                      The Secret must be in the same namespace as the OGXServer and must have
                                      the label ogx.io/watch: "true" to be detected by the operator's cache.
                                    properties:
                                      key:
                                        description: Key is the key within the Secret.
                                        maxLength: 253
                                        minLength: 1
                                        pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                        type: string
                                      name:
                                        description: Name is the name of the Kubernetes
                                          Secret.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                  description: |-
                                    SecretRefs is a map of named secret references for provider-specific
                                    connection fields (e.g., host, password). Each key becomes the env var
                                    field suffix and maps to config.<key> with env var substitution.
                                    Each Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  minProperties: 1
                                  type: object
                                settings:
                                  description: |-
                                    Settings contains provider-specific configuration merged into the
                                    provider's config section in config.yaml. Passed through as-is
                                    without any secret resolution. Use secretRefs for secret values.
                                  x-kubernetes-preserve-unknown-fields: true
                                type:
                                  description: |-
                                    Type is the provider type, specified with a "remote::" or "inline::"
                                    prefix (e.g., "remote::llama-guard", "inline::my-provider").
                                  minLength: 1
                                  type: string
                              required:
                              - type
                              type: object
                              x-kubernetes-validations:
                              - message: type must have a 'remote::' or 'inline::'
                                  prefix (e.g., 'remote::llama-guard', 'inline::my-provider')
                                rule: self.type.startsWith('remote::') || self.type.startsWith('inline::')
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                          openai:
                            items:
                              description: OpenAIProvider configures a remote::openai
                                inference provider instance.
                              properties:
                                allowedModels:
                                  description: |-
                                    AllowedModels restricts which models can be registered with this provider.
                                    When empty, all models are allowed.
                                  items:
                                    minLength: 1
                                    type: string
                                  minItems: 1
                                  type: array
                                apiKey:
                                  description: |-
                                    APIKey is the authentication credential for the OpenAI provider.
                                    The Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  properties:
                                    key:
                                      description: Key is the key within the Secret.
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                      type: string
                                    name:
                                      description: Name is the name of the Kubernetes
                                        Secret.
                                      minLength: 1
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                endpoint:
                                  description: Endpoint is the base URL for the OpenAI
                                    API.
                                  type: string
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                network:
                                  description: |-
                                    Network configures network settings (TLS, proxy, timeouts, headers)
                                    for the remote connection.
                                  properties:
                                    headers:
                                      additionalProperties:
                                        type: string
                                      description: Headers specifies additional HTTP
                                        headers to include in all requests.
                                      minProperties: 1
                                      type: object
                                    proxy:
                                      description: Proxy configures HTTP proxy settings.
                                      properties:
                                        cacert:
                                          description: CACert is the path to a CA
                                            certificate for verifying the proxy's
                                            certificate.
                                          type: string
                                        http:
                                          description: HTTP is the proxy URL for HTTP
                                            connections.
                                          type: string
                                        https:
                                          description: HTTPS is the proxy URL for
                                            HTTPS connections.
                                          type: string
                                        noProxy:
                                          description: NoProxy is a list of hosts
                                            that should bypass the proxy.
                                          items:
                                            minLength: 1
                                            type: string
                                          minItems: 1
                                          type: array
                                        url:
                                          description: URL is the proxy URL for all
                                            connections.
                                          type: string
                                      type: object
                                      x-kubernetes-validations:
                                      - message: url must not be empty if specified
                                        rule: '!has(self.url) || self.url.size() >
                                          0'
                                      - message: http must not be empty if specified
                                        rule: '!has(self.http) || self.http.size()
                                          > 0'
                                      - message: https must not be empty if specified
                                        rule: '!has(self.https) || self.https.size()
                                          > 0'
                                      - message: cacert must not be empty if specified
                                        rule: '!has(self.cacert) || self.cacert.size()
                                          > 0'
                                    timeout:
                                      description: Timeout configures connection and
                                        read timeout settings.
                                      properties:
                                        connect:
                                          description: Connect is the connection timeout
                                            in seconds.
                                          minimum: 1
                                          type: integer
                                        read:
                                          description: Read is the read timeout in
                                            seconds.
                                          minimum: 1
                                          type: integer
                                      type: object
                                    tls:
                                      description: TLS configures TLS/SSL settings.
                                      properties:
                                        ciphers:
                                          description: Ciphers is a list of allowed
                                            TLS cipher suites.
                                          items:
                                            minLength: 1
                                            type: string
                                          minItems: 1
                                          type: array
                                        minVersion:
                                          description: MinVersion sets the minimum
                                            TLS version.
                                          enum:
                                          - TLSv1.2
                                          - TLSv1.3
                                          type: string
                                        verify:
                                          description: |-
                                            Verify controls whether TLS certificate verification is enabled.
                                            Trust anchors and client identity are configured globally via spec.tls.
                                          type: boolean
                                      type: object
                                  type: object
                                refreshModels:
                                  description: |-
                                    RefreshModels controls whether the provider periodically refreshes
                                    its model list from the remote endpoint.
                                  type: boolean
                              required:
                              - apiKey
                              type: object
                              x-kubernetes-validations:
                              - message: endpoint must not be empty if specified
                                rule: '!has(self.endpoint) || self.endpoint.size()
                                  > 0'
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                          vertexai:
                            items:
                              description: VertexAIProvider configures a remote::vertexai
                                inference provider instance.
                              properties:
                                allowedModels:
                                  description: |-
                                    AllowedModels restricts which models can be registered with this provider.
                                    When empty, all models are allowed.
                                  items:
                                    minLength: 1
                                    type: string
                                  minItems: 1
                                  type: array
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                location:
                                  description: Location is the Google Cloud location
                                    for Vertex AI.
                                  type: string
                                network:
                                  description: |-
                                    Network configures network settings (TLS, proxy, timeouts, headers)
                                    for the remote connection.
                                  properties:
                                    headers:
                                      additionalProperties:
                                        type: string
                                      description: Headers specifies additional HTTP
                                        headers to include in all requests.
                                      minProperties: 1
                                      type: object
                                    proxy:
                                      description: Proxy configures HTTP proxy settings.
                                      properties:
                                        cacert:
                                          description: CACert is the path to a CA
                                            certificate for verifying the proxy's
                                            certificate.
                                          type: string
                                        http:
                                          description: HTTP is the proxy URL for HTTP
                                            connections.
                                          type: string
                                        https:
                                          description: HTTPS is the proxy URL for
                                            HTTPS connections.
                                          type: string
                                        noProxy:
                                          description: NoProxy is a list of hosts
                                            that should bypass the proxy.
                                          items:
                                            minLength: 1
                                            type: string
                                          minItems: 1
                                          type: array
                                        url:
                                          description: URL is the proxy URL for all
                                            connections.
                                          type: string
                                      type: object
                                      x-kubernetes-validations:
                                      - message: url must not be empty if specified
                                        rule: '!has(self.url) || self.url.size() >
                                          0'
                                      - message: http must not be empty if specified
                                        rule: '!has(self.http) || self.http.size()
                                          > 0'
                                      - message: https must not be empty if specified
                                        rule: '!has(self.https) || self.https.size()
                                          > 0'
                                      - message: cacert must not be empty if specified
                                        rule: '!has(self.cacert) || self.cacert.size()
                                          > 0'
                                    timeout:
                                      description: Timeout configures connection and
                                        read timeout settings.
                                      properties:
                                        connect:
                                          description: Connect is the connection timeout
                                            in seconds.
                                          minimum: 1
                                          type: integer
                                        read:
                                          description: Read is the read timeout in
                                            seconds.
                                          minimum: 1
                                          type: integer
                                      type: object
                                    tls:
                                      description: TLS configures TLS/SSL settings.
                                      properties:
                                        ciphers:
                                          description: Ciphers is a list of allowed
                                            TLS cipher suites.
                                          items:
                                            minLength: 1
                                            type: string
                                          minItems: 1
                                          type: array
                                        minVersion:
                                          description: MinVersion sets the minimum
                                            TLS version.
                                          enum:
                                          - TLSv1.2
                                          - TLSv1.3
                                          type: string
                                        verify:
                                          description: |-
                                            Verify controls whether TLS certificate verification is enabled.
                                            Trust anchors and client identity are configured globally via spec.tls.
                                          type: boolean
                                      type: object
                                  type: object
                                project:
                                  description: Project is the Google Cloud project
                                    ID for Vertex AI.
                                  minLength: 1
                                  type: string
                                refreshModels:
                                  description: |-
                                    RefreshModels controls whether the provider periodically refreshes
                                    its model list from the remote endpoint.
                                  type: boolean
                              required:
                              - project
                              type: object
                              x-kubernetes-validations:
                              - message: location must not be empty if specified
                                rule: '!has(self.location) || self.location.size()
                                  > 0'
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                          vllm:
                            items:
                              description: VLLMProvider configures a remote::vllm
                                inference provider instance.
                              properties:
                                allowedModels:
                                  description: |-
                                    AllowedModels restricts which models can be registered with this provider.
                                    When empty, all models are allowed.
                                  items:
                                    minLength: 1
                                    type: string
                                  minItems: 1
                                  type: array
                                apiToken:
                                  description: |-
                                    APIToken is the authentication token for the vLLM endpoint.
                                    The Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  properties:
                                    key:
                                      description: Key is the key within the Secret.
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                      type: string
                                    name:
                                      description: Name is the name of the Kubernetes
                                        Secret.
                                      minLength: 1
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                endpoint:
                                  description: Endpoint is the URL for the vLLM model
                                    serving endpoint.
                                  minLength: 1
                                  type: string
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                maxTokens:
                                  description: MaxTokens is the maximum number of
                                    tokens to generate.
                                  minimum: 1
                                  type: integer
                                network:
                                  description: |-
                                    Network configures network settings (TLS, proxy, timeouts, headers)
                                    for the remote connection.
                                  properties:
                                    headers:
                                      additionalProperties:
                                        type: string
                                      description: Headers specifies additional HTTP
                                        headers to include in all requests.
                                      minProperties: 1
                                      type: object
                                    proxy:
                                      description: Proxy configures HTTP proxy settings.
                                      properties:
                                        cacert:
                                          description: CACert is the path to a CA
                                            certificate for verifying the proxy's
                                            certificate.
                                          type: string
                                        http:
                                          description: HTTP is the proxy URL for HTTP
                                            connections.
                                          type: string
                                        https:
                                          description: HTTPS is the proxy URL for
                                            HTTPS connections.
                                          type: string
                                        noProxy:
                                          description: NoProxy is a list of hosts
                                            that should bypass the proxy.
                                          items:
                                            minLength: 1
                                            type: string
                                          minItems: 1
                                          type: array
                                        url:
                                          description: URL is the proxy URL for all
                                            connections.
                                          type: string
                                      type: object
                                      x-kubernetes-validations:
                                      - message: url must not be empty if specified
                                        rule: '!has(self.url) || self.url.size() >
                                          0'
                                      - message: http must not be empty if specified
                                        rule: '!has(self.http) || self.http.size()
                                          > 0'
                                      - message: https must not be empty if specified
                                        rule: '!has(self.https) || self.https.size()
                                          > 0'
                                      - message: cacert must not be empty if specified
                                        rule: '!has(self.cacert) || self.cacert.size()
                                          > 0'
                                    timeout:
                                      description: Timeout configures connection and
                                        read timeout settings.
                                      properties:
                                        connect:
                                          description: Connect is the connection timeout
                                            in seconds.
                                          minimum: 1
                                          type: integer
                                        read:
                                          description: Read is the read timeout in
                                            seconds.
                                          minimum: 1
                                          type: integer
                                      type: object
                                    tls:
                                      description: TLS configures TLS/SSL settings.
                                      properties:
                                        ciphers:
                                          description: Ciphers is a list of allowed
                                            TLS cipher suites.
                                          items:
                                            minLength: 1
                                            type: string
                                          minItems: 1
                                          type: array
                                        minVersion:
                                          description: MinVersion sets the minimum
                                            TLS version.
                                          enum:
                                          - TLSv1.2
                                          - TLSv1.3
                                          type: string
                                        verify:
                                          description: |-
                                            Verify controls whether TLS certificate verification is enabled.
                                            Trust anchors and client identity are configured globally via spec.tls.
                                          type: boolean
                                      type: object
                                  type: object
                                refreshModels:
                                  description: |-
                                    RefreshModels controls whether the provider periodically refreshes
                                    its model list from the remote endpoint.
                                  type: boolean
                              required:
                              - endpoint
                              type: object
                              x-kubernetes-validations:
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                          watsonx:
                            items:
                              description: WatsonxProvider configures a remote::watsonx
                                inference provider instance.
                              properties:
                                allowedModels:
                                  description: |-
                                    AllowedModels restricts which models can be registered with this provider.
                                    When empty, all models are allowed.
                                  items:
                                    minLength: 1
                                    type: string
                                  minItems: 1
                                  type: array
                                apiKey:
                                  description: |-
                                    APIKey is the authentication credential for the watsonx provider.
                                    The Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  properties:
                                    key:
                                      description: Key is the key within the Secret.
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                      type: string
                                    name:
                                      description: Name is the name of the Kubernetes
                                        Secret.
                                      minLength: 1
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                endpoint:
                                  description: Endpoint is the base URL for accessing
                                    watsonx.ai.
                                  type: string
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                network:
                                  description: |-
                                    Network configures network settings (TLS, proxy, timeouts, headers)
                                    for the remote connection.
                                  properties:
                                    headers:
                                      additionalProperties:
                                        type: string
                                      description: Headers specifies additional HTTP
                                        headers to include in all requests.
                                      minProperties: 1
                                      type: object
                                    proxy:
                                      description: Proxy configures HTTP proxy settings.
                                      properties:
                                        cacert:
                                          description: CACert is the path to a CA
                                            certificate for verifying the proxy's
                                            certificate.
                                          type: string
                                        http:
                                          description: HTTP is the proxy URL for HTTP
                                            connections.
                                          type: string
                                        https:
                                          description: HTTPS is the proxy URL for
                                            HTTPS connections.
                                          type: string
                                        noProxy:
                                          description: NoProxy is a list of hosts
                                            that should bypass the proxy.
                                          items:
                                            minLength: 1
                                            type: string
                                          minItems: 1
                                          type: array
                                        url:
                                          description: URL is the proxy URL for all
                                            connections.
                                          type: string
                                      type: object
                                      x-kubernetes-validations:
                                      - message: url must not be empty if specified
                                        rule: '!has(self.url) || self.url.size() >
                                          0'
                                      - message: http must not be empty if specified
                                        rule: '!has(self.http) || self.http.size()
                                          > 0'
                                      - message: https must not be empty if specified
                                        rule: '!has(self.https) || self.https.size()
                                          > 0'
                                      - message: cacert must not be empty if specified
                                        rule: '!has(self.cacert) || self.cacert.size()
                                          > 0'
                                    timeout:
                                      description: Timeout configures connection and
                                        read timeout settings.
                                      properties:
                                        connect:
                                          description: Connect is the connection timeout
                                            in seconds.
                                          minimum: 1
                                          type: integer
                                        read:
                                          description: Read is the read timeout in
                                            seconds.
                                          minimum: 1
                                          type: integer
                                      type: object
                                    tls:
                                      description: TLS configures TLS/SSL settings.
                                      properties:
                                        ciphers:
                                          description: Ciphers is a list of allowed
                                            TLS cipher suites.
                                          items:
                                            minLength: 1
                                            type: string
                                          minItems: 1
                                          type: array
                                        minVersion:
                                          description: MinVersion sets the minimum
                                            TLS version.
                                          enum:
                                          - TLSv1.2
                                          - TLSv1.3
                                          type: string
                                        verify:
                                          description: |-
                                            Verify controls whether TLS certificate verification is enabled.
                                            Trust anchors and client identity are configured globally via spec.tls.
                                          type: boolean
                                      type: object
                                  type: object
                                projectId:
                                  description: ProjectID is the watsonx.ai project
                                    ID.
                                  type: string
                                refreshModels:
                                  description: |-
                                    RefreshModels controls whether the provider periodically refreshes
                                    its model list from the remote endpoint.
                                  type: boolean
                                timeout:
                                  description: Timeout is the timeout in seconds for
                                    HTTP requests.
                                  minimum: 1
                                  type: integer
                              required:
                              - apiKey
                              type: object
                              x-kubernetes-validations:
                              - message: endpoint must not be empty if specified
                                rule: '!has(self.endpoint) || self.endpoint.size()
                                  > 0'
                              - message: projectId must not be empty if specified
                                rule: '!has(self.projectId) || self.projectId.size()
                                  > 0'
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                        type: object
                    type: object
                  responses:
                    description: ResponsesProvidersSpec configures responses providers.
                    properties:
                      inline:
                        description: ResponsesInlineProviders groups inline responses
                          providers.
                        properties:
                          builtin:
                            description: InlineBuiltinResponsesProvider configures
                              inline::builtin for responses.
                            properties:
                              compactionConfig:
                                description: |-
                                  CompactionConfig configures conversation compaction behavior
                                  and prompt templates.
                                properties:
                                  defaultCompactThreshold:
                                    description: |-
                                      DefaultCompactThreshold is the token count threshold for auto-compaction.
                                      Conversations exceeding this count will be automatically compacted.
                                    minimum: 1
                                    type: integer
                                  summarizationModel:
                                    description: |-
                                      SummarizationModel is the model to use for generating compaction
                                      summaries. If unset, uses the same model as the conversation.
                                    type: string
                                  summarizationPrompt:
                                    description: |-
                                      SummarizationPrompt is the prompt used to instruct the model to
                                      summarize conversation history during compaction.
                                    type: string
                                  summaryPrefix:
                                    description: |-
                                      SummaryPrefix is text prepended to the compaction summary to frame
                                      it as a handoff for the next LLM context window.
                                    type: string
                                  tokenizerEncoding:
                                    description: |-
                                      TokenizerEncoding is the tiktoken encoding name for token counting
                                      (e.g., "o200k_base", "cl100k_base").
                                    type: string
                                type: object
                                x-kubernetes-validations:
                                - message: summarizationPrompt must not be empty if
                                    specified
                                  rule: '!has(self.summarizationPrompt) || self.summarizationPrompt.size()
                                    > 0'
                                - message: summaryPrefix must not be empty if specified
                                  rule: '!has(self.summaryPrefix) || self.summaryPrefix.size()
                                    > 0'
                                - message: summarizationModel must not be empty if
                                    specified
                                  rule: '!has(self.summarizationModel) || self.summarizationModel.size()
                                    > 0'
                                - message: tokenizerEncoding must not be empty if
                                    specified
                                  rule: '!has(self.tokenizerEncoding) || self.tokenizerEncoding.size()
                                    > 0'
                              vectorStoresConfig:
                                description: |-
                                  VectorStoresConfig configures vector store behavior for file search
                                  and retrieval-augmented generation.
                                properties:
                                  annotationPromptParams:
                                    description: AnnotationPromptParams configures
                                      source annotation settings.
                                    properties:
                                      annotationInstructionTemplate:
                                        description: AnnotationInstructionTemplate
                                          provides instructions for citing sources.
                                        type: string
                                      chunkAnnotationTemplate:
                                        description: ChunkAnnotationTemplate is the
                                          template for chunks with annotation info.
                                        type: string
                                      enableAnnotations:
                                        description: EnableAnnotations controls whether
                                          source annotations are included.
                                        type: boolean
                                    type: object
                                  chunkRetrievalParams:
                                    description: ChunkRetrievalParams configures chunk
                                      retrieval and ranking.
                                    properties:
                                      chunkMultiplier:
                                        description: ChunkMultiplier multiplies the
                                          number of chunks retrieved for over-retrieval.
                                        minimum: 1
                                        type: integer
                                      defaultRerankerStrategy:
                                        description: DefaultRerankerStrategy is the
                                          default reranking strategy.
                                        enum:
                                        - rrf
                                        - weighted
                                        - normalized
                                        type: string
                                      defaultSearchMode:
                                        description: DefaultSearchMode is the default
                                          search mode.
                                        enum:
                                        - vector
                                        - keyword
                                        - hybrid
                                        type: string
                                      maxTokensInContext:
                                        description: MaxTokensInContext limits total
                                          tokens allowed in RAG context.
                                        minimum: 1
                                        type: integer
                                      rrfImpactFactor:
                                        description: |-
                                          RRFImpactFactor is the impact factor for Reciprocal Rank Fusion reranking.
                                          Specified as a decimal string (e.g., "60.0").
                                        type: string
                                      weightedSearchAlpha:
                                        description: |-
                                          WeightedSearchAlpha is the alpha weight for weighted search reranking (0.0-1.0).
                                          Specified as a decimal string (e.g., "0.5").
                                        type: string
                                    type: object
                                  contextPromptParams:
                                    description: ContextPromptParams configures context
                                      prompt templates.
                                    properties:
                                      chunkAnnotationTemplate:
                                        description: ChunkAnnotationTemplate is the
                                          template for formatting individual chunks.
                                        type: string
                                      contextTemplate:
                                        description: ContextTemplate is the template
                                          for explaining search results to the model.
                                        type: string
                                    type: object
                                  contextualRetrievalParams:
                                    description: ContextualRetrievalParams configures
                                      contextual retrieval during ingestion.
                                    properties:
                                      defaultMaxConcurrency:
                                        description: DefaultMaxConcurrency limits
                                          concurrent LLM calls for contextualization.
                                        minimum: 1
                                        type: integer
                                      defaultTimeoutSeconds:
                                        description: DefaultTimeoutSeconds is the
                                          timeout per LLM contextualization call.
                                        minimum: 1
                                        type: integer
                                      maxDocumentTokens:
                                        description: MaxDocumentTokens limits document
                                          size in tokens for contextual retrieval.
                                        minimum: 1
                                        type: integer
                                      model:
                                        description: Model is the default LLM model
                                          for contextual retrieval.
                                        properties:
                                          embeddingDimensions:
                                            description: EmbeddingDimensions is the
                                              dimensionality of the embedding vectors.
                                            minimum: 1
                                            type: integer
                                          modelId:
                                            description: ModelID is the model identifier.
                                            minLength: 1
                                            type: string
                                          providerId:
                                            description: ProviderID is the provider
                                              to use for this model.
                                            minLength: 1
                                            type: string
                                        required:
                                        - modelId
                                        - providerId
                                        type: object
                                    type: object
                                  defaultEmbeddingModel:
                                    description: DefaultEmbeddingModel configures
                                      the default embedding model.
                                    properties:
                                      embeddingDimensions:
                                        description: EmbeddingDimensions is the dimensionality
                                          of the embedding vectors.
                                        minimum: 1
                                        type: integer
                                      modelId:
                                        description: ModelID is the model identifier.
                                        minLength: 1
                                        type: string
                                      providerId:
                                        description: ProviderID is the provider to
                                          use for this model.
                                        minLength: 1
                                        type: string
                                    required:
                                    - modelId
                                    - providerId
                                    type: object
                                  defaultProviderId:
                                    description: |-
                                      DefaultProviderID is the vector_io provider to use when multiple
                                      providers are available and none is specified.
                                    type: string
                                  defaultRerankerModel:
                                    description: DefaultRerankerModel configures the
                                      default reranker model.
                                    properties:
                                      modelId:
                                        description: ModelID is the model identifier.
                                        minLength: 1
                                        type: string
                                      providerId:
                                        description: ProviderID is the provider to
                                          use for this model.
                                        minLength: 1
                                        type: string
                                    required:
                                    - modelId
                                    - providerId
                                    type: object
                                  fileBatchParams:
                                    description: FileBatchParams configures file batch
                                      processing.
                                    properties:
                                      cleanupIntervalSeconds:
                                        description: CleanupIntervalSeconds is the
                                          interval between expired batch cleanup runs.
                                        minimum: 1
                                        type: integer
                                      fileBatchChunkSize:
                                        description: FileBatchChunkSize is the number
                                          of files to process in each batch chunk.
                                        minimum: 1
                                        type: integer
                                      maxConcurrentFilesPerBatch:
                                        description: MaxConcurrentFilesPerBatch limits
                                          concurrent files processed per batch.
                                        minimum: 1
                                        type: integer
                                    type: object
                                  fileIngestionParams:
                                    description: FileIngestionParams configures file
                                      ingestion chunk settings.
                                    properties:
                                      defaultChunkOverlapTokens:
                                        description: DefaultChunkOverlapTokens is
                                          the default overlap between chunks in tokens.
                                        minimum: 0
                                        type: integer
                                      defaultChunkSizeTokens:
                                        description: DefaultChunkSizeTokens is the
                                          default chunk size in tokens.
                                        minimum: 1
                                        type: integer
                                    type: object
                                  fileSearchParams:
                                    description: FileSearchParams configures file
                                      search output formatting.
                                    properties:
                                      footerTemplate:
                                        description: FooterTemplate is the template
                                          for the footer text after search results.
                                        type: string
                                      headerTemplate:
                                        description: HeaderTemplate is the template
                                          for the header text before search results.
                                        type: string
                                    type: object
                                  rewriteQueryParams:
                                    description: RewriteQueryParams configures query
                                      rewriting/expansion. Nil disables rewriting.
                                    properties:
                                      maxTokens:
                                        description: MaxTokens is the maximum number
                                          of tokens for query expansion responses.
                                        minimum: 1
                                        type: integer
                                      model:
                                        description: Model is the LLM model used for
                                          query rewriting.
                                        properties:
                                          embeddingDimensions:
                                            description: EmbeddingDimensions is the
                                              dimensionality of the embedding vectors.
                                            minimum: 1
                                            type: integer
                                          modelId:
                                            description: ModelID is the model identifier.
                                            minLength: 1
                                            type: string
                                          providerId:
                                            description: ProviderID is the provider
                                              to use for this model.
                                            minLength: 1
                                            type: string
                                        required:
                                        - modelId
                                        - providerId
                                        type: object
                                      prompt:
                                        description: |-
                                          Prompt is the prompt template for query rewriting.
                                          Use {query} as a placeholder for the original query.
                                        type: string
                                      temperature:
                                        description: |-
                                          Temperature controls randomness in query rewriting (0.0 = deterministic, 1.0 = creative).
                                          Specified as a decimal string (e.g., "0.7").
                                        type: string
                                    type: object
                                type: object
                                x-kubernetes-validations:
                                - message: defaultProviderId must not be empty if
                                    specified
                                  rule: '!has(self.defaultProviderId) || self.defaultProviderId.size()
                                    > 0'
                            type: object
                          custom:
                            items:
                              description: CustomProvider defines the configuration
                                for a custom provider instance.
                              properties:
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                secretRefs:
                                  additionalProperties:
                                    description: |-
                                      SecretKeyRef references a specific key in a Kubernetes Secret.
                                      The Secret must be in the same namespace as the OGXServer and must have
                                      the label ogx.io/watch: "true" to be detected by the operator's cache.
                                    properties:
                                      key:
                                        description: Key is the key within the Secret.
                                        maxLength: 253
                                        minLength: 1
                                        pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                        type: string
                                      name:
                                        description: Name is the name of the Kubernetes
                                          Secret.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                  description: |-
                                    SecretRefs is a map of named secret references for provider-specific
                                    connection fields (e.g., host, password). Each key becomes the env var
                                    field suffix and maps to config.<key> with env var substitution.
                                    Each Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  minProperties: 1
                                  type: object
                                settings:
                                  description: |-
                                    Settings contains provider-specific configuration merged into the
                                    provider's config section in config.yaml. Passed through as-is
                                    without any secret resolution. Use secretRefs for secret values.
                                  x-kubernetes-preserve-unknown-fields: true
                                type:
                                  description: |-
                                    Type is the provider type, specified with a "remote::" or "inline::"
                                    prefix (e.g., "remote::llama-guard", "inline::my-provider").
                                  minLength: 1
                                  type: string
                              required:
                              - type
                              type: object
                              x-kubernetes-validations:
                              - message: type must have a 'remote::' or 'inline::'
                                  prefix (e.g., 'remote::llama-guard', 'inline::my-provider')
                                rule: self.type.startsWith('remote::') || self.type.startsWith('inline::')
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                        type: object
                      remote:
                        description: ResponsesRemoteProviders groups remote responses
                          providers.
                        properties:
                          custom:
                            items:
                              description: CustomProvider defines the configuration
                                for a custom provider instance.
                              properties:
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                secretRefs:
                                  additionalProperties:
                                    description: |-
                                      SecretKeyRef references a specific key in a Kubernetes Secret.
                                      The Secret must be in the same namespace as the OGXServer and must have
                                      the label ogx.io/watch: "true" to be detected by the operator's cache.
                                    properties:
                                      key:
                                        description: Key is the key within the Secret.
                                        maxLength: 253
                                        minLength: 1
                                        pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                        type: string
                                      name:
                                        description: Name is the name of the Kubernetes
                                          Secret.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                  description: |-
                                    SecretRefs is a map of named secret references for provider-specific
                                    connection fields (e.g., host, password). Each key becomes the env var
                                    field suffix and maps to config.<key> with env var substitution.
                                    Each Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  minProperties: 1
                                  type: object
                                settings:
                                  description: |-
                                    Settings contains provider-specific configuration merged into the
                                    provider's config section in config.yaml. Passed through as-is
                                    without any secret resolution. Use secretRefs for secret values.
                                  x-kubernetes-preserve-unknown-fields: true
                                type:
                                  description: |-
                                    Type is the provider type, specified with a "remote::" or "inline::"
                                    prefix (e.g., "remote::llama-guard", "inline::my-provider").
                                  minLength: 1
                                  type: string
                              required:
                              - type
                              type: object
                              x-kubernetes-validations:
                              - message: type must have a 'remote::' or 'inline::'
                                  prefix (e.g., 'remote::llama-guard', 'inline::my-provider')
                                rule: self.type.startsWith('remote::') || self.type.startsWith('inline::')
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                        type: object
                    type: object
                  toolRuntime:
                    description: ToolRuntimeProvidersSpec configures tool runtime
                      providers.
                    properties:
                      inline:
                        description: ToolRuntimeInlineProviders groups inline tool
                          runtime providers.
                        properties:
                          custom:
                            items:
                              description: CustomProvider defines the configuration
                                for a custom provider instance.
                              properties:
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                secretRefs:
                                  additionalProperties:
                                    description: |-
                                      SecretKeyRef references a specific key in a Kubernetes Secret.
                                      The Secret must be in the same namespace as the OGXServer and must have
                                      the label ogx.io/watch: "true" to be detected by the operator's cache.
                                    properties:
                                      key:
                                        description: Key is the key within the Secret.
                                        maxLength: 253
                                        minLength: 1
                                        pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                        type: string
                                      name:
                                        description: Name is the name of the Kubernetes
                                          Secret.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                  description: |-
                                    SecretRefs is a map of named secret references for provider-specific
                                    connection fields (e.g., host, password). Each key becomes the env var
                                    field suffix and maps to config.<key> with env var substitution.
                                    Each Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  minProperties: 1
                                  type: object
                                settings:
                                  description: |-
                                    Settings contains provider-specific configuration merged into the
                                    provider's config section in config.yaml. Passed through as-is
                                    without any secret resolution. Use secretRefs for secret values.
                                  x-kubernetes-preserve-unknown-fields: true
                                type:
                                  description: |-
                                    Type is the provider type, specified with a "remote::" or "inline::"
                                    prefix (e.g., "remote::llama-guard", "inline::my-provider").
                                  minLength: 1
                                  type: string
                              required:
                              - type
                              type: object
                              x-kubernetes-validations:
                              - message: type must have a 'remote::' or 'inline::'
                                  prefix (e.g., 'remote::llama-guard', 'inline::my-provider')
                                rule: self.type.startsWith('remote::') || self.type.startsWith('inline::')
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                          fileSearch:
                            items:
                              description: InlineFileSearchProvider configures inline::file-search.
                              properties:
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                vectorStoresConfig:
                                  description: VectorStoresConfig configures vector
                                    store behavior for file search.
                                  properties:
                                    annotationPromptParams:
                                      description: AnnotationPromptParams configures
                                        source annotation settings.
                                      properties:
                                        annotationInstructionTemplate:
                                          description: AnnotationInstructionTemplate
                                            provides instructions for citing sources.
                                          type: string
                                        chunkAnnotationTemplate:
                                          description: ChunkAnnotationTemplate is
                                            the template for chunks with annotation
                                            info.
                                          type: string
                                        enableAnnotations:
                                          description: EnableAnnotations controls
                                            whether source annotations are included.
                                          type: boolean
                                      type: object
                                    chunkRetrievalParams:
                                      description: ChunkRetrievalParams configures
                                        chunk retrieval and ranking.
                                      properties:
                                        chunkMultiplier:
                                          description: ChunkMultiplier multiplies
                                            the number of chunks retrieved for over-retrieval.
                                          minimum: 1
                                          type: integer
                                        defaultRerankerStrategy:
                                          description: DefaultRerankerStrategy is
                                            the default reranking strategy.
                                          enum:
                                          - rrf
                                          - weighted
                                          - normalized
                                          type: string
                                        defaultSearchMode:
                                          description: DefaultSearchMode is the default
                                            search mode.
                                          enum:
                                          - vector
                                          - keyword
                                          - hybrid
                                          type: string
                                        maxTokensInContext:
                                          description: MaxTokensInContext limits total
                                            tokens allowed in RAG context.
                                          minimum: 1
                                          type: integer
                                        rrfImpactFactor:
                                          description: |-
                                            RRFImpactFactor is the impact factor for Reciprocal Rank Fusion reranking.
                                            Specified as a decimal string (e.g., "60.0").
                                          type: string
                                        weightedSearchAlpha:
                                          description: |-
                                            WeightedSearchAlpha is the alpha weight for weighted search reranking (0.0-1.0).
                                            Specified as a decimal string (e.g., "0.5").
                                          type: string
                                      type: object
                                    contextPromptParams:
                                      description: ContextPromptParams configures
                                        context prompt templates.
                                      properties:
                                        chunkAnnotationTemplate:
                                          description: ChunkAnnotationTemplate is
                                            the template for formatting individual
                                            chunks.
                                          type: string
                                        contextTemplate:
                                          description: ContextTemplate is the template
                                            for explaining search results to the model.
                                          type: string
                                      type: object
                                    contextualRetrievalParams:
                                      description: ContextualRetrievalParams configures
                                        contextual retrieval during ingestion.
                                      properties:
                                        defaultMaxConcurrency:
                                          description: DefaultMaxConcurrency limits
                                            concurrent LLM calls for contextualization.
                                          minimum: 1
                                          type: integer
                                        defaultTimeoutSeconds:
                                          description: DefaultTimeoutSeconds is the
                                            timeout per LLM contextualization call.
                                          minimum: 1
                                          type: integer
                                        maxDocumentTokens:
                                          description: MaxDocumentTokens limits document
                                            size in tokens for contextual retrieval.
                                          minimum: 1
                                          type: integer
                                        model:
                                          description: Model is the default LLM model
                                            for contextual retrieval.
                                          properties:
                                            embeddingDimensions:
                                              description: EmbeddingDimensions is
                                                the dimensionality of the embedding
                                                vectors.
                                              minimum: 1
                                              type: integer
                                            modelId:
                                              description: ModelID is the model identifier.
                                              minLength: 1
                                              type: string
                                            providerId:
                                              description: ProviderID is the provider
                                                to use for this model.
                                              minLength: 1
                                              type: string
                                          required:
                                          - modelId
                                          - providerId
                                          type: object
                                      type: object
                                    defaultEmbeddingModel:
                                      description: DefaultEmbeddingModel configures
                                        the default embedding model.
                                      properties:
                                        embeddingDimensions:
                                          description: EmbeddingDimensions is the
                                            dimensionality of the embedding vectors.
                                          minimum: 1
                                          type: integer
                                        modelId:
                                          description: ModelID is the model identifier.
                                          minLength: 1
                                          type: string
                                        providerId:
                                          description: ProviderID is the provider
                                            to use for this model.
                                          minLength: 1
                                          type: string
                                      required:
                                      - modelId
                                      - providerId
                                      type: object
                                    defaultProviderId:
                                      description: |-
                                        DefaultProviderID is the vector_io provider to use when multiple
                                        providers are available and none is specified.
                                      type: string
                                    defaultRerankerModel:
                                      description: DefaultRerankerModel configures
                                        the default reranker model.
                                      properties:
                                        modelId:
                                          description: ModelID is the model identifier.
                                          minLength: 1
                                          type: string
                                        providerId:
                                          description: ProviderID is the provider
                                            to use for this model.
                                          minLength: 1
                                          type: string
                                      required:
                                      - modelId
                                      - providerId
                                      type: object
                                    fileBatchParams:
                                      description: FileBatchParams configures file
                                        batch processing.
                                      properties:
                                        cleanupIntervalSeconds:
                                          description: CleanupIntervalSeconds is the
                                            interval between expired batch cleanup
                                            runs.
                                          minimum: 1
                                          type: integer
                                        fileBatchChunkSize:
                                          description: FileBatchChunkSize is the number
                                            of files to process in each batch chunk.
                                          minimum: 1
                                          type: integer
                                        maxConcurrentFilesPerBatch:
                                          description: MaxConcurrentFilesPerBatch
                                            limits concurrent files processed per
                                            batch.
                                          minimum: 1
                                          type: integer
                                      type: object
                                    fileIngestionParams:
                                      description: FileIngestionParams configures
                                        file ingestion chunk settings.
                                      properties:
                                        defaultChunkOverlapTokens:
                                          description: DefaultChunkOverlapTokens is
                                            the default overlap between chunks in
                                            tokens.
                                          minimum: 0
                                          type: integer
                                        defaultChunkSizeTokens:
                                          description: DefaultChunkSizeTokens is the
                                            default chunk size in tokens.
                                          minimum: 1
                                          type: integer
                                      type: object
                                    fileSearchParams:
                                      description: FileSearchParams configures file
                                        search output formatting.
                                      properties:
                                        footerTemplate:
                                          description: FooterTemplate is the template
                                            for the footer text after search results.
                                          type: string
                                        headerTemplate:
                                          description: HeaderTemplate is the template
                                            for the header text before search results.
                                          type: string
                                      type: object
                                    rewriteQueryParams:
                                      description: RewriteQueryParams configures query
                                        rewriting/expansion. Nil disables rewriting.
                                      properties:
                                        maxTokens:
                                          description: MaxTokens is the maximum number
                                            of tokens for query expansion responses.
                                          minimum: 1
                                          type: integer
                                        model:
                                          description: Model is the LLM model used
                                            for query rewriting.
                                          properties:
                                            embeddingDimensions:
                                              description: EmbeddingDimensions is
                                                the dimensionality of the embedding
                                                vectors.
                                              minimum: 1
                                              type: integer
                                            modelId:
                                              description: ModelID is the model identifier.
                                              minLength: 1
                                              type: string
                                            providerId:
                                              description: ProviderID is the provider
                                                to use for this model.
                                              minLength: 1
                                              type: string
                                          required:
                                          - modelId
                                          - providerId
                                          type: object
                                        prompt:
                                          description: |-
                                            Prompt is the prompt template for query rewriting.
                                            Use {query} as a placeholder for the original query.
                                          type: string
                                        temperature:
                                          description: |-
                                            Temperature controls randomness in query rewriting (0.0 = deterministic, 1.0 = creative).
                                            Specified as a decimal string (e.g., "0.7").
                                          type: string
                                      type: object
                                  type: object
                                  x-kubernetes-validations:
                                  - message: defaultProviderId must not be empty if
                                      specified
                                    rule: '!has(self.defaultProviderId) || self.defaultProviderId.size()
                                      > 0'
                              type: object
                              x-kubernetes-validations:
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                        type: object
                      remote:
                        description: ToolRuntimeRemoteProviders groups remote tool
                          runtime providers.
                        properties:
                          braveSearch:
                            items:
                              description: BraveSearchProvider configures a remote::brave-search
                                tool runtime provider.
                              properties:
                                apiKey:
                                  description: |-
                                    APIKey is the Brave Search API key.
                                    The Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  properties:
                                    key:
                                      description: Key is the key within the Secret.
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                      type: string
                                    name:
                                      description: Name is the name of the Kubernetes
                                        Secret.
                                      minLength: 1
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                maxResults:
                                  description: MaxResults is the maximum number of
                                    search results to return.
                                  minimum: 1
                                  type: integer
                              required:
                              - apiKey
                              type: object
                              x-kubernetes-validations:
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                          custom:
                            items:
                              description: CustomProvider defines the configuration
                                for a custom provider instance.
                              properties:
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                secretRefs:
                                  additionalProperties:
                                    description: |-
                                      SecretKeyRef references a specific key in a Kubernetes Secret.
                                      The Secret must be in the same namespace as the OGXServer and must have
                                      the label ogx.io/watch: "true" to be detected by the operator's cache.
                                    properties:
                                      key:
                                        description: Key is the key within the Secret.
                                        maxLength: 253
                                        minLength: 1
                                        pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                        type: string
                                      name:
                                        description: Name is the name of the Kubernetes
                                          Secret.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                  description: |-
                                    SecretRefs is a map of named secret references for provider-specific
                                    connection fields (e.g., host, password). Each key becomes the env var
                                    field suffix and maps to config.<key> with env var substitution.
                                    Each Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  minProperties: 1
                                  type: object
                                settings:
                                  description: |-
                                    Settings contains provider-specific configuration merged into the
                                    provider's config section in config.yaml. Passed through as-is
                                    without any secret resolution. Use secretRefs for secret values.
                                  x-kubernetes-preserve-unknown-fields: true
                                type:
                                  description: |-
                                    Type is the provider type, specified with a "remote::" or "inline::"
                                    prefix (e.g., "remote::llama-guard", "inline::my-provider").
                                  minLength: 1
                                  type: string
                              required:
                              - type
                              type: object
                              x-kubernetes-validations:
                              - message: type must have a 'remote::' or 'inline::'
                                  prefix (e.g., 'remote::llama-guard', 'inline::my-provider')
                                rule: self.type.startsWith('remote::') || self.type.startsWith('inline::')
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                          modelContextProtocol:
                            items:
                              description: ModelContextProtocolProvider configures
                                remote::model-context-protocol.
                              properties:
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                              type: object
                              x-kubernetes-validations:
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                          tavilySearch:
                            items:
                              description: TavilySearchProvider configures a remote::tavily-search
                                tool runtime provider.
                              properties:
                                apiKey:
                                  description: |-
                                    APIKey is the Tavily Search API key.
                                    The Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  properties:
                                    key:
                                      description: Key is the key within the Secret.
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                      type: string
                                    name:
                                      description: Name is the name of the Kubernetes
                                        Secret.
                                      minLength: 1
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                maxResults:
                                  description: MaxResults is the maximum number of
                                    search results to return.
                                  minimum: 1
                                  type: integer
                              required:
                              - apiKey
                              type: object
                              x-kubernetes-validations:
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                        type: object
                    type: object
                  vectorIo:
                    description: VectorIOProvidersSpec configures vector I/O providers.
                    properties:
                      inline:
                        description: VectorIOInlineProviders groups inline vector
                          I/O providers.
                        properties:
                          custom:
                            items:
                              description: CustomProvider defines the configuration
                                for a custom provider instance.
                              properties:
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                secretRefs:
                                  additionalProperties:
                                    description: |-
                                      SecretKeyRef references a specific key in a Kubernetes Secret.
                                      The Secret must be in the same namespace as the OGXServer and must have
                                      the label ogx.io/watch: "true" to be detected by the operator's cache.
                                    properties:
                                      key:
                                        description: Key is the key within the Secret.
                                        maxLength: 253
                                        minLength: 1
                                        pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                        type: string
                                      name:
                                        description: Name is the name of the Kubernetes
                                          Secret.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                  description: |-
                                    SecretRefs is a map of named secret references for provider-specific
                                    connection fields (e.g., host, password). Each key becomes the env var
                                    field suffix and maps to config.<key> with env var substitution.
                                    Each Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  minProperties: 1
                                  type: object
                                settings:
                                  description: |-
                                    Settings contains provider-specific configuration merged into the
                                    provider's config section in config.yaml. Passed through as-is
                                    without any secret resolution. Use secretRefs for secret values.
                                  x-kubernetes-preserve-unknown-fields: true
                                type:
                                  description: |-
                                    Type is the provider type, specified with a "remote::" or "inline::"
                                    prefix (e.g., "remote::llama-guard", "inline::my-provider").
                                  minLength: 1
                                  type: string
                              required:
                              - type
                              type: object
                              x-kubernetes-validations:
                              - message: type must have a 'remote::' or 'inline::'
                                  prefix (e.g., 'remote::llama-guard', 'inline::my-provider')
                                rule: self.type.startsWith('remote::') || self.type.startsWith('inline::')
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                        type: object
                      remote:
                        description: VectorIORemoteProviders groups remote vector
                          I/O providers.
                        properties:
                          custom:
                            items:
                              description: CustomProvider defines the configuration
                                for a custom provider instance.
                              properties:
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                secretRefs:
                                  additionalProperties:
                                    description: |-
                                      SecretKeyRef references a specific key in a Kubernetes Secret.
                                      The Secret must be in the same namespace as the OGXServer and must have
                                      the label ogx.io/watch: "true" to be detected by the operator's cache.
                                    properties:
                                      key:
                                        description: Key is the key within the Secret.
                                        maxLength: 253
                                        minLength: 1
                                        pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                        type: string
                                      name:
                                        description: Name is the name of the Kubernetes
                                          Secret.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                  description: |-
                                    SecretRefs is a map of named secret references for provider-specific
                                    connection fields (e.g., host, password). Each key becomes the env var
                                    field suffix and maps to config.<key> with env var substitution.
                                    Each Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  minProperties: 1
                                  type: object
                                settings:
                                  description: |-
                                    Settings contains provider-specific configuration merged into the
                                    provider's config section in config.yaml. Passed through as-is
                                    without any secret resolution. Use secretRefs for secret values.
                                  x-kubernetes-preserve-unknown-fields: true
                                type:
                                  description: |-
                                    Type is the provider type, specified with a "remote::" or "inline::"
                                    prefix (e.g., "remote::llama-guard", "inline::my-provider").
                                  minLength: 1
                                  type: string
                              required:
                              - type
                              type: object
                              x-kubernetes-validations:
                              - message: type must have a 'remote::' or 'inline::'
                                  prefix (e.g., 'remote::llama-guard', 'inline::my-provider')
                                rule: self.type.startsWith('remote::') || self.type.startsWith('inline::')
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                          milvus:
                            items:
                              description: MilvusProvider configures a remote::milvus
                                vector I/O provider instance.
                              properties:
                                consistencyLevel:
                                  description: ConsistencyLevel is the consistency
                                    level of the Milvus server.
                                  type: string
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                token:
                                  description: |-
                                    Token is the authentication token for the Milvus server.
                                    The Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  properties:
                                    key:
                                      description: Key is the key within the Secret.
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                      type: string
                                    name:
                                      description: Name is the name of the Kubernetes
                                        Secret.
                                      minLength: 1
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                uri:
                                  description: URI is the URI of the Milvus server.
                                  minLength: 1
                                  type: string
                              required:
                              - uri
                              type: object
                              x-kubernetes-validations:
                              - message: consistencyLevel must not be empty if specified
                                rule: '!has(self.consistencyLevel) || self.consistencyLevel.size()
                                  > 0'
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                          pgvector:
                            items:
                              description: PgvectorProvider configures a remote::pgvector
                                vector I/O provider instance.
                              properties:
                                db:
                                  description: DB is the PostgreSQL database name.
                                  type: string
                                distanceMetric:
                                  description: DistanceMetric is the distance metric
                                    used for vector search.
                                  enum:
                                  - COSINE
                                  - L2
                                  - L1
                                  - INNER_PRODUCT
                                  type: string
                                host:
                                  description: Host is the PostgreSQL server hostname.
                                  type: string
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                password:
                                  description: |-
                                    Password is the PostgreSQL password.
                                    The Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  properties:
                                    key:
                                      description: Key is the key within the Secret.
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                      type: string
                                    name:
                                      description: Name is the name of the Kubernetes
                                        Secret.
                                      minLength: 1
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                port:
                                  description: Port is the PostgreSQL server port.
                                  maximum: 65535
                                  minimum: 1
                                  type: integer
                                user:
                                  description: User is the PostgreSQL username.
                                  type: string
                                vectorIndex:
                                  description: |-
                                    VectorIndex configures the vector index strategy for
                                    Approximate Nearest Neighbor (ANN) search.
                                  properties:
                                    hnsw:
                                      description: HNSW configures an HNSW index.
                                      properties:
                                        efConstruction:
                                          description: EfConstruction controls the
                                            index build-time accuracy/speed tradeoff.
                                          minimum: 1
                                          type: integer
                                        efSearch:
                                          description: EfSearch controls the query-time
                                            accuracy/speed tradeoff.
                                          minimum: 1
                                          type: integer
                                        m:
                                          description: M is the maximum number of
                                            connections per element in the HNSW graph.
                                          minimum: 1
                                          type: integer
                                      type: object
                                    ivfFlat:
                                      description: IVFFlat configures an IVFFlat index.
                                      properties:
                                        nlist:
                                          description: Nlist is the number of inverted
                                            lists (clusters).
                                          minimum: 1
                                          type: integer
                                        nprobe:
                                          description: Nprobe is the number of clusters
                                            to search at query time.
                                          minimum: 1
                                          type: integer
                                      type: object
                                  type: object
                                  x-kubernetes-validations:
                                  - message: one of hnsw or ivfFlat must be specified
                                    rule: has(self.hnsw) || has(self.ivfFlat)
                                  - message: only one of hnsw or ivfFlat can be specified
                                    rule: '!(has(self.hnsw) && has(self.ivfFlat))'
                              required:
                              - password
                              type: object
                              x-kubernetes-validations:
                              - message: host must not be empty if specified
                                rule: '!has(self.host) || self.host.size() > 0'
                              - message: db must not be empty if specified
                                rule: '!has(self.db) || self.db.size() > 0'
                              - message: user must not be empty if specified
                                rule: '!has(self.user) || self.user.size() > 0'
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                          qdrant:
                            items:
                              description: QdrantProvider configures a remote::qdrant
                                vector I/O provider instance.
                              properties:
                                apiKey:
                                  description: |-
                                    APIKey is the authentication key for the Qdrant server.
                                    The Secret must be in the same namespace as the OGXServer
                                    and must have the label ogx.io/watch: "true".
                                  properties:
                                    key:
                                      description: Key is the key within the Secret.
                                      maxLength: 253
                                      minLength: 1
                                      pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                                      type: string
                                    name:
                                      description: Name is the name of the Kubernetes
                                        Secret.
                                      minLength: 1
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                                grpcPort:
                                  description: GRPCPort is the gRPC port of the Qdrant
                                    server.
                                  maximum: 65535
                                  minimum: 1
                                  type: integer
                                host:
                                  description: Host is the hostname of the Qdrant
                                    server.
                                  type: string
                                https:
                                  description: HTTPS controls whether to use HTTPS
                                    for the connection.
                                  type: boolean
                                id:
                                  description: |-
                                    ID is a unique provider identifier. Derived from the provider
                                    type when omitted. Must be unique across all providers.
                                  type: string
                                location:
                                  description: Location is the Qdrant server location
                                    identifier.
                                  type: string
                                port:
                                  description: Port is the REST API port of the Qdrant
                                    server.
                                  maximum: 65535
                                  minimum: 1
                                  type: integer
                                preferGrpc:
                                  description: PreferGRPC controls whether to prefer
                                    gRPC over REST for communication.
                                  type: boolean
                                prefix:
                                  description: Prefix is the URL path prefix for the
                                    Qdrant server.
                                  type: string
                                timeout:
                                  description: Timeout is the connection timeout in
                                    seconds.
                                  minimum: 1
                                  type: integer
                                url:
                                  description: URL is the URL of the Qdrant server.
                                  type: string
                              type: object
                              x-kubernetes-validations:
                              - message: at least one of url or host must be specified
                                rule: has(self.url) || has(self.host)
                              - message: url must not be empty if specified
                                rule: '!has(self.url) || self.url.size() > 0'
                              - message: host must not be empty if specified
                                rule: '!has(self.host) || self.host.size() > 0'
                              - message: location must not be empty if specified
                                rule: '!has(self.location) || self.location.size()
                                  > 0'
                              - message: prefix must not be empty if specified
                                rule: '!has(self.prefix) || self.prefix.size() > 0'
                              - message: id must not be empty if specified
                                rule: '!has(self.id) || self.id.size() > 0'
                            maxItems: 100
                            minItems: 1
                            type: array
                        type: object
                    type: object
                type: object
              registryRefreshIntervalSeconds:
                description: |-
                  RegistryRefreshIntervalSeconds configures how often the server refreshes
                  its model registry, in seconds. When omitted, the server's built-in
                  default is used.
                format: int32
                minimum: 1
                type: integer
              resources:
                description: |-
                  Resources declares models to register.
                  Mutually exclusive with overrideConfig.
                properties:
                  models:
                    description: Models to register with inference providers.
                    items:
                      description: ModelConfig defines a model registration with optional
                        provider assignment and metadata.
                      properties:
                        contextLength:
                          description: ContextLength is the model context window size.
                          type: integer
                        modelType:
                          description: ModelType is the model type classification.
                          type: string
                        name:
                          description: Name is the model identifier (e.g., "llama3.2-8b").
                          minLength: 1
                          type: string
                        provider:
                          description: |-
                            Provider is the ID of the provider to register this model with.
                            Defaults to the first inference provider when omitted.
                          type: string
                        quantization:
                          description: Quantization is the quantization method.
                          type: string
                      required:
                      - name
                      type: object
                      x-kubernetes-validations:
                      - message: provider must not be empty if specified
                        rule: '!has(self.provider) || self.provider.size() > 0'
                      - message: modelType must not be empty if specified
                        rule: '!has(self.modelType) || self.modelType.size() > 0'
                      - message: quantization must not be empty if specified
                        rule: '!has(self.quantization) || self.quantization.size()
                          > 0'
                    minItems: 1
                    type: array
                type: object
              storage:
                description: |-
                  Storage configures state storage backends (KV and SQL).
                  Mutually exclusive with overrideConfig.
                properties:
                  kv:
                    description: KV configures key-value storage.
                    properties:
                      endpoint:
                        description: Endpoint is the Redis endpoint URL. Required
                          when type is "redis".
                        type: string
                      password:
                        description: |-
                          Password references a Secret for Redis authentication.
                          The Secret must be in the same namespace as the OGXServer
                          and must have the label ogx.io/watch: "true".
                        properties:
                          key:
                            description: Key is the key within the Secret.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                            type: string
                          name:
                            description: Name is the name of the Kubernetes Secret.
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      type:
                        default: sqlite
                        description: Type is the KV storage backend type.
                        enum:
                        - sqlite
                        - redis
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: endpoint is required when type is redis
                      rule: self.type != 'redis' || has(self.endpoint)
                    - message: endpoint is only valid when type is redis
                      rule: '!has(self.endpoint) || self.type == ''redis'''
                    - message: password is only valid when type is redis
                      rule: '!has(self.password) || self.type == ''redis'''
                  sql:
                    description: SQL configures SQL storage.
                    properties:
                      connectionString:
                        description: |-
                          ConnectionString references a Secret containing the database connection string.
                          Required when type is "postgres".
                          The Secret must be in the same namespace as the OGXServer
                          and must have the label ogx.io/watch: "true".
                        properties:
                          key:
                            description: Key is the key within the Secret.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                            type: string
                          name:
                            description: Name is the name of the Kubernetes Secret.
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      type:
                        default: sqlite
                        description: Type is the SQL storage backend type.
                        enum:
                        - sqlite
                        - postgres
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: connectionString is required when type is postgres
                      rule: self.type != 'postgres' || has(self.connectionString)
                    - message: connectionString is only valid when type is postgres
                      rule: '!has(self.connectionString) || self.type == ''postgres'''
                type: object
              tls:
                description: |-
                  TLS configures outbound TLS trust anchors and client identity for
                  connections to providers and backends.
                properties:
                  identity:
                    description: |-
                      Identity configures client certificate and key for mTLS authentication
                      with providers and backends.
                    properties:
                      cert:
                        description: |-
                          Cert references a ConfigMap key containing the PEM-encoded TLS client certificate.
                          The ConfigMap must be in the same namespace as the OGXServer
                          and must have the label ogx.io/watch: "true".
                        properties:
                          key:
                            description: Key is the key within the ConfigMap.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                            type: string
                          name:
                            description: Name is the name of the ConfigMap.
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      key:
                        description: |-
                          Key references a Secret key containing the PEM-encoded TLS client private key.
                          The Secret must be in the same namespace as the OGXServer
                          and must have the label ogx.io/watch: "true".
                        properties:
                          key:
                            description: Key is the key within the Secret.
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                            type: string
                          name:
                            description: Name is the name of the Kubernetes Secret.
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    required:
                    - cert
                    - key
                    type: object
                  trust:
                    description: |-
                      Trust configures CA certificates for verifying outbound TLS connections
                      to providers and backends.
                    properties:
                      caCertificates:
                        description: |-
                          CACertificates lists ConfigMap keys containing PEM-encoded CA certificates.
                          All certificates are concatenated into a single trust bundle.
                          Referenced ConfigMaps must be in the same namespace as the OGXServer
                          and must have the label ogx.io/watch: "true".
                        items:
                          description: |-
                            ConfigMapKeyRef references a key within a ConfigMap.
                            The ConfigMap must be in the same namespace as the OGXServer and must have
                            the label ogx.io/watch: "true" to be detected by the operator's cache.
                          properties:
                            key:
                              description: Key is the key within the ConfigMap.
                              maxLength: 253
                              minLength: 1
                              pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-_.]*[a-zA-Z0-9])?$
                              type: string
                            name:
                              description: Name is the name of the ConfigMap.
                              minLength: 1
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        minItems: 1
                        type: array
                    type: object
                type: object
              workload:
                description: Workload consolidates Kubernetes deployment settings.
                properties:
                  autoscaling:
                    description: Autoscaling configures HPA for the server pods.
                    properties:
                      maxReplicas:
                        description: MaxReplicas is the upper bound replica count.
                        format: int32
                        minimum: 1
                        type: integer
                      minReplicas:
                        description: MinReplicas is the lower bound replica count.
                        format: int32
                        minimum: 1
                        type: integer
                      targetCPUUtilizationPercentage:
                        description: TargetCPUUtilizationPercentage configures CPU-based
                          scaling.
                        format: int32
                        maximum: 100
                        minimum: 1
                        type: integer
                      targetMemoryUtilizationPercentage:
                        description: TargetMemoryUtilizationPercentage configures
                          memory-based scaling.
                        format: int32
                        maximum: 100
                        minimum: 1
                        type: integer
                    required:
                    - maxReplicas
                    type: object
                    x-kubernetes-validations:
                    - message: maxReplicas must be greater than or equal to minReplicas
                      rule: '!has(self.minReplicas) || self.maxReplicas >= self.minReplicas'
                  overrides:
                    description: Overrides allows pod-level customization.
                    properties:
                      args:
                        description: Args overrides the container arguments.
                        items:
                          minLength: 1
                          type: string
                        minItems: 1
                        type: array
                      command:
                        description: Command overrides the container command.
                        items:
                          minLength: 1
                          type: string
                        minItems: 1
                        type: array
                      env:
                        description: Env specifies additional environment variables.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        minItems: 1
                        type: array
                      serviceAccountName:
                        description: ServiceAccountName specifies a custom ServiceAccount.
                        type: string
                      volumeMounts:
                        description: VolumeMounts adds additional volume mounts to
                          the container.
                        items:
                          description: VolumeMount describes a mounting of a Volume
                            within a container.
                          properties:
                            mountPath:
                              description: |-
                                Path within the container at which the volume should be mounted.  Must
                                not contain ':'.
                              type: string
                            mountPropagation:
                              description: |-
                                mountPropagation determines how mounts are propagated from the host
                                to container and the other way around.
                                When not set, MountPropagationNone is used.
                                This field is beta in 1.10.
                                When RecursiveReadOnly is set to IfPossible or to Enabled, MountPropagation must be None or unspecified
                                (which defaults to None).
                              type: string
                            name:
                              description: This must match the Name of a Volume.
                              type: string
                            readOnly:
                              description: |-
                                Mounted read-only if true, read-write otherwise (false or unspecified).
                                Defaults to false.
                              type: boolean
                            recursiveReadOnly:
                              description: |-
                                RecursiveReadOnly specifies whether read-only mounts should be handled
                                recursively.

                                If ReadOnly is false, this field has no meaning and must be unspecified.

                                If ReadOnly is true, and this field is set to Disabled, the mount is not made
                                recursively read-only.  If this field is set to IfPossible, the mount is made
                                recursively read-only, if it is supported by the container runtime.  If this
                                field is set to Enabled, the mount is made recursively read-only if it is
                                supported by the container runtime, otherwise the pod will not be started and
                                an error will be generated to indicate the reason.

                                If this field is set to IfPossible or Enabled, MountPropagation must be set to
                                None (or be unspecified, which defaults to None).

                                If this field is not specified, it is treated as an equivalent of Disabled.
                              type: string
                            subPath:
                              description: |-
                                Path within the volume from which the container's volume should be mounted.
                                Defaults to "" (volume's root).
                              type: string
                            subPathExpr:
                              description: |-
                                Expanded path within the volume from which the container's volume should be mounted.
                                Behaves similarly to SubPath but environment variable references $(VAR_NAME) are expanded using the container's environment.
                                Defaults to "" (volume's root).
                                SubPathExpr and SubPath are mutually exclusive.
                              type: string
                          required:
                          - mountPath
                          - name
                          type: object
                        minItems: 1
                        type: array
                      volumes:
                        description: Volumes adds additional volumes to the Pod.
                        items:
                          description: Volume represents a named volume in a pod that
                            may be accessed by any container in the pod.
                          properties:
                            awsElasticBlockStore:
                              description: |-
                                awsElasticBlockStore represents an AWS Disk resource that is attached to a
                                kubelet's host machine and then exposed to the pod.
                                Deprecated: AWSElasticBlockStore is deprecated. All operations for the in-tree
                                awsElasticBlockStore type are redirected to the ebs.csi.aws.com CSI driver.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore
                              properties:
                                fsType:
                                  description: |-
                                    fsType is the filesystem type of the volume that you want to mount.
                                    Tip: Ensure that the filesystem type is supported by the host operating system.
                                    Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                                    More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore
                                  type: string
                                partition:
                                  description: |-
                                    partition is the partition in the volume that you want to mount.
                                    If omitted, the default is to mount by volume name.
                                    Examples: For volume /dev/sda1, you specify the partition as "1".
                                    Similarly, the volume partition for /dev/sda is "0" (or you can leave the property empty).
                                  format: int32
                                  type: integer
                                readOnly:
                                  description: |-
                                    readOnly value true will force the readOnly setting in VolumeMounts.
                                    More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore
                                  type: boolean
                                volumeID:
                                  description: |-
                                    volumeID is unique ID of the persistent disk resource in AWS (Amazon EBS volume).
                                    More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore
                                  type: string
                              required:
                              - volumeID
                              type: object
                            azureDisk:
                              description: |-
                                azureDisk represents an Azure Data Disk mount on the host and bind mount to the pod.
                                Deprecated: AzureDisk is deprecated. All operations for the in-tree azureDisk type
                                are redirected to the disk.csi.azure.com CSI driver.
                              properties:
                                cachingMode:
                                  description: 'cachingMode is the Host Caching mode:
                                    None, Read Only, Read Write.'
                                  type: string
                                diskName:
                                  description: diskName is the Name of the data disk
                                    in the blob storage
                                  type: string
                                diskURI:
                                  description: diskURI is the URI of data disk in
                                    the blob storage
                                  type: string
                                fsType:
                                  default: ext4
                                  description: |-
                                    fsType is Filesystem type to mount.
                                    Must be a filesystem type supported by the host operating system.
                                    Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                                  type: string
                                kind:
                                  description: 'kind expected values are Shared: multiple
                                    blob disks per storage account  Dedicated: single
                                    blob disk per storage account  Managed: azure
                                    managed data disk (only in managed availability
                                    set). defaults to shared'
                                  type: string
                                readOnly:
                                  default: false
                                  description: |-
                                    readOnly Defaults to false (read/write). ReadOnly here will force
                                    the ReadOnly setting in VolumeMounts.
                                  type: boolean
                              required:
                              - diskName
                              - diskURI
                              type: object
                            azureFile:
                              description: |-
                                azureFile represents an Azure File Service mount on the host and bind mount to the pod.
                                Deprecated: AzureFile is deprecated. All operations for the in-tree azureFile type
                                are redirected to the file.csi.azure.com CSI driver.
                              properties:
                                readOnly:
                                  description: |-
                                    readOnly defaults to false (read/write). ReadOnly here will force
                                    the ReadOnly setting in VolumeMounts.
                                  type: boolean
                                secretName:
                                  description: secretName is the  name of secret that
                                    contains Azure Storage Account Name and Key
                                  type: string
                                shareName:
                                  description: shareName is the azure share Name
                                  type: string
                              required:
                              - secretName
                              - shareName
                              type: object
                            cephfs:
                              description: |-
                                cephFS represents a Ceph FS mount on the host that shares a pod's lifetime.
                                Deprecated: CephFS is deprecated and the in-tree cephfs type is no longer supported.
                              properties:
                                monitors:
                                  description: |-
                                    monitors is Required: Monitors is a collection of Ceph monitors
                                    More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                path:
                                  description: 'path is Optional: Used as the mounted
                                    root, rather than the full Ceph tree, default
                                    is /'
                                  type: string
                                readOnly:
                                  description: |-
                                    readOnly is Optional: Defaults to false (read/write). ReadOnly here will force
                                    the ReadOnly setting in VolumeMounts.
                                    More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it
                                  type: boolean
                                secretFile:
                                  description: |-
                                    secretFile is Optional: SecretFile is the path to key ring for User, default is /etc/ceph/user.secret
                                    More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it
                                  type: string
                                secretRef:
                                  description: |-
                                    secretRef is Optional: SecretRef is reference to the authentication secret for User, default is empty.
                                    More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it
                                  properties:
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                  type: object
                                  x-kubernetes-map-type: atomic
                                user:
                                  description: |-
                                    user is optional: User is the rados user name, default is admin
                                    More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it
                                  type: string
                              required:
                              - monitors
                              type: object
                            cinder:
                              description: |-
                                cinder represents a cinder volume attached and mounted on kubelets host machine.
                                Deprecated: Cinder is deprecated. All operations for the in-tree cinder type
                                are redirected to the cinder.csi.openstack.org CSI driver.
                                More info: https://examples.k8s.io/mysql-cinder-pd/README.md
                              properties:
                                fsType:
                                  description: |-
                                    fsType is the filesystem type to mount.
                                    Must be a filesystem type supported by the host operating system.
                                    Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                                    More info: https://examples.k8s.io/mysql-cinder-pd/README.md
                                  type: string
                                readOnly:
                                  description: |-
                                    readOnly defaults to false (read/write). ReadOnly here will force
                                    the ReadOnly setting in VolumeMounts.
                                    More info: https://examples.k8s.io/mysql-cinder-pd/README.md
                                  type: boolean
                                secretRef:
                                  description: |-
                                    secretRef is optional: points to a secret object containing parameters used to connect
                                    to OpenStack.
                                  properties:
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                  type: object
                                  x-kubernetes-map-type: atomic
                                volumeID:
                                  description: |-
                                    volumeID used to identify the volume in cinder.
                                    More info: https://examples.k8s.io/mysql-cinder-pd/README.md
                                  type: string
                              required:
                              - volumeID
                              type: object
                            configMap:
                              description: configMap represents a configMap that should
                                populate this volume
                              properties:
                                defaultMode:
                                  description: |-
                                    defaultMode is optional: mode bits used to set permissions on created files by default.
                                    Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
                                    YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
                                    Defaults to 0644.
                                    Directories within the path are not affected by this setting.
                                    This might be in conflict with other options that affect the file
                                    mode, like fsGroup, and the result can be other mode bits set.
                                  format: int32
                                  type: integer
                                items:
                                  description: |-
                                    items if unspecified, each key-value pair in the Data field of the referenced
                                    ConfigMap will be projected into the volume as a file whose name is the
                                    key and content is the value. If specified, the listed keys will be
                                    projected into the specified paths, and unlisted keys will not be
                                    present. If a key is specified which is not present in the ConfigMap,
                                    the volume setup will error unless it is marked optional. Paths must be
                                    relative and may not contain the '..' path or start with '..'.
                                  items:
                                    description: Maps a string key to a path within
                                      a volume.
                                    properties:
                                      key:
                                        description: key is the key to project.
                                        type: string
                                      mode:
                                        description: |-
                                          mode is Optional: mode bits used to set permissions on this file.
                                          Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
                                          YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
                                          If not specified, the volume defaultMode will be used.
                                          This might be in conflict with other options that affect the file
                                          mode, like fsGroup, and the result can be other mode bits set.
                                        format: int32
                                        type: integer
                                      path:
                                        description: |-
                                          path is the relative path of the file to map the key to.
                                          May not be an absolute path.
                                          May not contain the path element '..'.
                                          May not start with the string '..'.
                                        type: string
                                    required:
                                    - key
                                    - path
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: optional specify whether the ConfigMap
                                    or its keys must be defined
                                  type: boolean
                              type: object
                              x-kubernetes-map-type: atomic
                            csi:
                              description: csi (Container Storage Interface) represents
                                ephemeral storage that is handled by certain external
                                CSI drivers.
                              properties:
                                driver:
                                  description: |-
                                    driver is the name of the CSI driver that handles this volume.
                                    Consult with your admin for the correct name as registered in the cluster.
                                  type: string
                                fsType:
                                  description: |-
                                    fsType to mount. Ex. "ext4", "xfs", "ntfs".
                                    If not provided, the empty value is passed to the associated CSI driver
                                    which will determine the default filesystem to apply.
                                  type: string
                                nodePublishSecretRef:
                                  description: |-
                                    nodePublishSecretRef is a reference to the secret object containing
                                    sensitive information to pass to the CSI driver to complete the CSI
                                    NodePublishVolume and NodeUnpublishVolume calls.
                                    This field is optional, and  may be empty if no secret is required. If the
                                    secret object contains more than one secret, all secret references are passed.
                                  properties:
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                  type: object
                                  x-kubernetes-map-type: atomic
                                readOnly:
                                  description: |-
                                    readOnly specifies a read-only configuration for the volume.
                                    Defaults to false (read/write).
                                  type: boolean
                                volumeAttributes:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    volumeAttributes stores driver-specific properties that are passed to the CSI
                                    driver. Consult your driver's documentation for supported values.
                                  type: object
                              required:
                              - driver
                              type: object
                            downwardAPI:
                              description: downwardAPI represents downward API about
                                the pod that should populate this volume
                              properties:
                                defaultMode:
                                  description: |-
                                    Optional: mode bits to use on created files by default. Must be a
                                    Optional: mode bits used to set permissions on created files by default.
                                    Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
                                    YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
                                    Defaults to 0644.
                                    Directories within the path are not affected by this setting.
                                    This might be in conflict with other options that affect the file
                                    mode, like fsGroup, and the result can be other mode bits set.
                                  format: int32
                                  type: integer
                                items:
                                  description: Items is a list of downward API volume
                                    file
                                  items:
                                    description: DownwardAPIVolumeFile represents
                                      information to create the file containing the
                                      pod field
                                    properties:
                                      fieldRef:
                                        description: 'Required: Selects a field of
                                          the pod: only annotations, labels, name,
                                          namespace and uid are supported.'
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      mode:
                                        description: |-
                                          Optional: mode bits used to set permissions on this file, must be an octal value
                                          between 0000 and 0777 or a decimal value between 0 and 511.
                                          YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
                                          If not specified, the volume defaultMode will be used.
                                          This might be in conflict with other options that affect the file
                                          mode, like fsGroup, and the result can be other mode bits set.
                                        format: int32
                                        type: integer
                                      path:
                                        description: 'Required: Path is  the relative
                                          path name of the file to be created. Must
                                          not be absolute or contain the ''..'' path.
                                          Must be utf-8 encoded. The first item of
                                          the relative path must not start with ''..'''
                                        type: string
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, requests.cpu and requests.memory) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    required:
                                    - path
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                              type: object
                            emptyDir:
                              description: |-
                                emptyDir represents a temporary directory that shares a pod's lifetime.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
                              properties:
                                medium:
                                  description: |-
                                    medium represents what type of storage medium should back this directory.
                                    The default is "" which means to use the node's default medium.
                                    Must be an empty string (default) or Memory.
                                    More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
                                  type: string
                                sizeLimit:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: |-
                                    sizeLimit is the total amount of local storage required for this EmptyDir volume.
                                    The size limit is also applicable for memory medium.
                                    The maximum usage on memory medium EmptyDir would be the minimum value between
                                    the SizeLimit specified here and the sum of memory limits of all containers in a pod.
                                    The default is nil which means that the limit is undefined.
                                    More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                              type: object
                            ephemeral:
                              description: |-
                                ephemeral represents a volume that is handled by a cluster storage driver.
                                The volume's lifecycle is tied to the pod that defines it - it will be created before the pod starts,
                                and deleted when the pod is removed.

                                Use this if:
                                a) the volume is only needed while the pod runs,
                                b) features of normal volumes like restoring from snapshot or capacity
                                   tracking are needed,
                                c) the storage driver is specified through a storage class, and
                                d) the storage driver supports dynamic volume provisioning through
                                   a PersistentVolumeClaim (see EphemeralVolumeSource for more
                                   information on the connection between this volume type
                                   and PersistentVolumeClaim).

                                Use PersistentVolumeClaim or one of the vendor-specific
                                APIs for volumes that persist for longer than the lifecycle
                                of an individual pod.

                                Use CSI for light-weight local ephemeral volumes if the CSI driver is meant to
                                be used that way - see the documentation of the driver for
                                more information.

                                A pod can use both types of ephemeral volumes and
                                persistent volumes at the same time.
                              properties:
                                volumeClaimTemplate:
                                  description: |-
                                    Will be used to create a stand-alone PVC to provision the volume.
                                    The pod in which this EphemeralVolumeSource is embedded will be the
                                    owner of the PVC, i.e. the PVC will be deleted together with the
                                    pod.  The name of the PVC will be `<pod name>-<volume name>` where
                                    `<volume name>` is the name from the `PodSpec.Volumes` array
                                    entry. Pod validation will reject the pod if the concatenated name
                                    is not valid for a PVC (for example, too long).

                                    An existing PVC with that name that is not owned by the pod
                                    will *not* be used for the pod to avoid using an unrelated
                                    volume by mistake. Starting the pod is then blocked until
                                    the unrelated PVC is removed. If such a pre-created PVC is
                                    meant to be used by the pod, the PVC has to updated with an
                                    owner reference to the pod once the pod exists. Normally
                                    this should not be necessary, but it may be useful when
                                    manually reconstructing a broken cluster.

                                    This field is read-only and no changes will be made by Kubernetes
                                    to the PVC after it has been created.

                                    Required, must not be nil.
                                  properties:
                                    metadata:
                                      description: |-
                                        May contain labels and annotations that will be copied into the PVC
                                        when creating it. No other fields are allowed and will be rejected during
                                        validation.
                                      type: object
                                    spec:
                                      description: |-
                                        The specification for the PersistentVolumeClaim. The entire content is
                                        copied unchanged into the PVC that gets created from this
                                        template. The same fields as in a PersistentVolumeClaim
                                        are also valid here.
                                      properties:
                                        accessModes:
                                          description: |-
                                            accessModes contains the desired access modes the volume should have.
                                            More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1
                                          items:
                                            type: string
                                          type: array
                                          x-kubernetes-list-type: atomic
                                        dataSource:
                                          description: |-
                                            dataSource field can be used to specify either:
                                            * An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)
                                            * An existing PVC (PersistentVolumeClaim)
                                            If the provisioner or an external controller can support the specified data source,
                                            it will create a new volume based on the contents of the specified data source.
                                            When the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,
                                            and dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.
                                            If the namespace is specified, then dataSourceRef will not be copied to dataSource.
                                          properties:
                                            apiGroup:
                                              description: |-
                                                APIGroup is the group for the resource being referenced.
                                                If APIGroup is not specified, the specified Kind must be in the core API group.
                                                For any other third-party types, APIGroup is required.
                                              type: string
                                            kind:
                                              description: Kind is the type of resource
                                                being referenced
                                              type: string
                                            name:
                                              description: Name is the name of resource
                                                being referenced
                                              type: string
                                          required:
                                          - kind
                                          - name
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        dataSourceRef:
                                          description: |-
                                            dataSourceRef specifies the object from which to populate the volume with data, if a non-empty
                                            volume is desired. This may be any object from a non-empty API group (non
                                            core object) or a PersistentVolumeClaim object.
                                            When this field is specified, volume binding will only succeed if the type of
                                            the specified object matches some installed volume populator or dynamic
                                            provisioner.
                                            This field will replace the functionality of the dataSource field and as such
                                            if both fields are non-empty, they must have the same value. For backwards
                                            compatibility, when namespace isn't specified in dataSourceRef,
                                            both fields (dataSource and dataSourceRef) will be set to the same
                                            value automatically if one of them is empty and the other is non-empty.
                                            When namespace is specified in dataSourceRef,
                                            dataSource isn't set to the same value and must be empty.
                                            There are three important differences between dataSource and dataSourceRef:
                                            * While dataSource only allows two specific types of objects, dataSourceRef
                                              allows any non-core object, as well as PersistentVolumeClaim objects.
                                            * While dataSource ignores disallowed values (dropping them), dataSourceRef
                                              preserves all values, and generates an error if a disallowed value is
                                              specified.
                                            * While dataSource only allows local objects, dataSourceRef allows objects
                                              in any namespaces.
                                            (Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.
                                            (Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
                                          properties:
                                            apiGroup:
                                              description: |-
                                                APIGroup is the group for the resource being referenced.
                                                If APIGroup is not specified, the specified Kind must be in the core API group.
                                                For any other third-party types, APIGroup is required.
                                              type: string
                                            kind:
                                              description: Kind is the type of resource
                                                being referenced
                                              type: string
                                            name:
                                              description: Name is the name of resource
                                                being referenced
                                              type: string
                                            namespace:
                                              description: |-
                                                Namespace is the namespace of resource being referenced
                                                Note that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.
                                                (Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
                                              type: string
                                          required:
                                          - kind
                                          - name
                                          type: object
                                        resources:
                                          description: |-
                                            resources represents the minimum resources the volume should have.
                                            Users are allowed to specify resource requirements
                                            that are lower than previous value but must still be higher than capacity recorded in the
                                            status field of the claim.
                                            More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources
                                          properties:
                                            limits:
                                              additionalProperties:
                                                anyOf:
                                                - type: integer
                                                - type: string
                                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                                x-kubernetes-int-or-string: true
                                              description: |-
                                                Limits describes the maximum amount of compute resources allowed.
                                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                              type: object
                                            requests:
                                              additionalProperties:
                                                anyOf:
                                                - type: integer
                                                - type: string
                                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                                x-kubernetes-int-or-string: true
                                              description: |-
                                                Requests describes the minimum amount of compute resources required.
                                                If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                                otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                              type: object
                                          type: object
                                        selector:
                                          description: selector is a label query over
                                            volumes to consider for binding.
                                          properties:
                                            matchExpressions:
                                              description: matchExpressions is a list
                                                of label selector requirements. The
                                                requirements are ANDed.
                                              items:
                                                description: |-
                                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                                  relates the key and values.
                                                properties:
                                                  key:
                                                    description: key is the label
                                                      key that the selector applies
                                                      to.
                                                    type: string
                                                  operator:
                                                    description: |-
                                                      operator represents a key's relationship to a set of values.
                                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                                    type: string
                                                  values:
                                                    description: |-
                                                      values is an array of string values. If the operator is In or NotIn,
                                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                      the values array must be empty. This array is replaced during a strategic
                                                      merge patch.
                                                    items:
                                                      type: string
                                                    type: array
                                                    x-kubernetes-list-type: atomic
                                                required:
                                                - key
                                                - operator
                                                type: object
                                              type: array
                                              x-kubernetes-list-type: atomic
                                            matchLabels:
                                              additionalProperties:
                                                type: string
                                              description: |-
                                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                                              type: object
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        storageClassName:
                                          description: |-
                                            storageClassName is the name of the StorageClass required by the claim.
                                            More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1
                                          type: string
                                        volumeAttributesClassName:
                                          description: |-
                                            volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.
                                            If specified, the CSI driver will create or update the volume with the attributes defined
                                            in the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,
                                            it can be changed after the claim is created. An empty string or nil value indicates that no
                                            VolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,
                                            this field can be reset to its previous value (including nil) to cancel the modification.
                                            If the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be
                                            set to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource
                                            exists.
                                            More info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/
                                          type: string
                                        volumeMode:
                                          description: |-
                                            volumeMode defines what type of volume is required by the claim.
                                            Value of Filesystem is implied when not included in claim spec.
                                          type: string
                                        volumeName:
                                          description: volumeName is the binding reference
                                            to the PersistentVolume backing this claim.
                                          type: string
                                      type: object
                                  required:
                                  - spec
                                  type: object
                              type: object
                            fc:
                              description: fc represents a Fibre Channel resource
                                that is attached to a kubelet's host machine and then
                                exposed to the pod.
                              properties:
                                fsType:
                                  description: |-
                                    fsType is the filesystem type to mount.
                                    Must be a filesystem type supported by the host operating system.
                                    Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                                  type: string
                                lun:
                                  description: 'lun is Optional: FC target lun number'
                                  format: int32
                                  type: integer
                                readOnly:
                                  description: |-
                                    readOnly is Optional: Defaults to false (read/write). ReadOnly here will force
                                    the ReadOnly setting in VolumeMounts.
                                  type: boolean
                                targetWWNs:
                                  description: 'targetWWNs is Optional: FC target
                                    worldwide names (WWNs)'
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                wwids:
                                  description: |-
                                    wwids Optional: FC volume world wide identifiers (wwids)
                                    Either wwids or combination of targetWWNs and lun must be set, but not both simultaneously.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                              type: object
                            flexVolume:
                              description: |-
                                flexVolume represents a generic volume resource that is
                                provisioned/attached using an exec based plugin.
                                Deprecated: FlexVolume is deprecated. Consider using a CSIDriver instead.
                              properties:
                                driver:
                                  description: driver is the name of the driver to
                                    use for this volume.
                                  type: string
                                fsType:
                                  description: |-
                                    fsType is the filesystem type to mount.
                                    Must be a filesystem type supported by the host operating system.
                                    Ex. "ext4", "xfs", "ntfs". The default filesystem depends on FlexVolume script.
                                  type: string
                                options:
                                  additionalProperties:
                                    type: string
                                  description: 'options is Optional: this field holds
                                    extra command options if any.'
                                  type: object
                                readOnly:
                                  description: |-
                                    readOnly is Optional: defaults to false (read/write). ReadOnly here will force
                                    the ReadOnly setting in VolumeMounts.
                                  type: boolean
                                secretRef:
                                  description: |-
                                    secretRef is Optional: secretRef is reference to the secret object containing
                                    sensitive information to pass to the plugin scripts. This may be
                                    empty if no secret object is specified. If the secret object
                                    contains more than one secret, all secrets are passed to the plugin
                                    scripts.
                                  properties:
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                  type: object
                                  x-kubernetes-map-type: atomic
                              required:
                              - driver
                              type: object
                            flocker:
                              description: |-
                                flocker represents a Flocker volume attached to a kubelet's host machine. This depends on the Flocker control service being running.
                                Deprecated: Flocker is deprecated and the in-tree flocker type is no longer supported.
                              properties:
                                datasetName:
                                  description: |-
                                    datasetName is Name of the dataset stored as metadata -> name on the dataset for Flocker
                                    should be considered as deprecated
                                  type: string
                                datasetUUID:
                                  description: datasetUUID is the UUID of the dataset.
                                    This is unique identifier of a Flocker dataset
                                  type: string
                              type: object
                            gcePersistentDisk:
                              description: |-
                                gcePersistentDisk represents a GCE Disk resource that is attached to a
                                kubelet's host machine and then exposed to the pod.
                                Deprecated: GCEPersistentDisk is deprecated. All operations for the in-tree
                                gcePersistentDisk type are redirected to the pd.csi.storage.gke.io CSI driver.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk
                              properties:
                                fsType:
                                  description: |-
                                    fsType is filesystem type of the volume that you want to mount.
                                    Tip: Ensure that the filesystem type is supported by the host operating system.
                                    Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                                    More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk
                                  type: string
                                partition:
                                  description: |-
                                    partition is the partition in the volume that you want to mount.
                                    If omitted, the default is to mount by volume name.
                                    Examples: For volume /dev/sda1, you specify the partition as "1".
                                    Similarly, the volume partition for /dev/sda is "0" (or you can leave the property empty).
                                    More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk
                                  format: int32
                                  type: integer
                                pdName:
                                  description: |-
                                    pdName is unique name of the PD resource in GCE. Used to identify the disk in GCE.
                                    More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk
                                  type: string
                                readOnly:
                                  description: |-
                                    readOnly here will force the ReadOnly setting in VolumeMounts.
                                    Defaults to false.
                                    More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk
                                  type: boolean
                              required:
                              - pdName
                              type: object
                            gitRepo:
                              description: |-
                                gitRepo represents a git repository at a particular revision.
                                Deprecated: GitRepo is deprecated. To provision a container with a git repo, mount an
                                EmptyDir into an InitContainer that clones the repo using git, then mount the EmptyDir
                                into the Pod's container.
                              properties:
                                directory:
                                  description: |-
                                    directory is the target directory name.
                                    Must not contain or start with '..'.  If '.' is supplied, the volume directory will be the
                                    git repository.  Otherwise, if specified, the volume will contain the git repository in
                                    the subdirectory with the given name.
                                  type: string
                                repository:
                                  description: repository is the URL
                                  type: string
                                revision:
                                  description: revision is the commit hash for the
                                    specified revision.
                                  type: string
                              required:
                              - repository
                              type: object
                            glusterfs:
                              description: |-
                                glusterfs represents a Glusterfs mount on the host that shares a pod's lifetime.
                                Deprecated: Glusterfs is deprecated and the in-tree glusterfs type is no longer supported.
                              properties:
                                endpoints:
                                  description: endpoints is the endpoint name that
                                    details Glusterfs topology.
                                  type: string
                                path:
                                  description: |-
                                    path is the Glusterfs volume path.
                                    More info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod
                                  type: string
                                readOnly:
                                  description: |-
                                    readOnly here will force the Glusterfs volume to be mounted with read-only permissions.
                                    Defaults to false.
                                    More info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod
                                  type: boolean
                              required:
                              - endpoints
                              - path
                              type: object
                            hostPath:
                              description: |-
                                hostPath represents a pre-existing file or directory on the host
                                machine that is directly exposed to the container. This is generally
                                used for system agents or other privileged things that are allowed
                                to see the host machine. Most containers will NOT need this.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath
                              properties:
                                path:
                                  description: |-
                                    path of the directory on the host.
                                    If the path is a symlink, it will follow the link to the real path.
                                    More info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath
                                  type: string
                                type:
                                  description: |-
                                    type for HostPath Volume
                                    Defaults to ""
                                    More info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath
                                  type: string
                              required:
                              - path
                              type: object
                            image:
                              description: |-
                                image represents an OCI object (a container image or artifact) pulled and mounted on the kubelet's host machine.
                                The volume is resolved at pod startup depending on which PullPolicy value is provided:

                                - Always: the kubelet always attempts to pull the reference. Container creation will fail If the pull fails.
                                - Never: the kubelet never pulls the reference and only uses a local image or artifact. Container creation will fail if the reference isn't present.
                                - IfNotPresent: the kubelet pulls if the reference isn't already present on disk. Container creation will fail if the reference isn't present and the pull fails.

                                The volume gets re-resolved if the pod gets deleted and recreated, which means that new remote content will become available on pod recreation.
                                A failure to resolve or pull the image during pod startup will block containers from starting and may add significant latency. Failures will be retried using normal volume backoff and will be reported on the pod reason and message.
                                The types of objects that may be mounted by this volume are defined by the container runtime implementation on a host machine and at minimum must include all valid types supported by the container image field.
                                The OCI object gets mounted in a single directory (spec.containers[*].volumeMounts.mountPath) by merging the manifest layers in the same way as for container images.
                                The volume will be mounted read-only (ro) and non-executable files (noexec).
                                Sub path mounts for containers are not supported (spec.containers[*].volumeMounts.subpath) before 1.33.
                                The field spec.securityContext.fsGroupChangePolicy has no effect on this volume type.
                              properties:
                                pullPolicy:
                                  description: |-
                                    Policy for pulling OCI objects. Possible values are:
                                    Always: the kubelet always attempts to pull the reference. Container creation will fail If the pull fails.
                                    Never: the kubelet never pulls the reference and only uses a local image or artifact. Container creation will fail if the reference isn't present.
                                    IfNotPresent: the kubelet pulls if the reference isn't already present on disk. Container creation will fail if the reference isn't present and the pull fails.
                                    Defaults to Always if :latest tag is specified, or IfNotPresent otherwise.
                                  type: string
                                reference:
                                  description: |-
                                    Required: Image or artifact reference to be used.
                                    Behaves in the same way as pod.spec.containers[*].image.
                                    Pull secrets will be assembled in the same way as for the container image by looking up node credentials, SA image pull secrets, and pod spec image pull secrets.
                                    More info: https://kubernetes.io/docs/concepts/containers/images
                                    This field is optional to allow higher level config management to default or override
                                    container images in workload controllers like Deployments and StatefulSets.
                                  type: string
                              type: object
                            iscsi:
                              description: |-
                                iscsi represents an ISCSI Disk resource that is attached to a
                                kubelet's host machine and then exposed to the pod.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes/#iscsi
                              properties:
                                chapAuthDiscovery:
                                  description: chapAuthDiscovery defines whether support
                                    iSCSI Discovery CHAP authentication
                                  type: boolean
                                chapAuthSession:
                                  description: chapAuthSession defines whether support
                                    iSCSI Session CHAP authentication
                                  type: boolean
                                fsType:
                                  description: |-
                                    fsType is the filesystem type of the volume that you want to mount.
                                    Tip: Ensure that the filesystem type is supported by the host operating system.
                                    Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                                    More info: https://kubernetes.io/docs/concepts/storage/volumes#iscsi
                                  type: string
                                initiatorName:
                                  description: |-
                                    initiatorName is the custom iSCSI Initiator Name.
                                    If initiatorName is specified with iscsiInterface simultaneously, new iSCSI interface
                                    <target portal>:<volume name> will be created for the connection.
                                  type: string
                                iqn:
                                  description: iqn is the target iSCSI Qualified Name.
                                  type: string
                                iscsiInterface:
                                  default: default
                                  description: |-
                                    iscsiInterface is the interface Name that uses an iSCSI transport.
                                    Defaults to 'default' (tcp).
                                  type: string
                                lun:
                                  description: lun represents iSCSI Target Lun number.
                                  format: int32
                                  type: integer
                                portals:
                                  description: |-
                                    portals is the iSCSI Target Portal List. The portal is either an IP or ip_addr:port if the port
                                    is other than default (typically TCP ports 860 and 3260).
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                readOnly:
                                  description: |-
                                    readOnly here will force the ReadOnly setting in VolumeMounts.
                                    Defaults to false.
                                  type: boolean
                                secretRef:
                                  description: secretRef is the CHAP Secret for iSCSI
                                    target and initiator authentication
                                  properties:
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                  type: object
                                  x-kubernetes-map-type: atomic
                                targetPortal:
                                  description: |-
                                    targetPortal is iSCSI Target Portal. The Portal is either an IP or ip_addr:port if the port
                                    is other than default (typically TCP ports 860 and 3260).
                                  type: string
                              required:
                              - iqn
                              - lun
                              - targetPortal
                              type: object
                            name:
                              description: |-
                                name of the volume.
                                Must be a DNS_LABEL and unique within the pod.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            nfs:
                              description: |-
                                nfs represents an NFS mount on the host that shares a pod's lifetime
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs
                              properties:
                                path:
                                  description: |-
                                    path that is exported by the NFS server.
                                    More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs
                                  type: string
                                readOnly:
                                  description: |-
                                    readOnly here will force the NFS export to be mounted with read-only permissions.
                                    Defaults to false.
                                    More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs
                                  type: boolean
                                server:
                                  description: |-
                                    server is the hostname or IP address of the NFS server.
                                    More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs
                                  type: string
                              required:
                              - path
                              - server
                              type: object
                            persistentVolumeClaim:
                              description: |-
                                persistentVolumeClaimVolumeSource represents a reference to a
                                PersistentVolumeClaim in the same namespace.
                                More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims
                              properties:
                                claimName:
                                  description: |-
                                    claimName is the name of a PersistentVolumeClaim in the same namespace as the pod using this volume.
                                    More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims
                                  type: string
                                readOnly:
                                  description: |-
                                    readOnly Will force the ReadOnly setting in VolumeMounts.
                                    Default false.
                                  type: boolean
                              required:
                              - claimName
                              type: object
                            photonPersistentDisk:
                              description: |-
                                photonPersistentDisk represents a PhotonController persistent disk attached and mounted on kubelets host machine.
                                Deprecated: PhotonPersistentDisk is deprecated and the in-tree photonPersistentDisk type is no longer supported.
                              properties:
                                fsType:
                                  description: |-
                                    fsType is the filesystem type to mount.
                                    Must be a filesystem type supported by the host operating system.
                                    Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                                  type: string
                                pdID:
                                  description: pdID is the ID that identifies Photon
                                    Controller persistent disk
                                  type: string
                              required:
                              - pdID
                              type: object
                            portworxVolume:
                              description: |-
                                portworxVolume represents a portworx volume attached and mounted on kubelets host machine.
                                Deprecated: PortworxVolume is deprecated. All operations for the in-tree portworxVolume type
                                are redirected to the pxd.portworx.com CSI driver when the CSIMigrationPortworx feature-gate
                                is on.
                              properties:
                                fsType:
                                  description: |-
                                    fSType represents the filesystem type to mount
                                    Must be a filesystem type supported by the host operating system.
                                    Ex. "ext4", "xfs". Implicitly inferred to be "ext4" if unspecified.
                                  type: string
                                readOnly:
                                  description: |-
                                    readOnly defaults to false (read/write). ReadOnly here will force
                                    the ReadOnly setting in VolumeMounts.
                                  type: boolean
                                volumeID:
                                  description: volumeID uniquely identifies a Portworx
                                    volume
                                  type: string
                              required:
                              - volumeID
                              type: object
                            projected:
                              description: projected items for all in one resources
                                secrets, configmaps, and downward API
                              properties:
                                defaultMode:
                                  description: |-
                                    defaultMode are the mode bits used to set permissions on created files by default.
                                    Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
                                    YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
                                    Directories within the path are not affected by this setting.
                                    This might be in conflict with other options that affect the file
                                    mode, like fsGroup, and the result can be other mode bits set.
                                  format: int32
                                  type: integer
                                sources:
                                  description: |-
                                    sources is the list of volume projections. Each entry in this list
                                    handles one source.
                                  items:
                                    description: |-
                                      Projection that may be projected along with other supported volume types.
                                      Exactly one of these fields must be set.
                                    properties:
                                      clusterTrustBundle:
                                        description: |-
                                          ClusterTrustBundle allows a pod to access the `.spec.trustBundle` field
                                          of ClusterTrustBundle objects in an auto-updating file.

                                          Alpha, gated by the ClusterTrustBundleProjection feature gate.

                                          ClusterTrustBundle objects can either be selected by name, or by the
                                          combination of signer name and a label selector.

                                          Kubelet performs aggressive normalization of the PEM contents written
                                          into the pod filesystem.  Esoteric PEM features such as inter-block
                                          comments and block headers are stripped.  Certificates are deduplicated.
                                          The ordering of certificates within the file is arbitrary, and Kubelet
                                          may change the order over time.
                                        properties:
                                          labelSelector:
                                            description: |-
                                              Select all ClusterTrustBundles that match this label selector.  Only has
                                              effect if signerName is set.  Mutually-exclusive with name.  If unset,
                                              interpreted as "match nothing".  If set but empty, interpreted as "match
                                              everything".
                                            properties:
                                              matchExpressions:
                                                description: matchExpressions is a
                                                  list of label selector requirements.
                                                  The requirements are ANDed.
                                                items:
                                                  description: |-
                                                    A label selector requirement is a selector that contains values, a key, and an operator that
                                                    relates the key and values.
                                                  properties:
                                                    key:
                                                      description: key is the label
                                                        key that the selector applies
                                                        to.
                                                      type: string
                                                    operator:
                                                      description: |-
                                                        operator represents a key's relationship to a set of values.
                                                        Valid operators are In, NotIn, Exists and DoesNotExist.
                                                      type: string
                                                    values:
                                                      description: |-
                                                        values is an array of string values. If the operator is In or NotIn,
                                                        the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                        the values array must be empty. This array is replaced during a strategic
                                                        merge patch.
                                                      items:
                                                        type: string
                                                      type: array
                                                      x-kubernetes-list-type: atomic
                                                  required:
                                                  - key
                                                  - operator
                                                  type: object
                                                type: array
                                                x-kubernetes-list-type: atomic
                                              matchLabels:
                                                additionalProperties:
                                                  type: string
                                                description: |-
                                                  matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                                  map is equivalent to an element of matchExpressions, whose key field is "key", the
                                                  operator is "In", and the values array contains only "value". The requirements are ANDed.
                                                type: object
                                            type: object
                                            x-kubernetes-map-type: atomic
                                          name:
                                            description: |-
                                              Select a single ClusterTrustBundle by object name.  Mutually-exclusive
                                              with signerName and labelSelector.
                                            type: string
                                          optional:
                                            description: |-
                                              If true, don't block pod startup if the referenced ClusterTrustBundle(s)
                                              aren't available.  If using name, then the named ClusterTrustBundle is
                                              allowed not to exist.  If using signerName, then the combination of
                                              signerName and labelSelector is allowed to match zero
                                              ClusterTrustBundles.
                                            type: boolean
                                          path:
                                            description: Relative path from the volume
                                              root to write the bundle.
                                            type: string
                                          signerName:
                                            description: |-
                                              Select all ClusterTrustBundles that match this signer name.
                                              Mutually-exclusive with name.  The contents of all selected
                                              ClusterTrustBundles will be unified and deduplicated.
                                            type: string
                                        required:
                                        - path
                                        type: object
                                      configMap:
                                        description: configMap information about the
                                          configMap data to project
                                        properties:
                                          items:
                                            description: |-
                                              items if unspecified, each key-value pair in the Data field of the referenced
                                              ConfigMap will be projected into the volume as a file whose name is the
                                              key and content is the value. If specified, the listed keys will be
                                              projected into the specified paths, and unlisted keys will not be
                                              present. If a key is specified which is not present in the ConfigMap,
                                              the volume setup will error unless it is marked optional. Paths must be
                                              relative and may not contain the '..' path or start with '..'.
                                            items:
                                              description: Maps a string key to a
                                                path within a volume.
                                              properties:
                                                key:
                                                  description: key is the key to project.
                                                  type: string
                                                mode:
                                                  description: |-
                                                    mode is Optional: mode bits used to set permissions on this file.
                                                    Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
                                                    YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
                                                    If not specified, the volume defaultMode will be used.
                                                    This might be in conflict with other options that affect the file
                                                    mode, like fsGroup, and the result can be other mode bits set.
                                                  format: int32
                                                  type: integer
                                                path:
                                                  description: |-
                                                    path is the relative path of the file to map the key to.
                                                    May not be an absolute path.
                                                    May not contain the path element '..'.
                                                    May not start with the string '..'.
                                                  type: string
                                              required:
                                              - key
                                              - path
                                              type: object
                                            type: array
                                            x-kubernetes-list-type: atomic
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: optional specify whether
                                              the ConfigMap or its keys must be defined
                                            type: boolean
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      downwardAPI:
                                        description: downwardAPI information about
                                          the downwardAPI data to project
                                        properties:
                                          items:
                                            description: Items is a list of DownwardAPIVolume
                                              file
                                            items:
                                              description: DownwardAPIVolumeFile represents
                                                information to create the file containing
                                                the pod field
                                              properties:
                                                fieldRef:
                                                  description: 'Required: Selects
                                                    a field of the pod: only annotations,
                                                    labels, name, namespace and uid
                                                    are supported.'
                                                  properties:
                                                    apiVersion:
                                                      description: Version of the
                                                        schema the FieldPath is written
                                                        in terms of, defaults to "v1".
                                                      type: string
                                                    fieldPath:
                                                      description: Path of the field
                                                        to select in the specified
                                                        API version.
                                                      type: string
                                                  required:
                                                  - fieldPath
                                                  type: object
                                                  x-kubernetes-map-type: atomic
                                                mode:
                                                  description: |-
                                                    Optional: mode bits used to set permissions on this file, must be an octal value
                                                    between 0000 and 0777 or a decimal value between 0 and 511.
                                                    YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
                                                    If not specified, the volume defaultMode will be used.
                                                    This might be in conflict with other options that affect the file
                                                    mode, like fsGroup, and the result can be other mode bits set.
                                                  format: int32
                                                  type: integer
                                                path:
                                                  description: 'Required: Path is  the
                                                    relative path name of the file
                                                    to be created. Must not be absolute
                                                    or contain the ''..'' path. Must
                                                    be utf-8 encoded. The first item
                                                    of the relative path must not
                                                    start with ''..'''
                                                  type: string
                                                resourceFieldRef:
                                                  description: |-
                                                    Selects a resource of the container: only resources limits and requests
                                                    (limits.cpu, limits.memory, requests.cpu and requests.memory) are currently supported.
                                                  properties:
                                                    containerName:
                                                      description: 'Container name:
                                                        required for volumes, optional
                                                        for env vars'
                                                      type: string
                                                    divisor:
                                                      anyOf:
                                                      - type: integer
                                                      - type: string
                                                      description: Specifies the output
                                                        format of the exposed resources,
                                                        defaults to "1"
                                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                                      x-kubernetes-int-or-string: true
                                                    resource:
                                                      description: 'Required: resource
                                                        to select'
                                                      type: string
                                                  required:
                                                  - resource
                                                  type: object
                                                  x-kubernetes-map-type: atomic
                                              required:
                                              - path
                                              type: object
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        type: object
                                      podCertificate:
                                        description: |-
                                          Projects an auto-rotating credential bundle (private key and certificate
                                          chain) that the pod can use either as a TLS client or server.

                                          Kubelet generates a private key and uses it to send a
                                          PodCertificateRequest to the named signer.  Once the signer approves the
                                          request and issues a certificate chain, Kubelet writes the key and
                                          certificate chain to the pod filesystem.  The pod does not start until
                                          certificates have been issued for each podCertificate projected volume
                                          source in its spec.

                                          Kubelet will begin trying to rotate the certificate at the time indicated
                                          by the signer using the PodCertificateRequest.Status.BeginRefreshAt
                                          timestamp.

                                          Kubelet can write a single file, indicated by the credentialBundlePath
                                          field, or separate files, indicated by the keyPath and
                                          certificateChainPath fields.

                                          The credential bundle is a single file in PEM format.  The first PEM
                                          entry is the private key (in PKCS#8 format), and the remaining PEM
                                          entries are the certificate chain issued by the signer (typically,
                                          signers will return their certificate chain in leaf-to-root order).

                                          Prefer using the credential bundle format, since your application code
                                          can read it atomically.  If you use keyPath and certificateChainPath,
                                          your application must make two separate file reads. If these coincide
                                          with a certificate rotation, it is possible that the private key and leaf
                                          certificate you read may not correspond to each other.  Your application
                                          will need to check for this condition, and re-read until they are
                                          consistent.

                                          The named signer controls chooses the format of the certificate it
                                          issues; consult the signer implementation's documentation to learn how to
                                          use the certificates it issues.
                                        properties:
                                          certificateChainPath:
                                            description: |-
                                              Write the certificate chain at this path in the projected volume.

                                              Most applications should use credentialBundlePath.  When using keyPath
                                              and certificateChainPath, your application needs to check that the key
                                              and leaf certificate are consistent, because it is possible to read the
                                              files mid-rotation.
                                            type: string
                                          credentialBundlePath:
                                            description: |-
                                              Write the credential bundle at this path in the projected volume.

                                              The credential bundle is a single file that contains multiple PEM blocks.
                                              The first PEM block is a PRIVATE KEY block, containing a PKCS#8 private
                                              key.

                                              The remaining blocks are CERTIFICATE blocks, containing the issued
                                              certificate chain from the signer (leaf and any intermediates).

                                              Using credentialBundlePath lets your Pod's application code make a single
                                              atomic read that retrieves a consistent key and certificate chain.  If you
                                              project them to separate files, your application code will need to
                                              additionally check that the leaf certificate was issued to the key.
                                            type: string
                                          keyPath:
                                            description: |-
                                              Write the key at this path in the projected volume.

                                              Most applications should use credentialBundlePath.  When using keyPath
                                              and certificateChainPath, your application needs to check that the key
                                              and leaf certificate are consistent, because it is possible to read the
                                              files mid-rotation.
                                            type: string
                                          keyType:
                                            description: |-
                                              The type of keypair Kubelet will generate for the pod.

                                              Valid values are "RSA3072", "RSA4096", "ECDSAP256", "ECDSAP384",
                                              "ECDSAP521", and "ED25519".
                                            type: string
                                          maxExpirationSeconds:
                                            description: |-
                                              maxExpirationSeconds is the maximum lifetime permitted for the
                                              certificate.

                                              Kubelet copies this value verbatim into the PodCertificateRequests it
                                              generates for this projection.

                                              If omitted, kube-apiserver will set it to 86400(24 hours). kube-apiserver
                                              will reject values shorter than 3600 (1 hour).  The maximum allowable
                                              value is 7862400 (91 days).

                                              The signer implementation is then free to issue a certificate with any
                                              lifetime *shorter* than MaxExpirationSeconds, but no shorter than 3600
                                              seconds (1 hour).  This constraint is enforced by kube-apiserver.
                                              `kubernetes.io` signers will never issue certificates with a lifetime
                                              longer than 24 hours.
                                            format: int32
                                            type: integer
                                          signerName:
                                            description: Kubelet's generated CSRs
                                              will be addressed to this signer.
                                            type: string
                                          userAnnotations:
                                            additionalProperties:
                                              type: string
                                            description: |-
                                              userAnnotations allow pod authors to pass additional information to
                                              the signer implementation.  Kubernetes does not restrict or validate this
                                              metadata in any way.

                                              These values are copied verbatim into the `spec.unverifiedUserAnnotations` field of
                                              the PodCertificateRequest objects that Kubelet creates.

                                              Entries are subject to the same validation as object metadata annotations,
                                              with the addition that all keys must be domain-prefixed. No restrictions
                                              are placed on values, except an overall size limitation on the entire field.

                                              Signers should document the keys and values they support. Signers should
                                              deny requests that contain keys they do not recognize.
                                            type: object
                                        required:
                                        - keyType
                                        - signerName
                                        type: object
                                      secret:
                                        description: secret information about the
                                          secret data to project
                                        properties:
                                          items:
                                            description: |-
                                              items if unspecified, each key-value pair in the Data field of the referenced
                                              Secret will be projected into the volume as a file whose name is the
                                              key and content is the value. If specified, the listed keys will be
                                              projected into the specified paths, and unlisted keys will not be
                                              present. If a key is specified which is not present in the Secret,
                                              the volume setup will error unless it is marked optional. Paths must be
                                              relative and may not contain the '..' path or start with '..'.
                                            items:
                                              description: Maps a string key to a
                                                path within a volume.
                                              properties:
                                                key:
                                                  description: key is the key to project.
                                                  type: string
                                                mode:
                                                  description: |-
                                                    mode is Optional: mode bits used to set permissions on this file.
                                                    Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
                                                    YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
                                                    If not specified, the volume defaultMode will be used.
                                                    This might be in conflict with other options that affect the file
                                                    mode, like fsGroup, and the result can be other mode bits set.
                                                  format: int32
                                                  type: integer
                                                path:
                                                  description: |-
                                                    path is the relative path of the file to map the key to.
                                                    May not be an absolute path.
                                                    May not contain the path element '..'.
                                                    May not start with the string '..'.
                                                  type: string
                                              required:
                                              - key
                                              - path
                                              type: object
                                            type: array
                                            x-kubernetes-list-type: atomic
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: optional field specify whether
                                              the Secret or its key must be defined
                                            type: boolean
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      serviceAccountToken:
                                        description: serviceAccountToken is information
                                          about the serviceAccountToken data to project
                                        properties:
                                          audience:
                                            description: |-
                                              audience is the intended audience of the token. A recipient of a token
                                              must identify itself with an identifier specified in the audience of the
                                              token, and otherwise should reject the token. The audience defaults to the
                                              identifier of the apiserver.
                                            type: string
                                          expirationSeconds:
                                            description: |-
                                              expirationSeconds is the requested duration of validity of the service
                                              account token. As the token approaches expiration, the kubelet volume
                                              plugin will proactively rotate the service account token. The kubelet will
                                              start trying to rotate the token if the token is older than 80 percent of
                                              its time to live or if the token is older than 24 hours.Defaults to 1 hour
                                              and must be at least 10 minutes.
                                            format: int64
                                            type: integer
                                          path:
                                            description: |-
                                              path is the path relative to the mount point of the file to project the
                                              token into.
                                            type: string
                                        required:
                                        - path
                                        type: object
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                              type: object
                            quobyte:
                              description: |-
                                quobyte represents a Quobyte mount on the host that shares a pod's lifetime.
                                Deprecated: Quobyte is deprecated and the in-tree quobyte type is no longer supported.
                              properties:
                                group:
                                  description: |-
                                    group to map volume access to
                                    Default is no group
                                  type: string
                                readOnly:
                                  description: |-
                                    readOnly here will force the Quobyte volume to be mounted with read-only permissions.
                                    Defaults to false.
                                  type: boolean
                                registry:
                                  description: |-
                                    registry represents a single or multiple Quobyte Registry services
                                    specified as a string as host:port pair (multiple entries are separated with commas)
                                    which acts as the central registry for volumes
                                  type: string
                                tenant:
                                  description: |-
                                    tenant owning the given Quobyte volume in the Backend
                                    Used with dynamically provisioned Quobyte volumes, value is set by the plugin
                                  type: string
                                user:
                                  description: |-
                                    user to map volume access to
                                    Defaults to serivceaccount user
                                  type: string
                                volume:
                                  description: volume is a string that references
                                    an already created Quobyte volume by name.
                                  type: string
                              required:
                              - registry
                              - volume
                              type: object
                            rbd:
                              description: |-
                                rbd represents a Rados Block Device mount on the host that shares a pod's lifetime.
                                Deprecated: RBD is deprecated and the in-tree rbd type is no longer supported.
                              properties:
                                fsType:
                                  description: |-
                                    fsType is the filesystem type of the volume that you want to mount.
                                    Tip: Ensure that the filesystem type is supported by the host operating system.
                                    Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                                    More info: https://kubernetes.io/docs/concepts/storage/volumes#rbd
                                  type: string
                                image:
                                  description: |-
                                    image is the rados image name.
                                    More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
                                  type: string
                                keyring:
                                  default: /etc/ceph/keyring
                                  description: |-
                                    keyring is the path to key ring for RBDUser.
                                    Default is /etc/ceph/keyring.
                                    More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
                                  type: string
                                monitors:
                                  description: |-
                                    monitors is a collection of Ceph monitors.
                                    More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                                pool:
                                  default: rbd
                                  description: |-
                                    pool is the rados pool name.
                                    Default is rbd.
                                    More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
                                  type: string
                                readOnly:
                                  description: |-
                                    readOnly here will force the ReadOnly setting in VolumeMounts.
                                    Defaults to false.
                                    More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
                                  type: boolean
                                secretRef:
                                  description: |-
                                    secretRef is name of the authentication secret for RBDUser. If provided
                                    overrides keyring.
                                    Default is nil.
                                    More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
                                  properties:
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                  type: object
                                  x-kubernetes-map-type: atomic
                                user:
                                  default: admin
                                  description: |-
                                    user is the rados user name.
                                    Default is admin.
                                    More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
                                  type: string
                              required:
                              - image
                              - monitors
                              type: object
                            scaleIO:
                              description: |-
                                scaleIO represents a ScaleIO persistent volume attached and mounted on Kubernetes nodes.
                                Deprecated: ScaleIO is deprecated and the in-tree scaleIO type is no longer supported.
                              properties:
                                fsType:
                                  default: xfs
                                  description: |-
                                    fsType is the filesystem type to mount.
                                    Must be a filesystem type supported by the host operating system.
                                    Ex. "ext4", "xfs", "ntfs".
                                    Default is "xfs".
                                  type: string
                                gateway:
                                  description: gateway is the host address of the
                                    ScaleIO API Gateway.
                                  type: string
                                protectionDomain:
                                  description: protectionDomain is the name of the
                                    ScaleIO Protection Domain for the configured storage.
                                  type: string
                                readOnly:
                                  description: |-
                                    readOnly Defaults to false (read/write). ReadOnly here will force
                                    the ReadOnly setting in VolumeMounts.
                                  type: boolean
                                secretRef:
                                  description: |-
                                    secretRef references to the secret for ScaleIO user and other
                                    sensitive information. If this is not provided, Login operation will fail.
                                  properties:
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                  type: object
                                  x-kubernetes-map-type: atomic
                                sslEnabled:
                                  description: sslEnabled Flag enable/disable SSL
                                    communication with Gateway, default false
                                  type: boolean
                                storageMode:
                                  default: ThinProvisioned
                                  description: |-
                                    storageMode indicates whether the storage for a volume should be ThickProvisioned or ThinProvisioned.
                                    Default is ThinProvisioned.
                                  type: string
                                storagePool:
                                  description: storagePool is the ScaleIO Storage
                                    Pool associated with the protection domain.
                                  type: string
                                system:
                                  description: system is the name of the storage system
                                    as configured in ScaleIO.
                                  type: string
                                volumeName:
                                  description: |-
                                    volumeName is the name of a volume already created in the ScaleIO system
                                    that is associated with this volume source.
                                  type: string
                              required:
                              - gateway
                              - secretRef
                              - system
                              type: object
                            secret:
                              description: |-
                                secret represents a secret that should populate this volume.
                                More info: https://kubernetes.io/docs/concepts/storage/volumes#secret
                              properties:
                                defaultMode:
                                  description: |-
                                    defaultMode is Optional: mode bits used to set permissions on created files by default.
                                    Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
                                    YAML accepts both octal and decimal values, JSON requires decimal values
                                    for mode bits. Defaults to 0644.
                                    Directories within the path are not affected by this setting.
                                    This might be in conflict with other options that affect the file
                                    mode, like fsGroup, and the result can be other mode bits set.
                                  format: int32
                                  type: integer
                                items:
                                  description: |-
                                    items If unspecified, each key-value pair in the Data field of the referenced
                                    Secret will be projected into the volume as a file whose name is the
                                    key and content is the value. If specified, the listed keys will be
                                    projected into the specified paths, and unlisted keys will not be
                                    present. If a key is specified which is not present in the Secret,
                                    the volume setup will error unless it is marked optional. Paths must be
                                    relative and may not contain the '..' path or start with '..'.
                                  items:
                                    description: Maps a string key to a path within
                                      a volume.
                                    properties:
                                      key:
                                        description: key is the key to project.
                                        type: string
                                      mode:
                                        description: |-
                                          mode is Optional: mode bits used to set permissions on this file.
                                          Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
                                          YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
                                          If not specified, the volume defaultMode will be used.
                                          This might be in conflict with other options that affect the file
                                          mode, like fsGroup, and the result can be other mode bits set.
                                        format: int32
                                        type: integer
                                      path:
                                        description: |-
                                          path is the relative path of the file to map the key to.
                                          May not be an absolute path.
                                          May not contain the path element '..'.
                                          May not start with the string '..'.
                                        type: string
                                    required:
                                    - key
                                    - path
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                optional:
                                  description: optional field specify whether the
                                    Secret or its keys must be defined
                                  type: boolean
                                secretName:
                                  description: |-
                                    secretName is the name of the secret in the pod's namespace to use.
                                    More info: https://kubernetes.io/docs/concepts/storage/volumes#secret
                                  type: string
                              type: object
                            storageos:
                              description: |-
                                storageOS represents a StorageOS volume attached and mounted on Kubernetes nodes.
                                Deprecated: StorageOS is deprecated and the in-tree storageos type is no longer supported.
                              properties:
                                fsType:
                                  description: |-
                                    fsType is the filesystem type to mount.
                                    Must be a filesystem type supported by the host operating system.
                                    Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                                  type: string
                                readOnly:
                                  description: |-
                                    readOnly defaults to false (read/write). ReadOnly here will force
                                    the ReadOnly setting in VolumeMounts.
                                  type: boolean
                                secretRef:
                                  description: |-
                                    secretRef specifies the secret to use for obtaining the StorageOS API
                                    credentials.  If not specified, default values will be attempted.
                                  properties:
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                  type: object
                                  x-kubernetes-map-type: atomic
                                volumeName:
                                  description: |-
                                    volumeName is the human-readable name of the StorageOS volume.  Volume
                                    names are only unique within a namespace.
                                  type: string
                                volumeNamespace:
                                  description: |-
                                    volumeNamespace specifies the scope of the volume within StorageOS.  If no
                                    namespace is specified then the Pod's namespace will be used.  This allows the
                                    Kubernetes name scoping to be mirrored within StorageOS for tighter integration.
                                    Set VolumeName to any name to override the default behaviour.
                                    Set to "default" if you are not using namespaces within StorageOS.
                                    Namespaces that do not pre-exist within StorageOS will be created.
                                  type: string
                              type: object
                            vsphereVolume:
                              description: |-
                                vsphereVolume represents a vSphere volume attached and mounted on kubelets host machine.
                                Deprecated: VsphereVolume is deprecated. All operations for the in-tree vsphereVolume type
                                are redirected to the csi.vsphere.vmware.com CSI driver.
                              properties:
                                fsType:
                                  description: |-
                                    fsType is filesystem type to mount.
                                    Must be a filesystem type supported by the host operating system.
                                    Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
                                  type: string
                                storagePolicyID:
                                  description: storagePolicyID is the storage Policy
                                    Based Management (SPBM) profile ID associated
                                    with the StoragePolicyName.
                                  type: string
                                storagePolicyName:
                                  description: storagePolicyName is the storage Policy
                                    Based Management (SPBM) profile name.
                                  type: string
                                volumePath:
                                  description: volumePath is the path that identifies
                                    vSphere volume vmdk
                                  type: string
                              required:
                              - volumePath
                              type: object
                          required:
                          - name
                          type: object
                        minItems: 1
                        type: array
                    type: object
                    x-kubernetes-validations:
                    - message: serviceAccountName must not be empty if specified
                      rule: '!has(self.serviceAccountName) || self.serviceAccountName.size()
                        > 0'
                  podDisruptionBudget:
                    description: PodDisruptionBudget controls voluntary disruption
                      tolerance.
                    properties:
                      maxUnavailable:
                        anyOf:
                        - type: integer
                        - type: string
                        description: MaxUnavailable is the maximum number of pods
                          that can be disrupted simultaneously.
                        x-kubernetes-int-or-string: true
                      minAvailable:
                        anyOf:
                        - type: integer
                        - type: string
                        description: MinAvailable is the minimum number of pods that
                          must remain available.
                        x-kubernetes-int-or-string: true
                    type: object
                    x-kubernetes-validations:
                    - message: at least one of minAvailable or maxUnavailable must
                        be specified
                      rule: has(self.minAvailable) || has(self.maxUnavailable)
                    - message: minAvailable and maxUnavailable are mutually exclusive
                      rule: '!(has(self.minAvailable) && has(self.maxUnavailable))'
                  replicas:
                    default: 1
                    description: Replicas is the desired Pod replica count.
                    format: int32
                    minimum: 0
                    type: integer
                  resources:
                    description: Resources defines CPU/memory requests and limits.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  storage:
                    description: Storage defines PVC configuration.
                    properties:
                      mountPath:
                        default: /.ogx
                        description: MountPath is the container mount path for the
                          PVC.
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Size is the size of the PVC.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    type: object
                    x-kubernetes-validations:
                    - message: mountPath must not be empty if specified
                      rule: '!has(self.mountPath) || self.mountPath.size() > 0'
                    - message: size must be a positive quantity
                      rule: '!has(self.size) || quantity(self.size).isGreaterThan(quantity(''0''))'
                  topologySpreadConstraints:
                    description: TopologySpreadConstraints defines Pod spreading rules.
                    items:
                      description: TopologySpreadConstraint specifies how to spread
                        matching pods among the given topology.
                      properties:
                        labelSelector:
                          description: |-
                            LabelSelector is used to find matching pods.
                            Pods that match this label selector are counted to determine the number of pods
                            in their corresponding topology domain.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        matchLabelKeys:
                          description: |-
                            MatchLabelKeys is a set of pod label keys to select the pods over which
                            spreading will be calculated. The keys are used to lookup values from the
                            incoming pod labels, those key-value labels are ANDed with labelSelector
                            to select the group of existing pods over which spreading will be calculated
                            for the incoming pod. The same key is forbidden to exist in both MatchLabelKeys and LabelSelector.
                            MatchLabelKeys cannot be set when LabelSelector isn't set.
                            Keys that don't exist in the incoming pod labels will
                            be ignored. A null or empty list means only match against labelSelector.

                            This is a beta field and requires the MatchLabelKeysInPodTopologySpread feature gate to be enabled (enabled by default).
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                        maxSkew:
                          description: |-
                            MaxSkew describes the degree to which pods may be unevenly distributed.
                            When `whenUnsatisfiable=DoNotSchedule`, it is the maximum permitted difference
                            between the number of matching pods in the target topology and the global minimum.
                            The global minimum is the minimum number of matching pods in an eligible domain
                            or zero if the number of eligible domains is less than MinDomains.
                            For example, in a 3-zone cluster, MaxSkew is set to 1, and pods with the same
                            labelSelector spread as 2/2/1:
                            In this case, the global minimum is 1.
                            | zone1 | zone2 | zone3 |
                            |  P P  |  P P  |   P   |
                            - if MaxSkew is 1, incoming pod can only be scheduled to zone3 to become 2/2/2;
                            scheduling it onto zone1(zone2) would make the ActualSkew(3-1) on zone1(zone2)
                            violate MaxSkew(1).
                            - if MaxSkew is 2, incoming pod can be scheduled onto any zone.
                            When `whenUnsatisfiable=ScheduleAnyway`, it is used to give higher precedence
                            to topologies that satisfy it.
                            It's a required field. Default value is 1 and 0 is not allowed.
                          format: int32
                          type: integer
                        minDomains:
                          description: |-
                            MinDomains indicates a minimum number of eligible domains.
                            When the number of eligible domains with matching topology keys is less than minDomains,
                            Pod Topology Spread treats "global minimum" as 0, and then the calculation of Skew is performed.
                            And when the number of eligible domains with matching topology keys equals or greater than minDomains,
                            this value has no effect on scheduling.
                            As a result, when the number of eligible domains is less than minDomains,
                            scheduler won't schedule more than maxSkew Pods to those domains.
                            If value is nil, the constraint behaves as if MinDomains is equal to 1.
                            Valid values are integers greater than 0.
                            When value is not nil, WhenUnsatisfiable must be DoNotSchedule.

                            For example, in a 3-zone cluster, MaxSkew is set to 2, MinDomains is set to 5 and pods with the same
                            labelSelector spread as 2/2/2:
                            | zone1 | zone2 | zone3 |
                            |  P P  |  P P  |  P P  |
                            The number of domains is less than 5(MinDomains), so "global minimum" is treated as 0.
                            In this situation, new pod with the same labelSelector cannot be scheduled,
                            because computed skew will be 3(3 - 0) if new Pod is scheduled to any of the three zones,
                            it will violate MaxSkew.
                          format: int32
                          type: integer
                        nodeAffinityPolicy:
                          description: |-
                            NodeAffinityPolicy indicates how we will treat Pod's nodeAffinity/nodeSelector
                            when calculating pod topology spread skew. Options are:
                            - Honor: only nodes matching nodeAffinity/nodeSelector are included in the calculations.
                            - Ignore: nodeAffinity/nodeSelector are ignored. All nodes are included in the calculations.

                            If this value is nil, the behavior is equivalent to the Honor policy.
                          type: string
                        nodeTaintsPolicy:
                          description: |-
                            NodeTaintsPolicy indicates how we will treat node taints when calculating
                            pod topology spread skew. Options are:
                            - Honor: nodes without taints, along with tainted nodes for which the incoming pod
                            has a toleration, are included.
                            - Ignore: node taints are ignored. All nodes are included.

                            If this value is nil, the behavior is equivalent to the Ignore policy.
                          type: string
                        topologyKey:
                          description: |-
                            TopologyKey is the key of node labels. Nodes that have a label with this key
                            and identical values are considered to be in the same topology.
                            We consider each <key, value> as a "bucket", and try to put balanced number
                            of pods into each bucket.
                            We define a domain as a particular instance of a topology.
                            Also, we define an eligible domain as a domain whose nodes meet the requirements of
                            nodeAffinityPolicy and nodeTaintsPolicy.
                            e.g. If TopologyKey is "kubernetes.io/hostname", each Node is a domain of that topology.
                            And, if TopologyKey is "topology.kubernetes.io/zone", each zone is a domain of that topology.
                            It's a required field.
                          type: string
                        whenUnsatisfiable:
                          description: |-
                            WhenUnsatisfiable indicates how to deal with a pod if it doesn't satisfy
                            the spread constraint.
                            - DoNotSchedule (default) tells the scheduler not to schedule it.
                            - ScheduleAnyway tells the scheduler to schedule the pod in any location,
                              but giving higher precedence to topologies that would help reduce the
                              skew.
                            A constraint is considered "Unsatisfiable" for an incoming pod
                            if and only if every possible node assignment for that pod would violate
                            "MaxSkew" on some topology.
                            For example, in a 3-zone cluster, MaxSkew is set to 1, and pods with the same
                            labelSelector spread as 3/1/1:
                            | zone1 | zone2 | zone3 |
                            | P P P |   P   |   P   |
                            If WhenUnsatisfiable is set to DoNotSchedule, incoming pod can only be scheduled
                            to zone2(zone3) to become 3/2/1(3/1/2) as ActualSkew(2-1) on zone2(zone3) satisfies
                            MaxSkew(1). In other words, the cluster can still be imbalanced, but scheduler
                            won't make it *more* imbalanced.
                            It's a required field.
                          type: string
                      required:
                      - maxSkew
                      - topologyKey
                      - whenUnsatisfiable
                      type: object
                    minItems: 1
                    type: array
                  workers:
                    description: Workers configures the number of uvicorn worker processes.
                    format: int32
                    minimum: 1
                    type: integer
                type: object
            required:
            - distribution
            type: object
            x-kubernetes-validations:
            - message: overrideConfig and providers are mutually exclusive
              rule: '!has(self.overrideConfig) || !has(self.providers)'
            - message: overrideConfig and resources are mutually exclusive
              rule: '!has(self.overrideConfig) || !has(self.resources)'
            - message: overrideConfig and storage are mutually exclusive
              rule: '!has(self.overrideConfig) || !has(self.storage)'
            - message: overrideConfig and disabledAPIs are mutually exclusive
              rule: '!has(self.overrideConfig) || !has(self.disabledAPIs)'
            - message: overrideConfig and baseConfig are mutually exclusive
              rule: '!has(self.overrideConfig) || !has(self.baseConfig)'
            - message: inference cannot be both in providers and disabledAPIs
              rule: '!has(self.providers) || !has(self.disabledAPIs) || !self.disabledAPIs.exists(d,
                d == ''inference'') || !has(self.providers.inference)'
            - message: vector_io cannot be both in providers and disabledAPIs
              rule: '!has(self.providers) || !has(self.disabledAPIs) || !self.disabledAPIs.exists(d,
                d == ''vector_io'') || !has(self.providers.vectorIo)'
            - message: tool_runtime cannot be both in providers and disabledAPIs
              rule: '!has(self.providers) || !has(self.disabledAPIs) || !self.disabledAPIs.exists(d,
                d == ''tool_runtime'') || !has(self.providers.toolRuntime)'
            - message: files cannot be both in providers and disabledAPIs
              rule: '!has(self.providers) || !has(self.disabledAPIs) || !self.disabledAPIs.exists(d,
                d == ''files'') || !has(self.providers.files)'
            - message: batches cannot be both in providers and disabledAPIs
              rule: '!has(self.providers) || !has(self.disabledAPIs) || !self.disabledAPIs.exists(d,
                d == ''batches'') || !has(self.providers.batches)'
            - message: responses cannot be both in providers and disabledAPIs
              rule: '!has(self.providers) || !has(self.disabledAPIs) || !self.disabledAPIs.exists(d,
                d == ''responses'') || !has(self.providers.responses)'
            - message: file_processors cannot be both in providers and disabledAPIs
              rule: '!has(self.providers) || !has(self.disabledAPIs) || !self.disabledAPIs.exists(d,
                d == ''file_processors'') || !has(self.providers.fileProcessors)'
          status:
            description: OGXServerStatus defines the observed state of OGXServer.
            properties:
              availableReplicas:
                description: AvailableReplicas is the number of available replicas.
                format: int32
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                  of the server's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              configGeneration:
                description: ConfigGeneration tracks config generation details.
                properties:
                  configMapName:
                    description: ConfigMapName is the name of the generated ConfigMap.
                    type: string
                  configVersion:
                    description: ConfigVersion is the config.yaml schema version.
                    type: integer
                  generatedAt:
                    description: GeneratedAt is the timestamp of the last generation.
                    format: date-time
                    type: string
                  observedGeneration:
                    description: ObservedGeneration is the spec generation that was
                      last processed.
                    format: int64
                    type: integer
                  providerCount:
                    description: ProviderCount is the number of configured providers.
                    type: integer
                  resourceCount:
                    description: ResourceCount is the number of registered resources.
                    type: integer
                type: object
              distributionConfig:
                description: DistributionConfig contains provider information from
                  the running server.
                properties:
                  activeDistribution:
                    type: string
                  availableDistributions:
                    additionalProperties:
                      type: string
                    type: object
                  providers:
                    items:
                      description: ProviderInfo represents a single provider from
                        the providers endpoint.
                      properties:
                        api:
                          type: string
                        config:
                          x-kubernetes-preserve-unknown-fields: true
                        health:
                          description: ProviderHealthStatus represents the health
                            status of a provider.
                          properties:
                            message:
                              type: string
                            status:
                              type: string
                          required:
                          - message
                          - status
                          type: object
                        provider_id:
                          type: string
                        provider_type:
                          type: string
                      required:
                      - api
                      - config
                      - health
                      - provider_id
                      - provider_type
                      type: object
                    type: array
                type: object
              externalURL:
                description: ExternalURL is the external URL when external access
                  is configured.
                type: string
              phase:
                description: Phase represents the current phase of the server.
                enum:
                - Pending
                - Initializing
                - Ready
                - Failed
                - Terminating
                type: string
              resolvedDistribution:
                description: ResolvedDistribution tracks the resolved image and config
                  source.
                properties:
                  configHash:
                    description: ConfigHash is the SHA256 hash of the base config
                      used.
                    type: string
                  configSource:
                    description: ConfigSource indicates the base config origin (for
                      example "configmap" or "oci-label").
                    type: string
                  image:
                    description: Image is the resolved container image reference (with
                      digest when available).
                    type: string
                type: object
              serviceURL:
                description: ServiceURL is the internal Kubernetes service URL.
                type: string
              version:
                description: Version contains version information for both operator
                  and server.
                properties:
                  lastUpdated:
                    format: date-time
                    type: string
                  operatorVersion:
                    type: string
                  serverVersion:
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
