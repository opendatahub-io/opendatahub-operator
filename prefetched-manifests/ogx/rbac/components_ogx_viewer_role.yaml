# This rule is not used by the ogx-module itself.
# It is provided to allow read-only visibility into OGX module resources.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: components-ogx-viewer-role
  labels:
    app.kubernetes.io/name: opendatahub-ogx-operator
rules:
  - apiGroups:
      - components.platform.opendatahub.io
    resources:
      - ogxs
    verbs:
      - get
      - list
      - watch
  - apiGroups:
      - components.platform.opendatahub.io
    resources:
      - ogxs/status
    verbs:
      - get
