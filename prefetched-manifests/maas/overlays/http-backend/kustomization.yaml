apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base/maas-api  # maas-api with HTTP (default)
  - ../../base/policies  # Gateway-level policies (auth, rate limits)

