apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: llm-d-batch-gateway-operator-system

resources:
- ../crd
- ../rbac
- ../manager

images:
- name: controller
  newName: localhost/llm-d-batch-gateway-operator
  newTag: dev
