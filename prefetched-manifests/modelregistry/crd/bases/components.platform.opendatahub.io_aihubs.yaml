---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: aihubs.components.platform.opendatahub.io
spec:
  group: components.platform.opendatahub.io
  names:
    kind: AIHub
    listKind: AIHubList
    plural: aihubs
    singular: aihub
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.applicationNamespace
      name: AppNamespace
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AIHub is the module CR that the platform creates to manage the
          AI Hub.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AIHubSpec defines the desired state of AIHub.
            properties:
              applicationNamespace:
                description: ApplicationNamespace is the namespace where model registries
                  and the catalog are deployed.
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?$
                type: string
            required:
            - applicationNamespace
            type: object
          status:
            description: AIHubStatus defines the observed state of AIHub.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the AIHub's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase is the top-level lifecycle phase (Ready, Not Ready).
                enum:
                - Ready
                - Not Ready
                - ""
                type: string
              releases:
                description: |-
                  Releases tracks installed component versions. The entry with name "platform"
                  participates in the platform version handshake.
                items:
                  description: ComponentRelease tracks the version of a component
                    installed by the module.
                  properties:
                    name:
                      description: Name identifies the component (e.g. "platform"
                        for the version handshake).
                      maxLength: 253
                      type: string
                    repoUrl:
                      description: RepoURL is the source repository for the component.
                      maxLength: 2048
                      type: string
                    version:
                      description: Version is the installed version string.
                      maxLength: 64
                      type: string
                  required:
                  - name
                  type: object
                type: array
            type: object
        type: object
        x-kubernetes-validations:
        - message: Only the name 'default' is allowed
          rule: self.metadata.name == 'default'
    served: true
    storage: true
    subresources:
      status: {}
