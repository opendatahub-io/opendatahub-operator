apiVersion: v1
kind: Service
metadata:
  name: kuberay-operator
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: kuberay-operator-metrics-tls
spec:
  ports:
  - name: https
    port: 8443
    targetPort: 8443
