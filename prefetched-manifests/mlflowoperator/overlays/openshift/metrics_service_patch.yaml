apiVersion: v1
kind: Service
metadata:
  name: controller-manager-metrics-service
  annotations:
    # Request OpenShift to generate and mount a serving certificate
    service.beta.openshift.io/serving-cert-secret-name: controller-manager-metrics-tls
