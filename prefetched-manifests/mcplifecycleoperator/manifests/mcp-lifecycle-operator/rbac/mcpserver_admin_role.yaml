# Generated from kubebuilder template:
# https://github.com/kubernetes-sigs/kubebuilder/blob/v4.11.1/pkg/plugins/common/kustomize/v2/scaffolds/internal/templates/config/rbac/crd_admin_role.go

# This rule is not used by the project mcp-lifecycle-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants full CRUD permissions over mcp.x-k8s.io.
# This role is intended for users authorized to modify roles and bindings within the cluster,
# enabling them to delegate specific permissions to other users or groups as needed.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: mcp-lifecycle-operator
    app.kubernetes.io/managed-by: kustomize
    # Add these permissions to the "admin" default role.
    rbac.authorization.k8s.io/aggregate-to-admin: "true"
  name: mcpserver-admin-role
rules:
- apiGroups:
  - mcp.x-k8s.io
  resources:
  - mcpservers
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
  - deletecollection
- apiGroups:
  - mcp.x-k8s.io
  resources:
  - mcpservers/status
  verbs:
  - get
