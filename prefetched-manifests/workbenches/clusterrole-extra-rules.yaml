# Helm-only ClusterRole rules not generated by controller-gen.
# Appended by hack/chart-sync-rbac.sh — edit here, then re-run the script.
- apiGroups:
    - authentication.k8s.io
  resources:
    - tokenreviews
  verbs:
    - create
- nonResourceURLs:
    - /metrics
  verbs:
    - get
