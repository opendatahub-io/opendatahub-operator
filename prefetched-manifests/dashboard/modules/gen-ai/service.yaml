apiVersion: v1
kind: Service
metadata:
  name: odh-dashboard-gen-ai-ui
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: gen-ai-proxy-tls
    service.beta.kubernetes.io/backend-protocol: HTTPS
spec:
  selector:
    deployment: gen-ai-ui
  ports:
    - name: gen-ai-ui
      protocol: TCP
      port: 8143
      targetPort: 8143
