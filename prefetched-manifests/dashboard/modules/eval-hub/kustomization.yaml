apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - service-account.yaml
  - cluster-role.yaml
  - cluster-role-binding.yaml
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml
configMapGenerator:
  - name: eval-hub-params
    env: params.env
generatorOptions:
  disableNameSuffixHash: true
vars:
  - name: eval-hub-ui-image
    objref:
      kind: ConfigMap
      name: eval-hub-params
      apiVersion: v1
    fieldref:
      fieldpath: data.eval-hub-ui-image
configurations:
  - params.yaml
