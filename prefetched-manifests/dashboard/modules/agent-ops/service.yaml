apiVersion: v1
kind: Service
metadata:
  name: odh-dashboard-agent-ops-ui
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: agent-ops-proxy-tls
    service.beta.kubernetes.io/backend-protocol: HTTPS
spec:
  selector:
    deployment: agent-ops-ui
  ports:
    - name: agent-ops-ui
      protocol: TCP
      port: 8843
      targetPort: 8843
